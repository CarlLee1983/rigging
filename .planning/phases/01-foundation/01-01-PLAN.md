---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - .dockerignore
  - bunfig.toml
  - biome.json
  - src/main.ts
  - src/bootstrap/.gitkeep
  - src/shared/application/.gitkeep
  - src/shared/infrastructure/.gitkeep
  - src/shared/presentation/.gitkeep
  - src/_template/domain/.gitkeep
  - src/_template/application/.gitkeep
  - src/_template/infrastructure/.gitkeep
  - src/_template/presentation/.gitkeep
  - src/types/.gitkeep
  - tests/unit/.gitkeep
  - tests/integration/.gitkeep
  - tests/e2e/.gitkeep
  - tests/contract/.gitkeep
  - tests/biome-contract/.gitkeep
  - drizzle/.gitkeep
  - README.md
autonomous: true
requirements: [FND-06, ARCH-01, ARCH-04]

must_haves:
  truths:
    - "開發者可在 repo 根層跑 `bun install` 取得依賴，且 `bun run lint`、`bun run typecheck`、`bun run test` 三個腳本存在"
    - "DDD 四層目錄結構（shared/kernel、shared/application、shared/infrastructure、shared/presentation、_template/{domain,application,infrastructure,presentation}）於 repo 根層就位"
    - "Biome 2.x 為唯一 lint + format 工具；repo 根層無 .eslintrc / .prettierrc 檔"
    - "package.json 不含 tsyringe / inversify 等 IoC 套件"
  artifacts:
    - path: "package.json"
      provides: "依賴宣告 + scripts (dev/lint/typecheck/test)"
      contains: "\"@biomejs/biome\""
    - path: "tsconfig.json"
      provides: "TypeScript strict 設定"
      contains: "\"strict\": true"
    - path: "biome.json"
      provides: "Biome base 配置（recommended + formatter），overrides 留空"
      contains: "\"recommended\": true"
    - path: "src/main.ts"
      provides: "FND-01 stub，Phase 2 會替換為 Elysia app"
      min_lines: 2
    - path: ".gitignore"
      provides: "Node/Bun/env 檔忽略規則"
      contains: "!.env.example"
  key_links:
    - from: "package.json"
      to: "biome.json"
      via: "scripts.lint 呼叫 `biome check .`"
      pattern: "biome check"
    - from: "tsconfig.json"
      to: "src/**/*.ts"
      via: "include"
      pattern: "src/\\*\\*/\\*.ts"
---

<objective>
Phase 1 Plan 01 — **Project Bootstrap**：鋪設可 build / lint / typecheck / test 的最小骨架。不含任何 domain feature、也不含 Biome overrides（留到 03-PLAN 當 `src/**/domain/**` 路徑已有 placeholder 時再寫）。只交付「依賴、腳本、空目錄結構、tsconfig、base biome、`src/main.ts` stub」。

Purpose: 讓後續 Plan 02-05 有可 import、可 lint、可 test 的 runtime 環境；這是 RESEARCH.md §Bootstrap Ordering step 1-4 的精確落地。
Output: package.json、tsconfig.json、biome.json (base)、bunfig.toml、.gitignore、.dockerignore、`src/main.ts` stub、所有 DDD 四層目錄 placeholder (.gitkeep)、tests/ placeholder、README.md 最小首屏。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@AGENTS.md

<rationale>
- `RESEARCH.md §Bootstrap Ordering steps 1-4` 定義本 plan 落地的精確順序：package.json → tsconfig → .gitignore → biome.json(base, 無 overrides) → src/ placeholder → src/main.ts stub。
- `RESEARCH.md §Code Examples` 提供 package.json scripts、tsconfig、.gitignore、.dockerignore 草稿；executor 須照抄並確認版本號。
- `RESEARCH.md §Standard Stack` 鎖定所有 dependency 的版本與 exact pin 規則（`better-auth@1.6.5` 無 caret 在 05-PLAN 才安裝；本 plan 只裝 P1 absolutely needed 的 devDeps + TypeBox）。
- `RESEARCH.md §DDD Scaffold & Biome Rule §目錄結構` 定義本 plan 要建立的 `.gitkeep` placeholder 清單（`src/_template/{domain,application,infrastructure,presentation}/.gitkeep` 讓 03-PLAN 的 Biome overrides 有 glob 可匹配）。
- 本 plan **不含 Biome overrides** — overrides 落在 03-PLAN，因為 override rule 引用的 `docs/decisions/0003-ddd-layering.md` 路徑於 04-PLAN 才 ship；但 base biome.json (recommended + formatter) 本 plan 必須就位以支援後續 lint 腳本。
- 本 plan **不安裝 elysia / drizzle-orm / better-auth** — 根據 RESEARCH.md Bootstrap Ordering，這些在後續 plan 安裝（drizzle 配置在 05-PLAN，elysia 在 Phase 2）。本 plan 只裝「讓 lint / typecheck / test 跑得動」的最小集合。
- **Wave 1 解耦說明**：`src/shared/kernel/` 目錄不由本 plan 以 `.gitkeep` 預佔——01-02-PLAN（同 Wave 1 並行）會直接 `mkdir -p src/shared/kernel/` 並寫入實體 TS source files（result.ts、errors.ts、id.ts、brand.ts 等）。本 plan 移除對該目錄的 `.gitkeep` 寫入以避免兩個並行 plan 同時寫同一目錄的隱性競態（checker WARNING cross-plan）。01-02 的 mkdir 是冪等的，且本 plan 不驗證該目錄存在性。
</rationale>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 package.json、tsconfig.json、bunfig.toml 與忽略檔</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (sections: "Standard Stack", "Code Examples", "Bootstrap Ordering" steps 1-3)
    - /Users/carl/Dev/CMG/Rigging/.planning/research/STACK.md (版本矩陣 + Version Compatibility)
    - /Users/carl/Dev/CMG/Rigging/AGENTS.md (Tech stack constraints in GSD:project block)
    - /Users/carl/Dev/CMG/Rigging/.planning/PROJECT.md (Constraints section — Tech stack lock)
  </read_first>
  <files>
    - package.json
    - tsconfig.json
    - bunfig.toml
    - .gitignore
    - .dockerignore
  </files>
  <action>
建立 Node/Bun package bootstrap 檔案，**版本號與 scripts 名稱須與下列精確值一致（executor 不可任意改版本）**：

**`package.json`**（依 RESEARCH.md §Code Examples §package.json scripts + §Standard Stack §Installation 鎖定版本；但本 plan 只安裝最小 devDeps，其他 deps 由 05-PLAN 補齊）：

```json
{
  "name": "rigging",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:contract": "bun test tests/biome-contract tests/contract"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "@sinclair/typebox": "^0.34.49",
    "@types/bun": "latest",
    "typescript": "^5.9"
  }
}
```

然後執行 `bun install` 產生 `bun.lockb`（該 lockfile 必須 commit）。

**`tsconfig.json`**（依 RESEARCH.md §Code Examples §tsconfig.json 草稿精確抄寫）：

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ESNext"],
    "types": ["bun-types"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "paths": {
      "@/*": ["./src/*"]
    },
    "baseUrl": "."
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"],
  "exclude": ["node_modules", "drizzle", "dist", "tests/biome-contract/**"]
}
```

注意：`tests/biome-contract/**` 刻意 exclude（因為 03-PLAN 會在該目錄放「故意違規」檔案，tsc 不該編譯）。

**`bunfig.toml`**（zero-config 起步，僅預留 test 設定位）：

```toml
[test]
# P1 zero-config; Phase 5 may add coverage thresholds
```

**`.gitignore`**（依 RESEARCH.md §Code Examples §.gitignore 草稿精確抄寫）：

```
node_modules
dist
build

# Bun
.bun
bun.lockb.bak

# Env
.env
.env.*
!.env.example

# Logs
*.log
pnpm-debug.log*

# Editor
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

**`.dockerignore`**（依 RESEARCH.md §Code Examples §.dockerignore 草稿精確抄寫）：

```
node_modules
.git
.env
.env.*
!.env.example
*.log
dist
build
docs
.planning
.github
tests
```

安裝後執行 `bun run typecheck` 驗證 tsconfig 有效（此時 src/ 尚無檔案，tsc 應直接通過或僅抱怨找不到 include，由 Task 3 修正）。
  </action>
  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f bunfig.toml && test -f .gitignore && test -f .dockerignore && grep -q '"@biomejs/biome"' package.json && grep -q '"strict": true' tsconfig.json && grep -q '!.env.example' .gitignore && ! grep -qE '"(tsyringe|inversify)"' package.json && test -f bun.lockb && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f package.json && test -f tsconfig.json && test -f bunfig.toml && test -f .gitignore && test -f .dockerignore` 全通過
    - `grep -q '"@biomejs/biome": "\^2.4.12"' package.json` 找到
    - `grep -q '"@sinclair/typebox": "\^0.34.49"' package.json` 找到
    - `grep -q '"typescript": "\^5.9"' package.json` 找到
    - `grep -q '"lint": "biome check \."' package.json` 找到
    - `grep -q '"typecheck": "tsc --noEmit"' package.json` 找到
    - `grep -q '"strict": true' tsconfig.json` 找到（ARCH-01 strict 設定）
    - `grep -q '"noUncheckedIndexedAccess": true' tsconfig.json` 找到
    - `grep -q '"exactOptionalPropertyTypes": true' tsconfig.json` 找到
    - `grep -q 'tests/biome-contract/\*\*' tsconfig.json` 在 exclude 中
    - `grep -q '!.env.example' .gitignore` 找到
    - `! grep -qE '"(tsyringe|inversify|reflect-metadata)"' package.json` 無 IoC 套件（ARCH-04）
    - `test -f bun.lockb` 找到（`bun install` 已跑過）
    - `bun run typecheck` exits 0（或 exit 非 0 但只因 src/ 尚無檔案，由 Task 3 修正）
  </acceptance_criteria>
  <done>
    Bootstrap deps 安裝完成、tsconfig strict 就位、忽略檔符合 Rigging convention、無禁用 IoC 套件。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立 Biome base 配置（無 overrides）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/package.json (確認 biome 已於 Task 1 安裝)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "DDD Scaffold & Biome Rule 實作細節 §Biome 2.x noRestrictedImports 語法" — 抄寫 base 部分，overrides 留到 03-PLAN)
    - /Users/carl/Dev/CMG/Rigging/.planning/research/STACK.md (Biome 2.4.12 section)
  </read_first>
  <files>
    - biome.json
  </files>
  <action>
建立 `biome.json` **base 配置**（formatter + recommended linter；**不含 overrides**——overrides 於 03-PLAN 當 `src/**/domain/**` 路徑已有 placeholder 後才寫）。精確抄寫以下內容：

```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.12/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": ["src/**", "tests/**"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "semicolons": "asNeeded"
    }
  }
}
```

**為何不寫 overrides 在本 plan？** 因為 03-PLAN 的 overrides 會引用 message 如 "see docs/decisions/0003-ddd-layering.md"，但 ADR 0003 於 04-PLAN 才 ship；若本 plan 提前寫 overrides，lint 不會失敗（message 只是字串）但「rule 引用不存在檔案」會使之後的 Plan 04 發現 ADR 檔名若調整就得回改 biome.json。更重要原因：本 plan 的 src/ 尚無 `src/**/domain/**` 目錄對應檔案，overrides glob 雖能寫但無 placeholder 可 lint——執行 Task 3 + 03-PLAN 後才有意義。

執行 `bun run lint` 驗證 base config 有效（此時 src/ 只有 Task 3 的 stub 檔，應全通過）。
  </action>
  <verify>
    <automated>test -f biome.json && grep -q '"recommended": true' biome.json && grep -q '"$schema"' biome.json && ! grep -q 'overrides' biome.json && ! find . -maxdepth 2 -name '.eslintrc*' -o -name '.prettierrc*' 2>/dev/null | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `test -f biome.json` 找到
    - `grep -q '"\$schema": "https://biomejs.dev/schemas/2.4.12/schema.json"' biome.json` 找到（版本對齊）
    - `grep -q '"recommended": true' biome.json` 找到
    - `grep -q '"useIgnoreFile": true' biome.json` 找到
    - `grep -q '"includes": \["src/\*\*", "tests/\*\*"\]' biome.json` 找到
    - `grep -q '"quoteStyle": "single"' biome.json` 找到
    - `grep -q '"semicolons": "asNeeded"' biome.json` 找到
    - `! grep -q '"overrides"' biome.json`（Overrides 故意留到 03-PLAN；FND-06 single config 已滿足）
    - `! find . -maxdepth 2 \( -name '.eslintrc*' -o -name '.prettierrc*' \) 2>/dev/null | grep -q .`（無 ESLint / Prettier 殘留；FND-06 single config 唯一）
  </acceptance_criteria>
  <done>
    Biome 2.4.12 base config 就位；無 ESLint/Prettier 殘留；`bun run lint` 在空 src/ 下通過。
  </done>
</task>

<task type="auto">
  <name>Task 3: 建立 DDD 四層目錄 placeholder + src/main.ts stub + README 最小首屏</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/tsconfig.json (確認 include 路徑)
    - /Users/carl/Dev/CMG/Rigging/biome.json (確認 files.includes 包含 src/**)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (sections: "DDD Scaffold & Biome Rule 實作細節 §目錄結構 (P1 ship)"、"Code Examples §src/main.ts (P1 stub)"、"Bootstrap Ordering §7")
    - /Users/carl/Dev/CMG/Rigging/.planning/PROJECT.md (Core Value — README 首屏要點)
  </read_first>
  <files>
    - src/main.ts
    - src/bootstrap/.gitkeep
    - src/shared/application/.gitkeep
    - src/shared/infrastructure/.gitkeep
    - src/shared/presentation/.gitkeep
    - src/_template/domain/.gitkeep
    - src/_template/application/.gitkeep
    - src/_template/infrastructure/.gitkeep
    - src/_template/presentation/.gitkeep
    - src/types/.gitkeep
    - tests/unit/.gitkeep
    - tests/integration/.gitkeep
    - tests/e2e/.gitkeep
    - tests/contract/.gitkeep
    - tests/biome-contract/.gitkeep
    - drizzle/.gitkeep
    - README.md
  </files>
  <action>
建立 DDD 四層骨架目錄與 stub 檔（精確清單，不能漏）：

**1. src/ 結構（per RESEARCH.md §目錄結構 P1 ship）：**

建立以下目錄並在每個葉子目錄放 `.gitkeep`（讓 git 追蹤空目錄）：
- `src/bootstrap/.gitkeep` — Phase 2 會加 app.ts/config.ts，但 P1 的 config.ts 在 05-PLAN ship
- `src/shared/kernel/` — **目錄由 01-02-PLAN 建立並填入實體檔案 (result.ts / errors.ts / id.ts / brand.ts …)**；此 plan 不建立 `.gitkeep`，避免與 01-02（同 Wave 1 平行執行）對同一目錄寫入產生耦合
- `src/shared/application/.gitkeep` — 未來 ILogger / IClock port（P3 才用）
- `src/shared/infrastructure/.gitkeep` — 未來 db/client.ts（Phase 2）
- `src/shared/presentation/.gitkeep` — 未來 error-handler.plugin.ts（Phase 2）
- `src/_template/domain/.gitkeep` — scaffold placeholder，**讓 03-PLAN 的 Biome overrides glob `src/**/domain/**` 有檔案可匹配**
- `src/_template/application/.gitkeep`
- `src/_template/infrastructure/.gitkeep`
- `src/_template/presentation/.gitkeep`
- `src/types/.gitkeep`

**2. tests/ 結構：**

- `tests/unit/.gitkeep`
- `tests/integration/.gitkeep`
- `tests/e2e/.gitkeep`
- `tests/contract/.gitkeep`
- `tests/biome-contract/.gitkeep` — 03-PLAN 會在此放「故意違規」檔案

**3. drizzle/.gitkeep** — migration 目錄（Phase 2/3 才用）

**4. `src/main.ts` stub**（per RESEARCH.md §Code Examples §src/main.ts P1 stub，但 loadConfig 於 05-PLAN 才 ship；本 plan 先 ship stub 只 print，不 import config）：

```typescript
// Phase 1 stub: validates bootstrap artefacts compile + run.
// Plan 05 replaces the body with `loadConfig()` fail-fast (FND-02).
// Phase 2 replaces this with an Elysia app.listen().

console.log('[rigging] P1 foundation skeleton ready.')
console.log('[rigging] Next: shared kernel (Plan 02), DDD guards (Plan 03), ADRs + AGENTS.md (Plan 04), env + docker (Plan 05).')
```

**5. `README.md` 最小首屏**（Phase 5 會 polish，但 P1 需有首屏提及 Core Value per ROADMAP §Phase 5 DOC-01 前身）：

```markdown
# Rigging

**Harness Engineering for TypeScript backends** — an opinionated reference app where AI Agents write code on rails (type system + runtime guards + DI) so wrong patterns literally fail to wire.

> Core Value: any Domain operation must pass through `AuthContext`. Without `AuthContext`, the handler cannot even be wired.

## Status

Phase 1 (Foundation) is underway. See `.planning/ROADMAP.md` for the five-phase plan.

## Quickstart (Phase 1 minimum)

```bash
bun install
bun run lint
bun run typecheck
bun run test
bun run dev
```

Full docker-compose + env setup lands in Plan 05 of this phase.

## Where things live

See `AGENTS.md` for the Rigidity Map (three-tier strictness) and anti-features list.
See `docs/decisions/` for Architecture Decision Records (MADR 4.0).
See `.planning/` for the GSD planning artefacts.
```

建立完成後跑 `bun run lint && bun run typecheck && bun run test`，三項皆應 exit 0（`bun run test` 在無 test 檔時 exit 0 且 output `0 tests`）。
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/main.ts && grep -q 'P1 foundation skeleton ready' src/main.ts` 找到
    - `test -f src/bootstrap/.gitkeep`
    - `test -f src/shared/application/.gitkeep && test -f src/shared/infrastructure/.gitkeep && test -f src/shared/presentation/.gitkeep`（注意：`src/shared/kernel/` 由 01-02-PLAN 以實體 TS 檔建立，本 plan 不寫 `.gitkeep`）
    - `test -f src/_template/domain/.gitkeep && test -f src/_template/application/.gitkeep && test -f src/_template/infrastructure/.gitkeep && test -f src/_template/presentation/.gitkeep`（Biome overrides glob 所需；ARCH-01）
    - `test -f src/types/.gitkeep`
    - `test -f tests/unit/.gitkeep && test -f tests/integration/.gitkeep && test -f tests/e2e/.gitkeep && test -f tests/contract/.gitkeep && test -f tests/biome-contract/.gitkeep`
    - `test -f drizzle/.gitkeep`
    - `test -f README.md && grep -q 'Harness Engineering' README.md && grep -q 'AuthContext' README.md`（Core Value 提及）
    - `bun run lint` exit 0
    - `bun run typecheck` exit 0
    - `bun run test` exit 0（無 test 檔但 bun:test 不視為 failure）
    - `bun run dev` 可啟動並印出 "P1 foundation skeleton ready"（timeout 5s 內）：`timeout 5 bun run start 2>&1 | grep -q 'P1 foundation skeleton ready'`
  </acceptance_criteria>
  <done>
    DDD 四層目錄 placeholder + `src/_template/` scaffold + main.ts stub 就位；三個品質 gate（lint/typecheck/test）全通過；README.md 首屏提及 Core Value。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer local → git remote | commit 的檔案會被公開（若 repo 為 public）；`.env` 絕不可進 git |
| npm registry → local | 安裝的第三方套件可能含惡意碼（typosquat / supply-chain） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `.gitignore` | mitigate | `.gitignore` 明列 `.env*` + `!.env.example` 白名單；Task 1 acceptance_criteria 驗證字串；CI 於 05-PLAN 可額外 grep `git ls-files` 擋 `.env` 檔 |
| T-01-01-02 | Tampering | `package.json` 依賴 | mitigate | 精確版本號於 Task 1（`^2.4.12` / `^0.34.49` / `^5.9`）；CI（05-PLAN）跑 `bun install --frozen-lockfile`；`bun.lockb` commit；PR review 針對 package.json diff 必查 |
| T-01-01-03 | Tampering | `bun.lockb` | mitigate | `bun.lockb` commit；`.gitignore` 不含 lockfile；README 提醒 frozen-lockfile 於 CI |
| T-01-01-04 | Elevation of Privilege | IoC 套件繞過 ARCH-04 | mitigate | Task 1 acceptance `! grep -qE '"(tsyringe\|inversify\|reflect-metadata)"' package.json`；03-PLAN 的 Biome rule 會封鎖 import（application 層）；ADR 0003 / AGENTS.md Tier 1 聲明 |
| T-01-01-05 | Tampering | `bun.lockb` 於後續 pull / CI 時被替換為 corrupted 版本（supply-chain tamper）| mitigate | CI 固定使用 `bun install --frozen-lockfile`（05-PLAN ci.yml `frozen-lockfile` step 強制）；PR 對 `bun.lockb` 的 diff 須 reviewer 人工檢視；Dependabot / Renovate 於 v2 補 |
| T-01-01-06 | Elevation of Privilege | `package.json` 新增的 transitive dep 含惡意 `postinstall` lifecycle script（supply-chain）| mitigate | Bun 預設不執行 npm lifecycle scripts（`trustedDependencies` allowlist 為空時）；PR review 對 `package.json` / `bun.lockb` diff 必檢；日後若需開啟 lifecycle，須在 `trustedDependencies` 明列並走 ADR；`BUN_INSTALL_CACHE` / container isolation 屬 v2 強化 |
</threat_model>

<verification>
**整個 plan 完工條件：**

```bash
# 1. Lint base config 生效
bun run lint  # expect exit 0

# 2. TypeScript strict 設定生效
bun run typecheck  # expect exit 0

# 3. Test 架構可跑（即使無 test）
bun run test  # expect exit 0

# 4. Dev runtime 可啟動
timeout 5 bun run start 2>&1 | grep -q 'P1 foundation skeleton ready'

# 5. ARCH-04: 無 IoC 套件
! grep -qE '"(tsyringe|inversify|reflect-metadata)"' package.json

# 6. FND-06: single lint config
test -f biome.json && ! find . -maxdepth 2 \( -name '.eslintrc*' -o -name '.prettierrc*' \) 2>/dev/null | grep -q .

# 7. ARCH-01: DDD 四層目錄 placeholder 就位
test -d src/shared/application && test -d src/shared/infrastructure && test -d src/shared/presentation && test -d src/_template/domain && test -d src/_template/application && test -d src/_template/infrastructure && test -d src/_template/presentation  # 注意：src/shared/kernel 由 01-02-PLAN 建立（parallel Wave 1）；此 gate 不含該目錄以避開 Wave 內競態
```
</verification>

<success_criteria>
1. **FND-06**（Biome single config）— `biome.json` 於 repo root；無 ESLint / Prettier 檔；`bun run lint` 通過
2. **ARCH-01**（DDD 四層目錄）— `src/shared/{application,infrastructure,presentation}` + `src/_template/{domain,application,infrastructure,presentation}` 皆就位（後者作為 Biome overrides glob match 床）；`src/shared/kernel/` 由 01-02-PLAN 同 Wave 1 建立並填入實體 TS 檔（解耦同目錄寫入競態）
3. **ARCH-04**（無 IoC 套件）— package.json grep 無 `tsyringe` / `inversify` / `reflect-metadata`
4. `bun install && bun run lint && bun run typecheck && bun run test` 於 clean checkout 全部 exit 0
5. `src/main.ts` stub 可被 `bun run dev` 啟動並 print Phase 1 ready message
6. README.md 首屏提及 Core Value（harness engineering + AuthContext）
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` summarising:
- Bootstrap files created (package.json, tsconfig, biome.json base, bunfig.toml, .gitignore, .dockerignore)
- Directory scaffold (shared/ + _template/ + tests/ + drizzle/)
- src/main.ts stub behaviour
- Passing quality gates (lint / typecheck / test)
- Open handoffs to Plan 02 (kernel files go in src/shared/kernel/), Plan 03 (Biome overrides + biome-contract tests), Plan 04 (docs/decisions/ + AGENTS.md Rigidity Map), Plan 05 (config.ts + docker-compose + .env.example + drizzle.config.ts + CI)
</output>
