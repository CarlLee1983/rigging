---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/kernel/result.ts
  - src/shared/kernel/brand.ts
  - src/shared/kernel/id.ts
  - src/shared/kernel/errors.ts
  - src/shared/kernel/index.ts
  - tests/unit/shared/kernel/result.test.ts
  - tests/unit/shared/kernel/brand.test.ts
  - tests/unit/shared/kernel/id.test.ts
  - tests/unit/shared/kernel/errors.test.ts
  - tests/contract/kernel-framework-free.test.ts
autonomous: true
requirements: [FND-05, ARCH-05]

must_haves:
  truths:
    - "任意層（domain / application / infrastructure / presentation）可 import `src/shared/kernel/`，kernel 不 import 任何 framework 套件"
    - "`Result<T, E>` 支援 `ok` / `err` factory 與 `.isOk` / `.isErr` / `.map` / `.mapErr` / `.andThen` / `.match` 操作"
    - "`Brand<T, K>` 於 TypeScript 型別層區分同底型別（`UserId` ≠ `OrderId`），runtime zero-cost"
    - "`newUUID<K>()` 回傳 `crypto.randomUUID()` 的 UUID v4 值並 brand 為指定型別"
    - "`DomainError` 基底類別含 `code: string` + `httpStatus: number` + `cause?: unknown`；5 子類（ValidationError=400 / UnauthorizedError=401 / ForbiddenError=403 / NotFoundError=404 / ConflictError=409）各自宣告正確 httpStatus"
    - "Wave 0 契約測試覆蓋：Result 行為、Brand 型別隔離、UUID 格式、DomainError httpStatus、kernel framework-free"
  artifacts:
    - path: "src/shared/kernel/result.ts"
      provides: "Result<T, E> ADT（ok/err + map/andThen/match）"
      contains: "export type Result"
      min_lines: 40
    - path: "src/shared/kernel/brand.ts"
      provides: "Brand<T, K> phantom property type + brand<K>() helper"
      contains: "Brand<T, K"
    - path: "src/shared/kernel/id.ts"
      provides: "UUID<K> + newUUID<K>() via crypto.randomUUID"
      contains: "crypto.randomUUID"
    - path: "src/shared/kernel/errors.ts"
      provides: "DomainError abstract + ValidationError/UnauthorizedError/ForbiddenError/NotFoundError/ConflictError 5 子類"
      contains: "abstract class DomainError"
    - path: "src/shared/kernel/index.ts"
      provides: "barrel re-export"
      contains: "export * from './result'"
    - path: "tests/contract/kernel-framework-free.test.ts"
      provides: "grep-based contract test, framework import 違規即 fail"
  key_links:
    - from: "src/shared/kernel/id.ts"
      to: "src/shared/kernel/brand.ts"
      via: "import"
      pattern: "from './brand'"
    - from: "src/shared/kernel/index.ts"
      to: "result.ts / brand.ts / id.ts / errors.ts"
      via: "export * from"
      pattern: "export \\* from"
---

<objective>
Phase 1 Plan 02 — **Shared Kernel**：交付 framework-free 的四件套（`Result` / `Brand` / `UUID` / `DomainError`）與其 unit tests，再加一個 contract test 確保 kernel 永不 import framework 套件。這是 FND-05 + ARCH-05 的直接落地，也是 Phase 2 錯誤處理器與 Phase 3 Domain service 的基礎。

Purpose: 讓 domain 層可以用 `Result<T, DomainError>` 做 control flow、用 `Brand<T, K>` 防 ID 混用、用 `crypto.randomUUID` 生成有型別標記的 ID、用 `DomainError` 子類在全域 error handler（Phase 2）被直接映射到 HTTP status——全部零 framework 依賴。
Output: 4 檔 kernel source + 1 barrel + 4 檔 unit tests + 1 contract test。所有檔 < 100 LOC；總計 test 案例 ≥ 20。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/research/ARCHITECTURE.md

<rationale>
- `RESEARCH.md §Shared Kernel 實作 Sketch` 提供四件套的精確 signature 與 < 100 LOC 實作 sketch；executor 直接移植。
- `CONTEXT.md §D-05..D-08` 鎖定 shared kernel API shape（factory 命名、phantom property、DomainError 欄位）。
- `VALIDATION.md §Wave 0 Requirements` 明列本 plan 必須 ship 的 5 個 test 檔路徑。
- `VALIDATION.md §Per-Task Verification Map` 對每 REQ-ID 提供 automated command；本 plan 直接對應 FND-05 五列 + ARCH-05 一列。
- **本 plan 不依賴 01-PLAN**（Wave 1 並行）——executor 會在 Task 1 前先確認 `src/shared/kernel/` 目錄存在；若不存在（01-PLAN 未完）則先 `mkdir -p`。雖依 files_modified 衝突規則 Wave 1 不碰彼此檔案，但 `src/shared/kernel/.gitkeep` 由 01-PLAN ship（刪除無妨）；本 plan 的 result.ts 等為新增檔，不衝突。
- Result factory 命名：RESEARCH.md 指出 `Ok(...)` 作為 factory 會與 class name 衝突，**本 plan 採 lowercase `ok` / `err` 作為 factory 函式名稱**（與 neverthrow 相容），classes `Ok<T,E>` / `Err<T,E>` 保留 internal 不 export。
- kernel framework-free 的驗證策略：contract test 用 `fs.readFileSync` 讀 `src/shared/kernel/*.ts` 然後 regex 檢查 `from 'elysia|drizzle-orm|better-auth|postgres|pino|@bogeychan'`——純 `bun:test`，無額外 dep。
</rationale>

<interfaces>
<!-- Key contracts this plan creates. Phase 2 error handler, Phase 3 domain services will import from '@/shared/kernel'. -->

```typescript
// src/shared/kernel/result.ts — public API
export type Result<T, E> = Ok<T, E> | Err<T, E>
export const ok: <T, E = never>(value: T) => Result<T, E>
export const err: <T = never, E = unknown>(error: E) => Result<T, E>
export const isOk: <T, E>(r: Result<T, E>) => r is Ok<T, E>
export const isErr: <T, E>(r: Result<T, E>) => r is Err<T, E>
// Result instance methods: .isOk() / .isErr() / .map / .mapErr / .andThen / .match

// src/shared/kernel/brand.ts — public API
export type Brand<T, K extends string> = T & { readonly [__brand]: K }
export const brand: <K extends string>() => <T>(value: T) => Brand<T, K>

// src/shared/kernel/id.ts — public API
export type UUID<K extends string> = Brand<string, K>
export const newUUID: <K extends string>() => UUID<K>

// src/shared/kernel/errors.ts — public API
export abstract class DomainError extends Error {
  abstract readonly code: string
  abstract readonly httpStatus: number
  readonly cause?: unknown
  constructor(message: string, cause?: unknown)
}
export class ValidationError extends DomainError  // httpStatus = 400
export class UnauthorizedError extends DomainError // httpStatus = 401
export class ForbiddenError extends DomainError    // httpStatus = 403
export class NotFoundError extends DomainError     // httpStatus = 404
export class ConflictError extends DomainError     // httpStatus = 409
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 實作 Result + Brand + Id + DomainError 四件套與 barrel</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "Shared Kernel 實作 Sketch" — 完整 4 個檔的 sketch code)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (D-05, D-06, D-07, D-08 API shape)
    - /Users/carl/Dev/CMG/Rigging/tsconfig.json (確認 strict + exactOptionalPropertyTypes 生效；影響 DomainError cause 型別)
    - /Users/carl/Dev/CMG/Rigging/src/shared/kernel/ (若已由 01-PLAN ship .gitkeep 則保留；若不存在則 mkdir -p)
  </read_first>
  <files>
    - src/shared/kernel/result.ts
    - src/shared/kernel/brand.ts
    - src/shared/kernel/id.ts
    - src/shared/kernel/errors.ts
    - src/shared/kernel/index.ts
  </files>
  <behavior>
    - Result.ok(1).map(x => x + 1) 回傳 ok(2)；Result.err('e').map(x => x + 1) 仍回傳 err('e')
    - Result.ok(1).andThen(x => ok(x * 2)) 回傳 ok(2)；Result.err('e').andThen(_ => ok(1)) 仍回 err('e')
    - Result.match({ ok, err }) 對 Ok 呼叫 ok handler、對 Err 呼叫 err handler
    - isOk / isErr 對對應 tag 回 true / false
    - Brand<string, 'UserId'> 與 Brand<string, 'OrderId'> 於 tsc 型別層互不可賦值
    - brand<'UserId'>()('abc') 值為 'abc'（runtime zero-cost）
    - newUUID<'X'>() 每次回新的 UUID v4（36 字元、格式符合 RFC 4122）
    - new ValidationError('m').httpStatus === 400；new UnauthorizedError 401；new ForbiddenError 403；new NotFoundError 404；new ConflictError 409
    - new ValidationError('m') instanceof DomainError === true 且 instanceof Error === true
    - ValidationError.name === 'ValidationError'（錯誤 log 需要）
    - new ValidationError('m', new TypeError('x')).cause instanceof TypeError === true
  </behavior>
  <action>
依 RESEARCH.md §Shared Kernel 實作 Sketch 精確 signature 實作 4 個 TypeScript 檔，**每檔 zero framework import**（不 import `elysia` / `drizzle-orm` / `better-auth` / `postgres` / `pino` / `@bogeychan/*` / TypeBox 的 `t`）。

### 1. `src/shared/kernel/result.ts`（D-05, ~60 LOC）

精確抄寫 RESEARCH.md §`src/shared/kernel/result.ts` 的 code，但 **export 採 lowercase `ok` / `err` factory**（classes `Ok<T,E>` / `Err<T,E>` 保持 internal；外部 type guard 用 `isOk` / `isErr`）：

```typescript
// Result<T, E> — neverthrow-style, self-implemented, no dep.
// See docs/decisions/0003-ddd-layering.md — kernel stays framework-free.

export type Result<T, E> = Ok<T, E> | Err<T, E>

class Ok<T, E> {
  readonly _tag = 'Ok' as const
  constructor(readonly value: T) {}
  isOk(): this is Ok<T, E> { return true }
  isErr(): this is Err<T, E> { return false }
  map<U>(fn: (v: T) => U): Result<U, E> { return new Ok(fn(this.value)) }
  mapErr<F>(_fn: (e: E) => F): Result<T, F> { return this as unknown as Result<T, F> }
  andThen<U>(fn: (v: T) => Result<U, E>): Result<U, E> { return fn(this.value) }
  match<R>(handlers: { ok: (v: T) => R; err: (e: E) => R }): R { return handlers.ok(this.value) }
}

class Err<T, E> {
  readonly _tag = 'Err' as const
  constructor(readonly error: E) {}
  isOk(): this is Ok<T, E> { return false }
  isErr(): this is Err<T, E> { return true }
  map<U>(_fn: (v: T) => U): Result<U, E> { return this as unknown as Result<U, E> }
  mapErr<F>(fn: (e: E) => F): Result<T, F> { return new Err(fn(this.error)) }
  andThen<U>(_fn: (v: T) => Result<U, E>): Result<U, E> { return this as unknown as Result<U, E> }
  match<R>(handlers: { ok: (v: T) => R; err: (e: E) => R }): R { return handlers.err(this.error) }
}

export type { Ok, Err }

export const ok = <T, E = never>(value: T): Result<T, E> => new Ok(value)
export const err = <T = never, E = unknown>(error: E): Result<T, E> => new Err(error)

export const isOk = <T, E>(r: Result<T, E>): r is Ok<T, E> => r._tag === 'Ok'
export const isErr = <T, E>(r: Result<T, E>): r is Err<T, E> => r._tag === 'Err'
```

### 2. `src/shared/kernel/brand.ts`（D-06, ~10 LOC）

精確抄寫 RESEARCH.md §`src/shared/kernel/brand.ts`：

```typescript
// Brand<T, K> — phantom property pattern, compile-time only, runtime zero-cost.
// See docs/decisions/0003-ddd-layering.md — kernel stays framework-free.

declare const __brand: unique symbol
export type Brand<T, K extends string> = T & { readonly [__brand]: K }

/**
 * Internal `as` cast helper. Callers should define typed constructors per feature:
 *
 *   export type UserId = Brand<string, 'UserId'>
 *   export const UserId = (v: string): UserId => brand<'UserId'>()(v)
 *
 * If runtime validation is needed (e.g. UUID format check) the feature's
 * constructor performs the check before calling brand().
 */
export const brand = <K extends string>() => <T>(value: T): Brand<T, K> =>
  value as Brand<T, K>
```

### 3. `src/shared/kernel/id.ts`（D-07, ~15 LOC）

精確抄寫 RESEARCH.md §`src/shared/kernel/id.ts`：

```typescript
import { brand, type Brand } from './brand'

// Generic UUID Brand — features compose their own on top
export type UUID<K extends string> = Brand<string, K>

// Generate a new UUID v4 via WebCrypto (native in Bun / Node 20+)
export const newUUID = <K extends string>(): UUID<K> => brand<K>()(crypto.randomUUID())

// Example usage by features (not exported from kernel; for docs only):
//
//   // in src/auth/domain/values/user-id.ts
//   export type UserId = UUID<'UserId'>
//   export const UserId = (v: string): UserId => brand<'UserId'>()(v)
//   export const newUserId = (): UserId => newUUID<'UserId'>()
```

### 4. `src/shared/kernel/errors.ts`（D-08, ~45 LOC, ARCH-05 起源）

精確抄寫 RESEARCH.md §`src/shared/kernel/errors.ts`，**注意：tsconfig `exactOptionalPropertyTypes: true` 影響 `readonly cause?: unknown` 型別**；executor 須以以下寫法避免 exactOptional 陷阱：

```typescript
// DomainError hierarchy — framework-free, HTTP mapping via `httpStatus` field.
// Error handler plugin (Phase 2) reads err.httpStatus directly — no mapping table.
// See docs/decisions/0003-ddd-layering.md (framework-free) + docs/decisions/0006-authcontext-boundary.md.

export abstract class DomainError extends Error {
  abstract readonly code: string
  abstract readonly httpStatus: number

  constructor(message: string, public readonly cause?: unknown) {
    super(message)
    this.name = this.constructor.name
    if (typeof Error.captureStackTrace === 'function') {
      Error.captureStackTrace(this, this.constructor)
    }
  }
}

export class ValidationError extends DomainError {
  readonly code = 'VALIDATION_ERROR'
  readonly httpStatus = 400
}

export class UnauthorizedError extends DomainError {
  readonly code = 'UNAUTHORIZED'
  readonly httpStatus = 401
}

export class ForbiddenError extends DomainError {
  readonly code = 'FORBIDDEN'
  readonly httpStatus = 403
}

export class NotFoundError extends DomainError {
  readonly code = 'NOT_FOUND'
  readonly httpStatus = 404
}

export class ConflictError extends DomainError {
  readonly code = 'CONFLICT'
  readonly httpStatus = 409
}
```

### 5. `src/shared/kernel/index.ts`（barrel）

```typescript
export * from './result'
export * from './brand'
export * from './id'
export * from './errors'
```

完成後跑 `bun run lint && bun run typecheck`；兩者皆 exit 0。
  </action>
  <verify>
    <automated>bun run lint src/shared/kernel/ && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/shared/kernel/result.ts && test -f src/shared/kernel/brand.ts && test -f src/shared/kernel/id.ts && test -f src/shared/kernel/errors.ts && test -f src/shared/kernel/index.ts`
    - `grep -q 'export type Result<T, E>' src/shared/kernel/result.ts`
    - `grep -q 'export const ok' src/shared/kernel/result.ts && grep -q 'export const err' src/shared/kernel/result.ts`
    - `grep -q 'export const isOk' src/shared/kernel/result.ts && grep -q 'export const isErr' src/shared/kernel/result.ts`
    - `grep -q 'export type Brand<T, K' src/shared/kernel/brand.ts`
    - `grep -q 'export const brand' src/shared/kernel/brand.ts`
    - `grep -q "import.*from './brand'" src/shared/kernel/id.ts`
    - `grep -q 'crypto.randomUUID' src/shared/kernel/id.ts`
    - `grep -q 'export const newUUID' src/shared/kernel/id.ts`
    - `grep -q 'abstract class DomainError' src/shared/kernel/errors.ts`
    - `grep -q 'httpStatus = 400' src/shared/kernel/errors.ts` (ValidationError)
    - `grep -q 'httpStatus = 401' src/shared/kernel/errors.ts`
    - `grep -q 'httpStatus = 403' src/shared/kernel/errors.ts`
    - `grep -q 'httpStatus = 404' src/shared/kernel/errors.ts`
    - `grep -q 'httpStatus = 409' src/shared/kernel/errors.ts`
    - `grep -cE "class (Validation|Unauthorized|Forbidden|NotFound|Conflict)Error" src/shared/kernel/errors.ts` 回 5（5 子類全備）
    - `grep -q "export \* from './result'" src/shared/kernel/index.ts && grep -q "export \* from './errors'" src/shared/kernel/index.ts`
    - `! grep -rE "from ['\"](elysia\|drizzle-orm\|better-auth\|postgres\|pino\|@bogeychan)" src/shared/kernel/`（framework-free；FND-05 核心）
    - `bun run lint src/shared/kernel/` exit 0
    - `bun run typecheck` exit 0
  </acceptance_criteria>
  <done>
    kernel 四件套 + barrel 就位；zero framework import；lint + typecheck 通過。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 撰寫 4 檔 unit tests（Result / Brand / Id / Errors）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/src/shared/kernel/result.ts (Task 1 產出；對準 API)
    - /Users/carl/Dev/CMG/Rigging/src/shared/kernel/brand.ts
    - /Users/carl/Dev/CMG/Rigging/src/shared/kernel/id.ts
    - /Users/carl/Dev/CMG/Rigging/src/shared/kernel/errors.ts
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (sections: "Shared Kernel Sketch §Tests" per 檔 — 每檔 test 案例清單)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map — FND-05 rows)
  </read_first>
  <files>
    - tests/unit/shared/kernel/result.test.ts
    - tests/unit/shared/kernel/brand.test.ts
    - tests/unit/shared/kernel/id.test.ts
    - tests/unit/shared/kernel/errors.test.ts
  </files>
  <behavior>
    - result.test.ts：ok/err factory、map/mapErr/andThen/match/isOk/isErr 六類行為共 ≥ 8 案例
    - brand.test.ts：runtime 值保留 identity；不同 brand 的 runtime 值互相 equal（string '===' 不受 phantom 影響）；compile-time type-level test 於註解描述（tsc 層面由 typecheck 驗證）
    - id.test.ts：newUUID 回 36 字元；regex 匹配 UUID v4 格式 `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i`；1000 次呼叫無 collision
    - errors.test.ts：5 子類 httpStatus 對照（400/401/403/404/409）；code 字串符合 SCREAMING_SNAKE_CASE；.name 等於 constructor 名；instanceof DomainError & instanceof Error；cause preservation
  </behavior>
  <action>
建立以下 4 個 test 檔，**使用 `bun:test` API**（import from 'bun:test'；不使用 vitest / jest）。每檔案至少含 mustHave behavior 所列案例：

### 1. `tests/unit/shared/kernel/result.test.ts`

```typescript
import { describe, expect, test } from 'bun:test'
import { ok, err, isOk, isErr, type Result } from '@/shared/kernel/result'

describe('Result: ok factory', () => {
  test('returns Ok with value', () => {
    const r = ok(42)
    expect(r.isOk()).toBe(true)
    expect(r.isErr()).toBe(false)
    if (r.isOk()) expect(r.value).toBe(42)
  })
})

describe('Result: err factory', () => {
  test('returns Err with error', () => {
    const r = err<never, string>('boom')
    expect(r.isErr()).toBe(true)
    expect(r.isOk()).toBe(false)
    if (r.isErr()) expect(r.error).toBe('boom')
  })
})

describe('Result.map', () => {
  test('Ok: applies fn', () => {
    const r = ok<number, string>(1).map(x => x + 1)
    expect(r.isOk()).toBe(true)
    if (r.isOk()) expect(r.value).toBe(2)
  })
  test('Err: passthrough', () => {
    const r: Result<number, string> = err('e')
    const mapped = r.map(x => x + 1)
    expect(mapped.isErr()).toBe(true)
    if (mapped.isErr()) expect(mapped.error).toBe('e')
  })
})

describe('Result.mapErr', () => {
  test('Ok: passthrough', () => {
    const r: Result<number, string> = ok(1)
    const mapped = r.mapErr(e => `wrapped: ${e}`)
    expect(mapped.isOk()).toBe(true)
  })
  test('Err: applies fn', () => {
    const r: Result<number, string> = err('e')
    const mapped = r.mapErr(e => `wrapped: ${e}`)
    if (mapped.isErr()) expect(mapped.error).toBe('wrapped: e')
  })
})

describe('Result.andThen', () => {
  test('Ok -> Ok chain', () => {
    const r = ok<number, string>(1).andThen(x => ok<number, string>(x * 2))
    if (r.isOk()) expect(r.value).toBe(2)
  })
  test('Ok -> Err chain', () => {
    const r = ok<number, string>(1).andThen(_ => err<number, string>('bad'))
    if (r.isErr()) expect(r.error).toBe('bad')
  })
  test('Err short-circuits', () => {
    const r: Result<number, string> = err('e')
    const chained = r.andThen(x => ok<number, string>(x * 2))
    if (chained.isErr()) expect(chained.error).toBe('e')
  })
})

describe('Result.match', () => {
  test('Ok: calls ok handler', () => {
    const result = ok<number, string>(1).match({ ok: v => `v=${v}`, err: e => `e=${e}` })
    expect(result).toBe('v=1')
  })
  test('Err: calls err handler', () => {
    const result = err<number, string>('boom').match({ ok: v => `v=${v}`, err: e => `e=${e}` })
    expect(result).toBe('e=boom')
  })
})

describe('Result: type guards isOk / isErr', () => {
  test('narrow Ok', () => {
    const r: Result<number, string> = ok(1)
    expect(isOk(r)).toBe(true)
    expect(isErr(r)).toBe(false)
  })
  test('narrow Err', () => {
    const r: Result<number, string> = err('e')
    expect(isErr(r)).toBe(true)
    expect(isOk(r)).toBe(false)
  })
})
```

### 2. `tests/unit/shared/kernel/brand.test.ts`

```typescript
import { describe, expect, test } from 'bun:test'
import { brand, type Brand } from '@/shared/kernel/brand'

// Type-level cross-check — these assertions rely on `tsc --noEmit`.
// If this file type-checks, the Brand isolation works.
type UserId = Brand<string, 'UserId'>
type OrderId = Brand<string, 'OrderId'>

describe('Brand: runtime value identity', () => {
  test('brand() is zero-cost (returns the same primitive)', () => {
    const raw = 'abc-123'
    const userId: UserId = brand<'UserId'>()(raw)
    // Phantom type is compile-time only; at runtime the value is the original string.
    expect(userId).toBe(raw)
    expect(typeof userId).toBe('string')
  })
})

describe('Brand: type isolation (compile-time)', () => {
  test('nominal typing survives at runtime as identity', () => {
    // This test exercises runtime equality; type-level isolation is enforced by tsc.
    const u: UserId = brand<'UserId'>()('x')
    const o: OrderId = brand<'OrderId'>()('x')
    expect(u).toBe(o as unknown as string)  // runtime equal
    // Compile-time: `const bad: UserId = o` would fail tsc (verified by bun run typecheck).
  })
})
```

### 3. `tests/unit/shared/kernel/id.test.ts`

```typescript
import { describe, expect, test } from 'bun:test'
import { newUUID } from '@/shared/kernel/id'

const UUID_V4_REGEX = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i

describe('newUUID', () => {
  test('returns a 36-char UUID v4 string', () => {
    const id = newUUID<'TestId'>()
    expect(typeof id).toBe('string')
    expect(id.length).toBe(36)
    expect(id).toMatch(UUID_V4_REGEX)
  })

  test('no collisions across 1000 calls', () => {
    const seen = new Set<string>()
    for (let i = 0; i < 1000; i++) {
      seen.add(newUUID<'X'>())
    }
    expect(seen.size).toBe(1000)
  })
})
```

### 4. `tests/unit/shared/kernel/errors.test.ts`

```typescript
import { describe, expect, test } from 'bun:test'
import {
  DomainError,
  ValidationError,
  UnauthorizedError,
  ForbiddenError,
  NotFoundError,
  ConflictError,
} from '@/shared/kernel/errors'

describe('DomainError: httpStatus per subclass (ARCH-05)', () => {
  test('ValidationError → 400', () => {
    expect(new ValidationError('bad').httpStatus).toBe(400)
  })
  test('UnauthorizedError → 401', () => {
    expect(new UnauthorizedError('no session').httpStatus).toBe(401)
  })
  test('ForbiddenError → 403', () => {
    expect(new ForbiddenError('no scope').httpStatus).toBe(403)
  })
  test('NotFoundError → 404', () => {
    expect(new NotFoundError('gone').httpStatus).toBe(404)
  })
  test('ConflictError → 409', () => {
    expect(new ConflictError('dup').httpStatus).toBe(409)
  })
})

describe('DomainError: code strings (SCREAMING_SNAKE_CASE)', () => {
  test('ValidationError.code', () => {
    expect(new ValidationError('x').code).toBe('VALIDATION_ERROR')
  })
  test('UnauthorizedError.code', () => {
    expect(new UnauthorizedError('x').code).toBe('UNAUTHORIZED')
  })
  test('ForbiddenError.code', () => {
    expect(new ForbiddenError('x').code).toBe('FORBIDDEN')
  })
  test('NotFoundError.code', () => {
    expect(new NotFoundError('x').code).toBe('NOT_FOUND')
  })
  test('ConflictError.code', () => {
    expect(new ConflictError('x').code).toBe('CONFLICT')
  })
})

describe('DomainError: instanceof + .name', () => {
  test('instanceof chains', () => {
    const e = new ValidationError('x')
    expect(e instanceof ValidationError).toBe(true)
    expect(e instanceof DomainError).toBe(true)
    expect(e instanceof Error).toBe(true)
  })
  test('.name equals constructor name (for log output)', () => {
    expect(new ValidationError('x').name).toBe('ValidationError')
    expect(new UnauthorizedError('x').name).toBe('UnauthorizedError')
  })
})

describe('DomainError: cause preservation', () => {
  test('cause is stored', () => {
    const root = new TypeError('underlying')
    const e = new ValidationError('wrap', root)
    expect(e.cause).toBe(root)
    expect(e.cause instanceof TypeError).toBe(true)
  })
  test('cause undefined when omitted', () => {
    const e = new ValidationError('no cause')
    expect(e.cause).toBeUndefined()
  })
})
```

完成後執行 `bun test tests/unit/shared/kernel/` 驗證全部案例通過。
  </action>
  <verify>
    <automated>bun test tests/unit/shared/kernel/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/shared/kernel/result.test.ts`
    - `test -f tests/unit/shared/kernel/brand.test.ts`
    - `test -f tests/unit/shared/kernel/id.test.ts`
    - `test -f tests/unit/shared/kernel/errors.test.ts`
    - `grep -q "from 'bun:test'" tests/unit/shared/kernel/result.test.ts`（非 vitest / jest）
    - `grep -q "@/shared/kernel/result" tests/unit/shared/kernel/result.test.ts`（使用 tsconfig paths alias）
    - `bun test tests/unit/shared/kernel/` exit 0
    - `bun test tests/unit/shared/kernel/ 2>&1 | grep -E 'pass|PASS'` 至少 20 個 test pass（Result 8 + Brand 2 + Id 2 + Errors 13 ≈ 25）
    - `bun run typecheck` exit 0（Brand 型別於 tsc 通過，證明 type isolation compile-level）
  </acceptance_criteria>
  <done>
    4 個 kernel unit test 檔就位；`bun test tests/unit/shared/kernel/` 全數通過；type-level 檢查透過 `bun run typecheck`。
  </done>
</task>

<task type="auto">
  <name>Task 3: 建立 kernel-framework-free contract test（FND-05 static gate）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/src/shared/kernel/ (確認所有檔案就位；Task 1 產出)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "Framework-free enforcement" — contract test script)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements — tests/contract/kernel-framework-free.test.ts)
  </read_first>
  <files>
    - tests/contract/kernel-framework-free.test.ts
  </files>
  <action>
建立 contract test 以 **grep-equivalent** 方式檢查 `src/shared/kernel/**/*.ts` 任何檔案都不 import 下列 framework 套件：`elysia`、`drizzle-orm`、`better-auth`、`postgres`、`pino`、`@bogeychan`。若未來 executor 誤加 import 立即 CI fail。

使用 `bun:test` + Node/Bun `fs` 讀檔（非 shell spawn；keep test pure TS）：

```typescript
import { describe, expect, test } from 'bun:test'
import { readdirSync, readFileSync, statSync } from 'node:fs'
import { join } from 'node:path'

const KERNEL_DIR = 'src/shared/kernel'
const FORBIDDEN = [
  'elysia',
  'drizzle-orm',
  'better-auth',
  '@better-auth/',
  'postgres',
  'pino',
  '@bogeychan/elysia-logger',
]

function listTsFiles(dir: string): string[] {
  const entries = readdirSync(dir)
  const out: string[] = []
  for (const entry of entries) {
    const full = join(dir, entry)
    const st = statSync(full)
    if (st.isDirectory()) {
      out.push(...listTsFiles(full))
    } else if (full.endsWith('.ts')) {
      out.push(full)
    }
  }
  return out
}

describe('Contract: shared/kernel is framework-free (FND-05)', () => {
  const files = listTsFiles(KERNEL_DIR)

  test('kernel directory has at least 4 source files (result/brand/id/errors + barrel)', () => {
    expect(files.length).toBeGreaterThanOrEqual(5)
  })

  for (const file of files) {
    test(`${file} does not import any framework package`, () => {
      const source = readFileSync(file, 'utf8')
      for (const pkg of FORBIDDEN) {
        // match: from 'pkg' | from "pkg" | from 'pkg/... | require('pkg')
        const patterns = [
          new RegExp(`from\\s+['"]${pkg.replace('/', '\\/')}(['"/])`),
          new RegExp(`require\\(['"]${pkg.replace('/', '\\/')}(['"/])`),
        ]
        for (const re of patterns) {
          expect(source).not.toMatch(re)
        }
      }
    })
  }
})
```

**為何不用 `grep -r` shell？** Contract test 在 CI 的 `bun test` 流程內跑；保留 pure TS 讓 test runner 回報個別檔案級訊息而非整體非 0，debug 更容易。

完成後跑 `bun test tests/contract/kernel-framework-free.test.ts`；**刻意試** 一次 negative：將 `src/shared/kernel/errors.ts` 頂部暫時加 `import 'elysia'`、跑測試應 fail，再移除之並確認恢復 pass。（此 smoke 由 executor 手動驗，不作 acceptance。）
  </action>
  <verify>
    <automated>bun test tests/contract/kernel-framework-free.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/contract/kernel-framework-free.test.ts`
    - `grep -q "from 'bun:test'" tests/contract/kernel-framework-free.test.ts`
    - `grep -q "src/shared/kernel" tests/contract/kernel-framework-free.test.ts`
    - `grep -q "elysia" tests/contract/kernel-framework-free.test.ts && grep -q "drizzle-orm" tests/contract/kernel-framework-free.test.ts && grep -q "better-auth" tests/contract/kernel-framework-free.test.ts && grep -q "postgres" tests/contract/kernel-framework-free.test.ts && grep -q "pino" tests/contract/kernel-framework-free.test.ts`（六個 forbidden package 都在 check list）
    - `bun test tests/contract/kernel-framework-free.test.ts` exit 0
    - `bun test tests/contract/kernel-framework-free.test.ts 2>&1 | grep -E '\(pass\)|PASS'` 至少 6 個 test（1 個 count check + ≥ 5 files × 1 test each）
    - Wave-end full suite 通過：`bun run lint && bun run typecheck && bun test` 全 exit 0
  </acceptance_criteria>
  <done>
    kernel framework-free contract test 就位並通過；未來誤 import framework 立即 CI fail；整體 Wave 1 kernel 側全綠。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared/kernel → 任何層 | kernel 被所有 domain / application / infrastructure / presentation import；若 kernel 吃 framework，framework 會洩漏到 domain 層 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `src/shared/kernel/*.ts` 被誤加 framework import | mitigate | Task 3 建立 contract test；03-PLAN 另加 Biome override 覆蓋 `src/shared/kernel/**` 禁 framework package（per RESEARCH Pitfall #6）|
| T-01-02-02 | Information Disclosure | `DomainError.cause` 被 Phase 2 error handler 序列化洩漏到 HTTP body | accept (P1) | P1 只定義 `cause?: unknown` 欄位；Phase 2 error handler 必須不序列化 cause（由 Phase 2 plan 處理）。本 plan 透過 ValidationError.test.ts 的 `.cause` 測試確認欄位存在但未定義外化契約 |
| T-01-02-03 | Spoofing | `newUUID()` collision / non-UUID-v4 format | accept | 採用 Bun/Node 原生 `crypto.randomUUID()`，WebCrypto 規範保證 UUID v4；Task 2 id.test.ts 用 1000 次 collision check + regex format check 做 regression |
</threat_model>

<verification>
```bash
# 完工 gate
bun run lint && bun run typecheck && bun test tests/unit/shared/kernel/ tests/contract/kernel-framework-free.test.ts

# FND-05 framework-free（靜態 grep fallback）
! grep -rE "from ['\"](elysia|drizzle-orm|better-auth|postgres|pino|@bogeychan)" src/shared/kernel/

# ARCH-05 five subclass httpStatus
grep -c 'readonly httpStatus' src/shared/kernel/errors.ts  # expect >= 5
```
</verification>

<success_criteria>
1. **FND-05**（framework-free kernel）— `src/shared/kernel/{result,brand,id,errors,index}.ts` 就位；contract test + grep 雙重驗證零 framework import
2. **FND-05**（Result 行為正確）— 8+ test 案例通過
3. **FND-05**（Brand 型別隔離）— `bun run typecheck` 通過；runtime 值 identity 確認
4. **FND-05**（UUID v4 生成）— 正確 regex + 1000 次 no-collision
5. **FND-05 + ARCH-05**（DomainError 基底與 5 子類）— httpStatus 400/401/403/404/409 各自正確；code 字串 SCREAMING_SNAKE_CASE；instanceof chain + .name 正確；cause 保留
6. `bun run lint && bun run typecheck && bun test` 於 Wave 1 結束時全綠
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` summarising:
- Four kernel files shipped (paths, LOC, key exports)
- Test counts (unit + contract)
- Framework-free evidence (contract test automated + grep fallback)
- Handoff to Plan 03 (Biome overrides will enforce same rule at lint time via `src/shared/kernel/**` scope if added)
- Handoff to Phase 2 (error-handler plugin reads DomainError.httpStatus directly)
- Handoff to Phase 3 (auth domain will use Result / Brand / newUUID / DomainError subclasses)
</output>
