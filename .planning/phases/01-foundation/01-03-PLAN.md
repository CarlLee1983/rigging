---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-foundation/01", "01-foundation/02"]
files_modified:
  - biome.json
  - tests/biome-contract/domain/drizzle-violation.ts
  - tests/biome-contract/domain/elysia-violation.ts
  - tests/biome-contract/domain/better-auth-violation.ts
  - tests/biome-contract/domain/postgres-violation.ts
  - tests/biome-contract/domain/logger-violation.ts
  - tests/biome-contract/application/drizzle-violation.ts
  - tests/biome-contract/application/postgres-violation.ts
  - tests/biome-contract/application/internal-barrel-violation.ts
  - tests/biome-contract/presentation/internal-barrel-violation.ts
  - tests/biome-contract/biome-rule-contract.test.ts
  - tests/integration/auth-bypass-contract.test.ts
autonomous: true
requirements: [ARCH-02, ARCH-03]

must_haves:
  truths:
    - "Biome `noRestrictedImports` override 封鎖 `src/**/domain/**` import `drizzle-orm` / `postgres` / `elysia` / `better-auth` / `@bogeychan/elysia-logger` / `pino`，違規 CI fail"
    - "Biome 封鎖 `src/**/application/**` import `drizzle-orm` / `postgres`（application 只接觸 ports，不直接碰 ORM / 驅動）"
    - "Biome `patterns[].group` 封鎖 `src/**/application/**` 與 `src/**/presentation/**` import `**/domain/internal/**`（only barrel legal）"
    - "每條 rule 的 `message` 嚴守四段格式（what + why + ADR link + 具體修法，per D-12）"
    - "biome-rule-contract.test.ts 於 `bun:test` 中對每個違規檔執行 `biome check` 並 expect exit non-zero + 錯誤訊息含 'Move Drizzle usage to' / 'getXxxService(ctx)' 等具體修法關鍵詞"
    - "ARCH-03（Repository 回 domain entity）於 P1 以 policy 靜態宣告（Biome rule 封鎖 infrastructure leakage path + ADR 0003 設計文）；Phase 3 才有實體 Repository 可驗"
    - "`tests/integration/auth-bypass-contract.test.ts` (skipped stub) 預留 AUX-06 的 Phase 3 實作槽"
  artifacts:
    - path: "biome.json"
      provides: "含 overrides 的完整 Biome 配置；D-09/D-11/D-12 rules 全就位"
      contains: "overrides"
    - path: "tests/biome-contract/domain/drizzle-violation.ts"
      provides: "故意違規檔，用於 rule contract test"
    - path: "tests/biome-contract/biome-rule-contract.test.ts"
      provides: "執行 biome check 於違規檔、斷言 fail + 關鍵 token 於 message"
    - path: "tests/integration/auth-bypass-contract.test.ts"
      provides: "AUX-06 Phase 3 stub"
      contains: "test.skip"
  key_links:
    - from: "biome.json overrides[0]"
      to: "src/**/domain/**"
      via: "includes"
      pattern: "src/\\*\\*/domain"
    - from: "biome.json overrides"
      to: "tests/biome-contract/domain/**"
      via: "includes（在 overrides array 中同時列入，使 contract test 檔案走相同 rule set）"
      pattern: "tests/biome-contract/domain"
    - from: "tests/biome-contract/biome-rule-contract.test.ts"
      to: "biome.json"
      via: "spawnSync biome check 讀 biome.json"
      pattern: "spawnSync|Bun.spawn"
---

<objective>
Phase 1 Plan 03 — **DDD Scaffold + Biome Overrides**：把 CONTEXT.md §D-09 / D-11 / D-12 的 lint rules 寫進 `biome.json`（含四段 message 格式），並在 `tests/biome-contract/` ship 一組故意違規檔 + 執行 `biome check` 於每檔 expect fail 的 contract test 套件。這是 ARCH-02（domain 禁 framework）與 ARCH-03（Repository 回 domain entity、不能洩漏 Drizzle row）靜態側的落地——domain、application、presentation 三層的違規 import 會在 CI 被物理擋住。

Purpose: 「錯的寫法根本跑不起來」的物理實作第一道關卡就是 Biome rule。沒有 rule，domain 能 import drizzle-orm 仍 CI 過，Core Value 失守到 Phase 3 才被發現。本 plan 同時做 rule 與 rule 的回歸測試——避免 RESEARCH.md Pitfall #5（glob 寫錯無違規源碼所以全過）。
Output: 完整 biome.json（Plan 01 base + overrides 段）、9 個故意違規檔、1 個 contract test 檔、1 個 AUX-06 stub。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<rationale>
- `RESEARCH.md §DDD Scaffold & Biome Rule 實作細節 §Biome 2.x noRestrictedImports 語法` 提供完整 biome.json 草稿（VERIFIED 2026-04-19：overrides 用 `includes`（複數）、`patterns[].group` 接受 gitignore-style glob）。
- `CONTEXT.md §D-12` 指定四段 message 格式：what + why + ADR link + 具體修法。本 plan 每條 rule 的 message 都嚴守。
- `RESEARCH.md §CI + Runtime Guard §B3 (推薦)` 提供 contract test approach：故意違規檔 + spawn biome check + assert non-zero + 關鍵 token 於 stdout。
- `RESEARCH.md §Open Questions §3` 建議 contract test 檔案位置：**獨立 `tests/biome-contract/` 並在 biome.json overrides 裡加 `tests/biome-contract/<layer>/**` 到對應 rule set**——本 plan 採用此策略，避免 production `src/` 混入故意違規。
- `RESEARCH.md §Pitfall #1` 警告 Biome 2.x 用 `includes`（複數）非 `include` 單數或 ESLint 的 `files`；executor 照抄 RESEARCH.md 的 JSON 草稿。
- `RESEARCH.md §Pitfall #6` 建議同時擴 override 到 `src/shared/kernel/**` — 本 plan 採納，加第四條 override 覆蓋 kernel 層（與 02-PLAN 的 contract test 互為 belt-and-suspenders）。
- `tests/biome-contract/**` 已於 01-PLAN 從 tsconfig.json exclude（不讓 tsc 編譯違規檔）；biome.json `files.includes` 於 01-PLAN 包含 `tests/**`，本 plan 於 overrides 加對應 layer 的 contract path 即生效。
- **ARCH-03 於 P1 是「policy 靜態聲明」**：P1 無 Repository 實作；Biome rule 阻 application/domain import drizzle-orm，加上 ADR 0003 文字要求 Repository port 回 domain entity + Mapper pattern——物理執行於 Phase 3。P1 此 plan 的 ARCH-03 覆蓋範圍僅是「static policy declaration」。
- **AUX-06 stub** 依 RESEARCH.md §CI + Runtime Guard §C 建議 ship skipped test，讓 ID 有占位——本 plan 納入避免 Phase 3 executor 漏找檔。
</rationale>

<interfaces>
<!-- Biome rule structure (verified 2026-04-19, RESEARCH.md §Biome 2.x noRestrictedImports 語法) -->

```
biome.json (post-03-PLAN):
{
  "$schema": "...",
  "files": { "includes": ["src/**", "tests/**"] },
  "formatter": { ... base from 01-PLAN ... },
  "linter": { "rules": { "recommended": true } },
  "javascript": { ... base from 01-PLAN ... },
  "overrides": [
    // 1. domain 禁 framework package（含 contract test 的 domain layer）
    { "includes": ["src/**/domain/**", "tests/biome-contract/domain/**"],
      "linter.rules.style.noRestrictedImports.options.paths": { "drizzle-orm": "...", "postgres": "...", "elysia": "...", "better-auth": "...", "@bogeychan/elysia-logger": "...", "pino": "..." } },
    // 2. application 禁 drizzle-orm / postgres
    { "includes": ["src/**/application/**", "tests/biome-contract/application/**"],
      "linter.rules.style.noRestrictedImports.options.paths": { "drizzle-orm": "...", "postgres": "..." } },
    // 3. application + presentation 禁 domain/internal/**
    { "includes": ["src/**/application/**", "src/**/presentation/**", "tests/biome-contract/application/**", "tests/biome-contract/presentation/**"],
      "linter.rules.style.noRestrictedImports.options.patterns": [{ "group": ["**/domain/internal/**", "**/domain/internal"], "message": "..." }] },
    // 4. shared/kernel 禁 framework package（Pitfall #6 補強）
    { "includes": ["src/shared/kernel/**"],
      "linter.rules.style.noRestrictedImports.options.paths": { "drizzle-orm": "...", "postgres": "...", "elysia": "...", "better-auth": "...", "@bogeychan/elysia-logger": "...", "pino": "..." } }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 擴充 biome.json 加上 4 條 overrides（domain / application / barrel guard / kernel）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/biome.json (01-PLAN 產出的 base config；保留 $schema / vcs / files / formatter / linter / javascript 段)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "DDD Scaffold & Biome Rule 實作細節 §Biome 2.x noRestrictedImports 語法" — 完整 JSON 草稿)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (D-09 / D-11 / D-12 — message 四段格式要求)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "Pitfall 1" — `includes` 複數 vs `include` 單數；Pitfall 6 — 擴至 shared/kernel)
  </read_first>
  <files>
    - biome.json
  </files>
  <action>
擴充 `biome.json`，**在 base config 後加上 `overrides` array**（4 個 object）。每條 rule 的 `message` 必須嚴守**四段格式（per D-12）：what（禁什麼）+ why（為何禁 + ADR link）+ ADR link + Fix（具體修法）**。

完整 `biome.json` 新內容如下（executor 整檔覆蓋；`$schema` / `vcs` / `files` / `formatter` / `linter` / `javascript` 段與 01-PLAN 一致，overrides 為本 plan 新增）：

```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.12/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": ["src/**", "tests/**"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "semicolons": "asNeeded"
    }
  },
  "overrides": [
    {
      "includes": ["src/**/domain/**", "tests/biome-contract/domain/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "drizzle-orm": "Domain layer cannot import 'drizzle-orm'. Reason: Domain must stay framework-free (see docs/decisions/0003-ddd-layering.md, docs/decisions/0009-rigidity-map.md Tier 1). Fix: Move Drizzle usage to src/{feature}/infrastructure/, use a Mapper (src/{feature}/infrastructure/mappers/) to convert rows to domain entities.",
                  "postgres": "Domain layer cannot import 'postgres'. Reason: Domain must stay framework-free (ADR 0003, ADR 0009 Tier 1). Fix: Use the 'postgres' client only in src/shared/infrastructure/db/client.ts or {feature}/infrastructure/.",
                  "elysia": "Domain layer cannot import 'elysia'. Reason: Domain entities and factories are framework-free (ADR 0003, ADR 0006). Fix: HTTP concerns belong in src/{feature}/presentation/; derive AuthContext via the requireAuth macro (ADR 0007) and pass it into domain factories as a value object.",
                  "better-auth": "Domain layer cannot import 'better-auth'. Reason: BetterAuth is an infrastructure concern behind an IIdentityService port (ADR 0004, ADR 0006). Fix: Use ports in src/auth/application/ports/ and implement in src/auth/infrastructure/.",
                  "@bogeychan/elysia-logger": "Domain layer cannot import logger adapters directly. Reason: Logger is a port (ILogger) injected via the shared kernel (ADR 0003). Fix: Depend on ILogger from src/shared/application/ports/, adapter lives in src/shared/infrastructure/logger/.",
                  "pino": "Domain layer cannot import 'pino' directly (same reason as @bogeychan/elysia-logger, ADR 0003). Fix: Use ILogger port from src/shared/application/ports/."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["src/**/application/**", "tests/biome-contract/application/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "drizzle-orm": "Application layer cannot import 'drizzle-orm'. Reason: Use cases talk to ports, not ORMs (ADR 0003). Fix: Define a Repository port in this feature's application/ports/ and let infrastructure/ implement it with Drizzle.",
                  "postgres": "Application layer cannot import 'postgres'. Reason: Use cases talk to ports, not DB clients (ADR 0003). Fix: Inject the port defined in application/ports/; the adapter in infrastructure/ may use 'postgres'."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["src/**/application/**", "src/**/presentation/**", "tests/biome-contract/application/**", "tests/biome-contract/presentation/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": ["**/domain/internal/**", "**/domain/internal"],
                    "message": "Cannot import from domain/internal/. Reason: Only the domain/index.ts barrel is a legal entry point (ADR 0003, CONTEXT D-11). Fix: Import from '{feature}/domain' (the barrel) and use getXxxService(ctx) factory, never instantiate classes directly."
                  }
                ]
              }
            }
          }
        }
      }
    },
    {
      "includes": ["src/shared/kernel/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "drizzle-orm": "shared/kernel cannot import 'drizzle-orm'. Reason: The kernel (Result/Brand/UUID/DomainError) must be framework-free so any layer can use it (ADR 0003 §kernel). Fix: Keep kernel files pure TypeScript; put ORM bindings in src/shared/infrastructure/db/.",
                  "postgres": "shared/kernel cannot import 'postgres'. Reason: framework-free kernel (ADR 0003). Fix: Keep DB client code in src/shared/infrastructure/db/client.ts.",
                  "elysia": "shared/kernel cannot import 'elysia'. Reason: kernel is re-usable across any framework surface (ADR 0003). Fix: HTTP primitives live in src/shared/presentation/.",
                  "better-auth": "shared/kernel cannot import 'better-auth'. Reason: kernel is framework-free (ADR 0003, ADR 0004). Fix: BetterAuth adapter belongs in src/auth/infrastructure/.",
                  "@bogeychan/elysia-logger": "shared/kernel cannot import logger adapters. Reason: kernel is framework-free (ADR 0003). Fix: ILogger port lives in src/shared/application/ports/.",
                  "pino": "shared/kernel cannot import 'pino'. Reason: kernel is framework-free (ADR 0003). Fix: ILogger port in src/shared/application/ports/."
                }
              }
            }
          }
        }
      }
    }
  ]
}
```

**Why 4 overrides, not 3:** D-09 + D-11 原定 3 條；本 plan 加第 4 條覆蓋 `src/shared/kernel/**`，per RESEARCH.md §Pitfall #6（kernel 被誤加 framework import 的風險）。與 02-PLAN 的 `tests/contract/kernel-framework-free.test.ts` 形成 belt-and-suspenders。

**ADR 0003 / 0009 文件本 plan 尚未 ship（在 04-PLAN），但 message 內已引用檔名** — 這是預期行為：04-PLAN 會用本 plan 中 message 引用的檔名建立 ADR，即「rule 先聲明路徑、04-PLAN 填內容」。此為 Bootstrap Ordering §12 記載的設計決策。

完成後跑 `bun run lint`（本步 src/shared/kernel/ 有真實檔案但無違規 import——應 exit 0；若非 0 表示 Plan 02 的 kernel 意外違反 rule 4）。
  </action>
  <verify>
    <automated>bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f biome.json`
    - `grep -c '"overrides"' biome.json` 回 1
    - `grep -c '"includes"' biome.json` 回 ≥ 5（files.includes + 4 overrides.includes）
    - `grep -q '"src/\*\*/domain/\*\*"' biome.json`（override 1 glob 正確）
    - `grep -q '"src/\*\*/application/\*\*"' biome.json`（override 2 / 3 glob）
    - `grep -q '"src/\*\*/presentation/\*\*"' biome.json`（override 3）
    - `grep -q '"src/shared/kernel/\*\*"' biome.json`（override 4，Pitfall #6）
    - `grep -q '"tests/biome-contract/domain/\*\*"' biome.json`（contract test 共用 rule）
    - `grep -q 'Move Drizzle usage to src/{feature}/infrastructure/' biome.json`（四段 message Fix 段）
    - `grep -q 'Reason: Domain must stay framework-free' biome.json`（四段 message Why 段）
    - `grep -q 'see docs/decisions/0003-ddd-layering.md' biome.json`（四段 message ADR link 段）
    - `grep -q 'Cannot import from domain/internal/' biome.json`（D-11 patterns rule）
    - `grep -q 'getXxxService(ctx) factory' biome.json`（barrel rule 四段 Fix）
    - `grep -q '"getXxxService(ctx)"' biome.json || grep -q 'getXxxService(ctx)' biome.json`
    - Message count: `grep -c 'ADR 00' biome.json` ≥ 10（每 rule 都有 ADR 引用）
    - `bun run lint` exit 0（src/ 內無違規 code，rule 可 loading 無 parse error）
    - `bunx biome check --help 2>&1 | grep -qv "error"` 確認 biome CLI 可 parse 配置
  </acceptance_criteria>
  <done>
    4 條 overrides 就位；每 rule message 嚴守四段格式；src/shared/kernel/ 無違反 rule 4（證 02-PLAN 輸出合規）；`bun run lint` 通過。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立 9 個故意違規檔 + biome-rule-contract.test.ts contract test</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/biome.json (Task 1 產出；確認 includes 覆蓋 tests/biome-contract/<layer>/)
    - /Users/carl/Dev/CMG/Rigging/tsconfig.json (確認 exclude tests/biome-contract/**)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "CI + Runtime Guard 策略 §B3 (推薦)" — contract test 範例；Pitfall 5 — rule 寫錯沒人發現)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements — 3 個 biome-contract 檔清單)
  </read_first>
  <files>
    - tests/biome-contract/domain/drizzle-violation.ts
    - tests/biome-contract/domain/elysia-violation.ts
    - tests/biome-contract/domain/better-auth-violation.ts
    - tests/biome-contract/domain/postgres-violation.ts
    - tests/biome-contract/domain/logger-violation.ts
    - tests/biome-contract/application/drizzle-violation.ts
    - tests/biome-contract/application/postgres-violation.ts
    - tests/biome-contract/application/internal-barrel-violation.ts
    - tests/biome-contract/presentation/internal-barrel-violation.ts
    - tests/biome-contract/biome-rule-contract.test.ts
  </files>
  <action>
建立 9 個故意違規 TypeScript 檔 + 1 個 contract test runner。**每個違規檔第一行必須是 `// @ts-nocheck`**（tsconfig 已 exclude，但保險）。每個違規檔只做一件事：import forbidden package + export dummy value（讓違規顯性）。

### 違規檔清單（對應 biome.json overrides rule set）

**Override 1（domain 禁 framework）— 5 檔：**

`tests/biome-contract/domain/drizzle-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: domain layer must not import drizzle-orm (ARCH-02).
// Expected: biome check returns non-zero with message containing "Move Drizzle usage to".
import { drizzle } from 'drizzle-orm'
export const x = drizzle
```

`tests/biome-contract/domain/elysia-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: domain layer must not import elysia.
// Expected: biome check returns non-zero with message containing "HTTP concerns belong in".
import { Elysia } from 'elysia'
export const x = Elysia
```

`tests/biome-contract/domain/better-auth-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: domain layer must not import better-auth.
// Expected: biome check returns non-zero with message containing "IIdentityService port".
import { betterAuth } from 'better-auth'
export const x = betterAuth
```

`tests/biome-contract/domain/postgres-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: domain layer must not import 'postgres'.
// Expected: biome check returns non-zero with message containing "src/shared/infrastructure/db".
import postgres from 'postgres'
export const x = postgres
```

`tests/biome-contract/domain/logger-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: domain layer must not import logger adapters.
// Expected: biome check returns non-zero with message containing "ILogger from src/shared/application/ports".
import { logger } from '@bogeychan/elysia-logger'
export const x = logger
```

**Override 2（application 禁 drizzle-orm/postgres）— 2 檔：**

`tests/biome-contract/application/drizzle-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: application layer must not import drizzle-orm.
// Expected: biome check returns non-zero with message containing "Repository port".
import { drizzle } from 'drizzle-orm'
export const x = drizzle
```

`tests/biome-contract/application/postgres-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: application layer must not import 'postgres'.
// Expected: biome check returns non-zero with message containing "Inject the port".
import postgres from 'postgres'
export const x = postgres
```

**Override 3（application + presentation 禁 domain/internal）— 2 檔：**

`tests/biome-contract/application/internal-barrel-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: application layer must not import from domain/internal (D-11).
// Expected: biome check returns non-zero with message containing "getXxxService(ctx) factory".
import { UserServiceImpl } from '../../../src/_template/domain/internal/user-service'
export const x = UserServiceImpl
```

`tests/biome-contract/presentation/internal-barrel-violation.ts`：
```typescript
// @ts-nocheck
// Intentional violation: presentation layer must not import from domain/internal (D-11).
// Expected: biome check returns non-zero with message containing "Only the domain/index.ts barrel".
import { UserServiceImpl } from '../../../src/_template/domain/internal/user-service'
export const x = UserServiceImpl
```

### Contract test runner

`tests/biome-contract/biome-rule-contract.test.ts`：

```typescript
import { describe, expect, test } from 'bun:test'
import { spawnSync } from 'node:child_process'

/**
 * Runs `bunx biome check <file>` and returns exit code + combined stdout/stderr.
 * We invoke biome CLI directly rather than `bun run lint` because `lint` runs
 * against the whole repo; here we want single-file diagnostic for each violator.
 */
function runBiome(file: string): { exitCode: number; output: string } {
  const proc = spawnSync('bunx', ['biome', 'check', file], {
    encoding: 'utf8',
    cwd: process.cwd(),
  })
  const output = `${proc.stdout ?? ''}\n${proc.stderr ?? ''}`
  return { exitCode: proc.status ?? -1, output }
}

const CASES: Array<{ file: string; mustContain: string[] }> = [
  {
    file: 'tests/biome-contract/domain/drizzle-violation.ts',
    mustContain: ['Move Drizzle usage to src/{feature}/infrastructure/'],
  },
  {
    file: 'tests/biome-contract/domain/elysia-violation.ts',
    mustContain: ['HTTP concerns belong in src/{feature}/presentation/'],
  },
  {
    file: 'tests/biome-contract/domain/better-auth-violation.ts',
    mustContain: ['IIdentityService port'],
  },
  {
    file: 'tests/biome-contract/domain/postgres-violation.ts',
    mustContain: ['src/shared/infrastructure/db/client.ts'],
  },
  {
    file: 'tests/biome-contract/domain/logger-violation.ts',
    mustContain: ['ILogger from src/shared/application/ports/'],
  },
  {
    file: 'tests/biome-contract/application/drizzle-violation.ts',
    mustContain: ['Repository port in this feature'],
  },
  {
    file: 'tests/biome-contract/application/postgres-violation.ts',
    mustContain: ['Inject the port defined in application/ports/'],
  },
  {
    file: 'tests/biome-contract/application/internal-barrel-violation.ts',
    mustContain: ['Only the domain/index.ts barrel', 'getXxxService(ctx) factory'],
  },
  {
    file: 'tests/biome-contract/presentation/internal-barrel-violation.ts',
    mustContain: ['Only the domain/index.ts barrel'],
  },
]

describe('Biome rule contract: restricted imports are actually blocked (ARCH-02, D-09, D-11, D-12)', () => {
  for (const { file, mustContain } of CASES) {
    test(`biome check ${file} exits non-zero`, () => {
      const { exitCode, output } = runBiome(file)
      expect(exitCode).not.toBe(0)
      for (const token of mustContain) {
        expect(output).toContain(token)
      }
    })
  }
})
```

**Negative smoke (executor 手動一次)：** 暫時將某一違規檔的 forbidden import 改成無害（如 `import { describe } from 'bun:test'`）、跑 `bun test tests/biome-contract/biome-rule-contract.test.ts` 應 fail（表示 contract test 有 bite）；確認後還原。**非 acceptance，但 executor SUMMARY.md 需註明已手動驗證過 negative 路徑。**

完成後跑 `bun test tests/biome-contract/biome-rule-contract.test.ts`；exit 0（全 9 case pass）。
  </action>
  <verify>
    <automated>bun test tests/biome-contract/biome-rule-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -d tests/biome-contract/domain && test -d tests/biome-contract/application && test -d tests/biome-contract/presentation`
    - `ls tests/biome-contract/domain/*.ts | wc -l` 回 5
    - `ls tests/biome-contract/application/*.ts | wc -l` 回 3
    - `ls tests/biome-contract/presentation/*.ts | wc -l` 回 1
    - 每個違規檔首行含 `// @ts-nocheck`：`head -1 tests/biome-contract/domain/drizzle-violation.ts | grep -q '@ts-nocheck'`（對所有 9 檔都 true）
    - `test -f tests/biome-contract/biome-rule-contract.test.ts`
    - `grep -q "from 'bun:test'" tests/biome-contract/biome-rule-contract.test.ts`
    - `grep -q "spawnSync" tests/biome-contract/biome-rule-contract.test.ts`
    - `grep -qE "bunx.*biome.*check" tests/biome-contract/biome-rule-contract.test.ts`
    - `grep -c "mustContain" tests/biome-contract/biome-rule-contract.test.ts` ≥ 9（9 CASES）
    - `bun test tests/biome-contract/biome-rule-contract.test.ts` exit 0
    - `bun test tests/biome-contract/biome-rule-contract.test.ts 2>&1 | grep -cE '\(pass\)|PASS'` ≥ 9
    - tsc 仍乾淨（tests/biome-contract/** 已於 01-PLAN 從 tsconfig.json exclude）：`bun run typecheck` exit 0
  </acceptance_criteria>
  <done>
    9 個違規檔就位、contract test 驗每檔 biome check 都 fail 且 message 含具體修法 token；證明 4 條 overrides 皆生效。
  </done>
</task>

<task type="auto">
  <name>Task 3: 建立 AUX-06 Phase 3 stub（tests/integration/auth-bypass-contract.test.ts）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "CI + Runtime Guard 策略 §C — Placeholder integration test" — 建議 ship skipped stub 讓 AUX-06 ID 占位)
    - /Users/carl/Dev/CMG/Rigging/.planning/REQUIREMENTS.md (AUX-06 — 不掛 auth plugin 則 protected route 必回 401)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements — tests/integration/auth-bypass-contract.test.ts)
  </read_first>
  <files>
    - tests/integration/auth-bypass-contract.test.ts
  </files>
  <action>
建立 **skipped** integration test stub，預留 Phase 3 AUX-06 實作槽。P1 無 Elysia app、無 auth plugin，但該 test ID 必須於 CI 可見以避免 Phase 3 executor 漏找檔。

```typescript
import { test, expect } from 'bun:test'

/**
 * [AUX-06, Phase 3] app without auth plugin returns 401 on protected routes.
 *
 * P1 ships this stub so the test ID is claimed;
 * Phase 3 (Auth Foundation) will replace `test.skip` with `test` and implement:
 *   1. Bootstrap app WITHOUT .use(authContextPlugin)
 *   2. Call every protected route (those declaring { requireAuth: true })
 *   3. Expect all to return 401 (not 500, not stale data)
 *
 * Reference: AGENTS.md Rigidity Map Tier 1 rule 1; docs/decisions/0006-authcontext-boundary.md;
 * docs/decisions/0007-runtime-guards-via-di.md.
 * Pitfall cross-reference: .planning/research/PITFALLS.md #3 (AuthContext bypass).
 */
test.skip('[AUX-06, Phase 3] app without auth plugin returns 401 on protected routes', () => {
  // Placeholder assertion — will be replaced in Phase 3.
  expect(true).toBe(true)
})
```

完成後跑 `bun test tests/integration/auth-bypass-contract.test.ts`；exit 0（skip 不視為失敗）；output 含 "skip" 字眼表明 placeholder 意圖明確。

同時執行整體 Wave-end gate：`bun run lint && bun run typecheck && bun test`；全部 exit 0（本 plan 的 contract + stub 加入 bun test suite 應全綠）。
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/integration/auth-bypass-contract.test.ts`
    - `grep -q "from 'bun:test'" tests/integration/auth-bypass-contract.test.ts`
    - `grep -q "test.skip" tests/integration/auth-bypass-contract.test.ts`
    - `grep -q "\[AUX-06" tests/integration/auth-bypass-contract.test.ts`（test 名含 REQ-ID）
    - `grep -q "Phase 3" tests/integration/auth-bypass-contract.test.ts`（提示後續 phase）
    - `grep -q "0006-authcontext-boundary.md\|0007-runtime-guards-via-di.md" tests/integration/auth-bypass-contract.test.ts`（ADR 引用）
    - `bun test tests/integration/auth-bypass-contract.test.ts 2>&1 | grep -qE 'skip'`（bun:test 輸出確認 skipped）
    - Wave-end full gate：`bun run lint && bun run typecheck && bun test` exit 0
  </acceptance_criteria>
  <done>
    AUX-06 Phase 3 stub 就位、標記為 skip 但 ID + ADR 引用明確；Wave 2 side plan 全綠；整體 `bun run lint && bun run typecheck && bun test` 通過。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → repo | PR 進 main 前 Biome rule 是「錯誤寫法跑不起來」的物理閘門 |
| developer → Biome rule | developer 可能用 `// biome-ignore` 繞過 rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | `// biome-ignore` 繞過 rule | mitigate | ADR 0009（04-PLAN ship）Tier 1 rule 2 明記「禁無 ADR 的 biome-ignore」；Phase 5 CI 可加 `grep 'biome-ignore' src/` 統計（本 plan 不 ship CI grep 留給 Phase 5 DOC/CI phase）|
| T-01-03-02 | Tampering | Biome override glob 寫錯，rule 未生效 | mitigate | Task 2 contract tests 對 9 個違規檔 assert fail + message token；glob 若寫錯則對應 case 的 exit code 為 0，立即 CI fail |
| T-01-03-03 | Tampering | `tests/biome-contract/` 被誤用為真實 code（import 到 production）| mitigate | tsconfig.json exclude `tests/biome-contract/**` (01-PLAN)；首行 `// @ts-nocheck`；無 production import path（檔名含 "violation"）|
| T-01-03-04 | Information Disclosure | Biome rule message 洩漏 ADR 位置讓攻擊者推測內部結構 | accept | message 只含路徑提示（`src/{feature}/infrastructure/`），無 secret；公開 repo 本就可見此結構 |
</threat_model>

<verification>
```bash
# biome 配置正確 load
bunx biome check --help  # 無 parse error

# biome rule 真實生效（9 case all fail）
bun test tests/biome-contract/biome-rule-contract.test.ts

# ARCH-02 現行驗證：domain 違規被擋
cd $(git rev-parse --show-toplevel) && bunx biome check tests/biome-contract/domain/drizzle-violation.ts && echo 'FAIL: violation should have errored'
# 上面 biome check 應 exit 非 0；若 echo 到 FAIL: 則 rule 未生效

# 全綠 gate
bun run lint && bun run typecheck && bun test
```
</verification>

<success_criteria>
1. **ARCH-02**（Biome 擋 domain 層 framework import）— `bun test tests/biome-contract/biome-rule-contract.test.ts` 9 case 全 pass；每 case assert biome check exit 非 0 + message 含具體修法 token
2. **ARCH-03**（Repository 回 domain entity 的 static policy）— override 1 + 2 + 3 + 4 共同封鎖 domain / application 層洩漏 Drizzle row 的 import path；ADR 0003 於 04-PLAN 提供設計聲明；Phase 3 執行時物理驗證
3. **D-09 / D-11 / D-12** — 4 條 overrides 就位；每 message 四段格式齊備；D-11 `patterns[].group` glob 封鎖 domain/internal/
4. `bun run lint && bun run typecheck && bun test` 於 Wave 2 結束時全綠
5. AUX-06 Phase 3 stub 就位並 skip
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` summarising:
- biome.json overrides 4 條 scope + forbidden modules（列表）
- 9 violation files + contract test 結果（pass count）
- 四段 message 格式於 rule 內的具體位置
- Handoff to 04-PLAN：message 已引用的 ADR 檔名（`0003-ddd-layering.md` / `0006-authcontext-boundary.md` / `0007-runtime-guards-via-di.md` / `0004-auth-betterauth.md` / `0009-rigidity-map.md`）必須由 04-PLAN 精確 ship（檔名一字不能差）
- Negative smoke（手動改一違規檔為合法 import、跑 contract test 應 fail）已驗證
- AUX-06 Phase 3 stub 就緒
</output>
