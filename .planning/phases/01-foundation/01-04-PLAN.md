---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-foundation/01"]
files_modified:
  - docs/decisions/0000-use-madr-for-adrs.md
  - docs/decisions/0001-runtime-bun.md
  - docs/decisions/0002-web-framework-elysia.md
  - docs/decisions/0003-ddd-layering.md
  - docs/decisions/0004-auth-betterauth.md
  - docs/decisions/0005-orm-drizzle.md
  - docs/decisions/0006-authcontext-boundary.md
  - docs/decisions/0007-runtime-guards-via-di.md
  - docs/decisions/0008-dual-auth-session-and-apikey.md
  - docs/decisions/0009-rigidity-map.md
  - docs/decisions/0010-postgres-driver-postgres-js.md
  - docs/decisions/0011-resolver-precedence-apikey-over-cookie.md
  - docs/decisions/README.md
  - .github/PULL_REQUEST_TEMPLATE.md
  - .github/workflows/adr-check.yml
  - AGENTS.md
autonomous: true
requirements: [ADR-01, ADR-02, ADR-03, ADR-04, ADR-05, AGM-01, AGM-02]

must_haves:
  truths:
    - "`docs/decisions/` 目錄下有恰好 12 個 MADR 4.0 格式 ADR（0000-0011）"
    - "每個 ADR 含 YAML front matter（status: accepted、date: 2026-04-19、deciders、consulted 欄位）與 MADR 'full' 結構（Context / Decision Drivers / Considered Options / Decision Outcome / Consequences / Pros and Cons）"
    - "`docs/decisions/README.md` 索引表列出 12 條 ADR，欄位：編號 / 標題 / Status / 日期 / Supersedes"
    - "`.github/PULL_REQUEST_TEMPLATE.md` 含 ADR checkbox 三選一 + 索引更新副條件 + Rigidity Map tier 選擇"
    - "`.github/workflows/adr-check.yml` 於 PR body 勾「需 ADR」但 diff 未新增 `docs/decisions/*.md` 時 CI fail"
    - "`AGENTS.md` 新增 `<!-- RIGGING:rigidity-map-* -->` 與 `<!-- RIGGING:anti-features-* -->` 段落，位置於 `<!-- GSD:workflow-end -->` 之後、`<!-- GSD:profile-start -->` 之前"
    - "AGENTS.md Rigidity Map 列 Tier 1 / Tier 2 / Tier 3 三級各自條目與 ADR cross-reference"
    - "AGENTS.md Anti-features 列 12 項 v2 out-of-scope 清單與 'That's a v2 feature' 回應 template"
  artifacts:
    - path: "docs/decisions/0000-use-madr-for-adrs.md"
      provides: "自指 ADR，聲明使用 MADR 4.0 'full' variant + NNNN-kebab-case 命名 + README.md 索引欄位"
    - path: "docs/decisions/0009-rigidity-map.md"
      provides: "Rigidity Map 三級嚴格度 ADR"
      contains: "Tier 1"
    - path: "docs/decisions/0010-postgres-driver-postgres-js.md"
      provides: "postgres-js driver（NOT bun:sql）+ revisit 條件（bun#21934 AND bun#22395 同時關閉）"
      contains: "bun#21934"
    - path: "docs/decisions/0011-resolver-precedence-apikey-over-cookie.md"
      provides: "Resolver precedence ADR + P3 spike 驗證計畫於 Decision Drivers"
      contains: "P3"
    - path: "docs/decisions/README.md"
      provides: "12 條 ADR 索引表 + MADR workflow"
      contains: "Supersedes"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR template 含 ADR checkbox + Rigidity Map tier 選擇"
      contains: "ADR"
    - path: ".github/workflows/adr-check.yml"
      provides: "GitHub Actions: PR body 勾「需 ADR」但 diff 無新 ADR → fail"
      contains: "docs/decisions"
    - path: "AGENTS.md"
      provides: "原 GSD 區塊保留 + 新增 RIGGING Rigidity Map 與 anti-features 自管理段落"
      contains: "RIGGING:rigidity-map-start"
  key_links:
    - from: "AGENTS.md RIGGING:rigidity-map"
      to: "docs/decisions/0009-rigidity-map.md"
      via: "source: comment tag + prose reference"
      pattern: "0009-rigidity-map"
    - from: "biome.json (Plan 03) rule messages"
      to: "docs/decisions/0003-ddd-layering.md / 0006 / 0009"
      via: "message string literal"
      pattern: "docs/decisions/0003"
    - from: ".github/workflows/adr-check.yml"
      to: "docs/decisions/*.md"
      via: "git diff path filter"
      pattern: "docs/decisions"
---

<objective>
Phase 1 Plan 04 — **ADR Seed + AGENTS.md + PR Gate**：一次 ship 12 條 MADR 4.0 ADR、`docs/decisions/README.md` 索引、PR template 的 ADR checkbox、GitHub Actions adr-check workflow、AGENTS.md 的 Rigidity Map 與 Anti-features 自管理段落。這是 ADR-01..ADR-05 + AGM-01..AGM-02 的落地，也是 03-PLAN 的 Biome rule message 引用 ADR 檔名的「填空」。

Purpose: 讓 Rigging 的紀律有「活文件」— Biome rule error message 引用的 `docs/decisions/0003-ddd-layering.md` 必須實際存在且內容對齊；AGENTS.md 的 Rigidity Map 讓 AI Agent 接手時首讀即知三級嚴格度；adr-check workflow 把「需 ADR 的 PR 必須 commit ADR」物理化。
Output: 12 ADR files + 索引 README + PR template + CI workflow + AGENTS.md 兩個自管理段落。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@AGENTS.md

<rationale>
- `RESEARCH.md §ADR Seed Content 摘要` 提供 12 條 ADR 的 Context / Decision / Consequences 初稿；executor 移植並補齊 MADR 'full' variant 的其他段落（Decision Drivers / Considered Options / Pros and Cons）。
- `RESEARCH.md §docs/decisions/README.md 索引範例` 提供索引 template（12 列）。
- `CONTEXT.md §D-13..D-16` 鎖定 12 條 ADR 編號、檔名、0011 於 P1 即 accepted、索引欄位（編號 / 標題 / Status / 日期 / Supersedes）、PR template + CI lint 結構。
- `RESEARCH.md §AGENTS.md Rigidity Map 段落 draft` 提供完整自管理段落模板（`<!-- RIGGING:rigidity-map-start -->` ... `<!-- RIGGING:anti-features-end -->`）。
- `RESEARCH.md §CI + Runtime Guard §D` 提供 `.github/workflows/adr-check.yml` 完整腳本（D-16 實作）。
- `RESEARCH.md §Pitfall 3` 警告 YAML front matter 格式不一致會破索引工具：12 條 ADR 的 front matter 格式必嚴格統一（`status:` 小寫、`date:` ISO 格式）。
- `RESEARCH.md §Pitfall 7` 警告 `<!-- RIGGING:* -->` 標記可能被 GSD 工具覆蓋；本 plan 雙寫保險（AGENTS.md + ADR 0009 canonical）。
- `RESEARCH.md §Assumption A7` 記錄 12 條 ADR 同日期屬 bootstrap 特例可接受，但 ADR 0000 須明說此 convention。
- **ADR 0011 於 P1 即 `status: accepted`**（per D-14）；但 Decision Drivers 段必包含 P3 spike 驗證計畫（per RESEARCH.md §0011）。
- **ADR 0010 revisit 條件**：bun#21934 AND bun#22395 同時關閉（per D-13，RESEARCH.md §0010）；須在 Decision 段明寫。
- **`.github/workflows/ci.yml`（本 plan 不 ship）**：完整 CI workflow 於 05-PLAN；本 plan 只 ship `adr-check.yml` 這一支獨立 workflow，因為 D-16 的 PR-body-ADR-linkage 與 lint/typecheck/test 邏輯正交。
</rationale>

<interfaces>
<!-- MADR 4.0 "full" template (reference, from adr.github.io/madr) -->

```markdown
---
status: accepted
date: 2026-04-19
deciders: the-team
consulted: research/STACK.md, research/ARCHITECTURE.md, research/PITFALLS.md
informed: future AI Agents and maintainers
---

# NNNN. Title (kebab-case of filename)

## Context and Problem Statement

...

## Decision Drivers

- driver 1
- driver 2

## Considered Options

- Option A
- Option B

## Decision Outcome

Chosen option: "Option A", because ...

### Consequences

- Good, because ...
- Bad, because ...

## Pros and Cons of the Options

### Option A

- Good, ...
- Bad, ...

### Option B

- ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 12 條 ADR (docs/decisions/0000-0011) + README 索引</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "ADR Seed Content 摘要" — 完整 12 條 ADR 的 Context/Decision/Consequences 初稿；docs/decisions/README.md 索引範例)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (D-13 / D-14 / D-15 — ADR 編號順序、0011 P1 accepted、索引欄位)
    - /Users/carl/Dev/CMG/Rigging/.planning/research/STACK.md (提供 ADR 0001 / 0002 / 0004 / 0005 / 0010 的技術細節)
    - /Users/carl/Dev/CMG/Rigging/.planning/research/PITFALLS.md (ADR 0004 / 0005 / 0010 的 revisit/caveat 依據：#5 bun:sql / #14 Bun native module / BetterAuth CVE)
    - /Users/carl/Dev/CMG/Rigging/biome.json (Plan 03 產出；確認 message 引用的 ADR 檔名與本 Task 檔名對齊：0003-ddd-layering.md / 0006-authcontext-boundary.md / 0007-runtime-guards-via-di.md / 0004-auth-betterauth.md / 0009-rigidity-map.md)
  </read_first>
  <files>
    - docs/decisions/0000-use-madr-for-adrs.md
    - docs/decisions/0001-runtime-bun.md
    - docs/decisions/0002-web-framework-elysia.md
    - docs/decisions/0003-ddd-layering.md
    - docs/decisions/0004-auth-betterauth.md
    - docs/decisions/0005-orm-drizzle.md
    - docs/decisions/0006-authcontext-boundary.md
    - docs/decisions/0007-runtime-guards-via-di.md
    - docs/decisions/0008-dual-auth-session-and-apikey.md
    - docs/decisions/0009-rigidity-map.md
    - docs/decisions/0010-postgres-driver-postgres-js.md
    - docs/decisions/0011-resolver-precedence-apikey-over-cookie.md
    - docs/decisions/README.md
  </files>
  <action>
建立 12 條 ADR，**全部採 MADR 4.0 "full" variant**（per D-13 + RESEARCH.md §Assumption A7；此選擇由 ADR 0000 自指）。每 ADR：

### 檔名規則

`docs/decisions/NNNN-kebab-case-title.md`（per D-13；禁止大寫、禁止 space、副檔名 `.md`）。

### YAML front matter（格式統一，per RESEARCH.md Pitfall #3）

每個 ADR 首 8 行（含 `---` 分隔）：

```yaml
---
status: accepted
date: 2026-04-19
deciders: the-team
consulted: .planning/research/STACK.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md
informed: future AI Agents and future maintainers
---
```

**不可有變體**（status 一律小寫、date 一律 ISO 8601；索引工具依賴此格式）。

### 各 ADR 內容（依 RESEARCH.md §ADR Seed Content 摘要 移植並補 MADR full variant 缺段）

每 ADR 必須含以下章節（標題文字固定，順序固定）：

1. `# NNNN. Title`
2. `## Context and Problem Statement`
3. `## Decision Drivers`（bullet list）
4. `## Considered Options`（bullet list）
5. `## Decision Outcome`（以 "Chosen option: ..., because ..." 開頭）
6. `### Consequences`（子段 of Decision Outcome；含 Good / Bad pros-cons）
7. `## Pros and Cons of the Options`（各 Option 獨立子段）

以下為**每 ADR 的關鍵內容要點**（executor 以此為骨架、填補 MADR full variant 所有段落——RESEARCH.md §ADR Seed Content 摘要已提供大部分內容，executor 無須憑空創作）：

- **0000-use-madr-for-adrs.md**: self-referential。Decision: MADR 4.0 "full" + `NNNN-kebab-case-title.md` + README 索引欄位 `編號 / 標題 / Status / 日期 / Supersedes`（per D-15）。Consequences 段明記 Assumption A7（12 條 seed 同日期為 bootstrap 特例，後續 ADR 日期自然分散）。
- **0001-runtime-bun.md**: Bun `^1.3.12`。Consequences 提 `Bun.password` argon2 / `bun:test` / Pitfall #14 (native module 2-5% 不相容)。
- **0002-web-framework-elysia.md**: Elysia `^1.4.28`（**≥ 1.4 強制**，`.mount()` Set-Cookie fix；peer-dep TypeBox 0.34）。Consequences 提 elysia#1366 scoped-plugin undefined cascade 需 runtime guard（cross-ref 0007）。
- **0003-ddd-layering.md**: `src/{feature}/{domain,application,infrastructure,presentation}/` 四層 + `src/shared/kernel/` framework-free + Biome `noRestrictedImports` overrides（cross-ref `biome.json` 具體 rule set：domain 禁 drizzle-orm / elysia / better-auth / postgres / @bogeychan/elysia-logger / pino；application 禁 drizzle-orm / postgres；application + presentation 禁 domain/internal/**；kernel 禁全 framework per Pitfall #6）。Consequences 提 Pitfall #11（ADR rot）+ Phase 4 dogfood 驗證。
- **0004-auth-betterauth.md**: BetterAuth `1.6.5`（**exact pin, 無 caret**）+ `@better-auth/drizzle-adapter@1.6.5`。Decision Drivers 含 CVE-2025-61928 exact pin 理由 + Lucia maintenance 狀態 + framework-agnostic。Consequences 提訂閱 GHSA advisory.
- **0005-orm-drizzle.md**: Drizzle `^0.45.2`（**NOT 1.0-beta**）+ `drizzle-kit ^0.31.10`。Decision Drivers 提 beta churn；revisit 條件：`latest` tag flip 時開 0005a migration ADR。
- **0006-authcontext-boundary.md**: `AuthContext` 是 Domain service 唯一取得路徑；factory `getXxxService(ctx: AuthContext)`；class internal, barrel `domain/index.ts`；Elysia `.macro({ requireAuth: { resolve } })` 是**唯一**產生 AuthContext 路徑。Consequences cross-ref 0007 / 0009。
- **0007-runtime-guards-via-di.md**: `if (!ctx?.userId) throw new AuthContextMissingError(...)` 在 factory 頭一行；不信 TS narrowing（elysia#1366）。Consequences：多一行 boilerplate/factory、integration test 可 assert 不掛 auth plugin 則全 401（AUX-06）。
- **0008-dual-auth-session-and-apikey.md**: 單一 AuthContext type；resolver `x-api-key` → cookie session；dual track 產同型別。Consequences 提 resolver precedence 必明定（cross-ref 0011）。
- **0009-rigidity-map.md**: Rigidity Map 三級（per CONTEXT.md §D-01..D-03）：
  - **Tier 1（必嚴格無逃生口）**：AuthContext is only path / Domain framework-free / core stack pin（Bun/Elysia/BetterAuth/Drizzle/postgres 版本）
  - **Tier 2（ADR 逃生）**：validator 選擇 / Postgres driver / logger / migration strategy / resolver precedence
  - **Tier 3（純約定）**：naming 約定、commit format、branch naming
  - **Detection**: CI (biome + tsc + bun test) + runtime (getXxxService factory) + **no pre-commit hook**（per D-04）
  - **Consequences** 明記 P1 application 層允許 import elysia/better-auth 為放寬決策，P3 若需收緊則寫 0009a（與 D-09 備註同步）
- **0010-postgres-driver-postgres-js.md**: `drizzle-orm/postgres-js` + `postgres@^3.4.9`；`bun:sql` 絕不在 v1。Decision Drivers 提 bun#21934 / bun#22395 / bun#17178 / bun#23215 均 open 2026-04-18。**Revisit 條件（硬性）**：bun#21934 **AND** bun#22395 同時關閉 → 寫 `0010a-postgres-driver-revisit.md` Supersedes 本 ADR。此段文字須一字不差包含 "bun#21934 AND bun#22395"。
- **0011-resolver-precedence-apikey-over-cookie.md**: Decision: API Key 優先 cookie（header `x-api-key` 存在且驗 OK → agent；否則 cookie → human；否則 401）。`status: accepted`（P1 即 lock per D-14）。**Decision Drivers 段必寫 P3 spike 驗證計畫**（per RESEARCH.md §0011）：
  - 模擬 browser dev tool 同時帶 cookie + API Key → 確認 'agent' 語意
  - 模擬 Agent CLI 帶 API Key → 確認 cookie 空也能通過
  - 模擬誤用 human cookie 配錯 API Key → 確認拒絕（401）、而非降級 cookie path
  - 若 spike 不符預期 → 寫 0011a Supersedes 本 ADR；不回改本 ADR Decision Outcome

### `docs/decisions/README.md` 索引

依 RESEARCH.md §docs/decisions/README.md 索引範例精確建立，欄位固定為 **編號 / 標題 / Status / 日期 / Supersedes**（per D-15）；12 列全 `accepted` / `2026-04-19` / `—`（無 supersedes）；加 `## Workflow` 段落說明 propose → accept → supersede lifecycle。

**Secret hygiene（per threat model）**：任何 ADR 範例 env / config 字串，不能寫成「真 secret」格式。`BETTER_AUTH_SECRET` 等若需舉例，使用 placeholder `<32-byte random hex, see .env.example>` 明示占位。

完成後執行：
1. `ls docs/decisions/0*.md | wc -l` 回 12
2. `for f in docs/decisions/0*.md; do head -7 "$f" | grep -q '^status: accepted$' || echo "FAIL: $f"; done` 無輸出
3. 手動 `diff` 1-2 條 ADR 的 Context 段與 RESEARCH.md §ADR Seed Content 摘要對應節，確認無遺漏關鍵事實（CVE 編號、bun#XXXXX、版本號）。
  </action>
  <verify>
    <automated>test $(ls docs/decisions/{0000,0001,0002,0003,0004,0005,0006,0007,0008,0009,0010,0011}-*.md 2>/dev/null | wc -l) -eq 12 && test -f docs/decisions/README.md && for f in docs/decisions/0*.md; do head -7 "$f" | grep -q "^status: accepted$" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/decisions/0000-use-madr-for-adrs.md docs/decisions/0001-runtime-bun.md docs/decisions/0002-web-framework-elysia.md docs/decisions/0003-ddd-layering.md docs/decisions/0004-auth-betterauth.md docs/decisions/0005-orm-drizzle.md docs/decisions/0006-authcontext-boundary.md docs/decisions/0007-runtime-guards-via-di.md docs/decisions/0008-dual-auth-session-and-apikey.md docs/decisions/0009-rigidity-map.md docs/decisions/0010-postgres-driver-postgres-js.md docs/decisions/0011-resolver-precedence-apikey-over-cookie.md` 全部存在
    - `ls docs/decisions/0*.md | wc -l` 恰好 12
    - **ADR-02 起始 9 ADR（0000-0008）split count**：`ls docs/decisions/{0000,0001,0002,0003,0004,0005,0006,0007,0008}-*.md 2>/dev/null | wc -l` 恰好 9
    - **ADR-03 追加 3 ADR（0009-0011）split count**：`ls docs/decisions/{0009,0010,0011}-*.md 2>/dev/null | wc -l` 恰好 3
    - 所有 12 檔前 7 行 YAML front matter 含 `status: accepted`：`for f in docs/decisions/0*.md; do head -7 "$f" | grep -q "^status: accepted$" || exit 1; done` exit 0
    - `for f in docs/decisions/0*.md; do head -7 "$f" | grep -q "^date: 2026-04-19$" || exit 1; done` exit 0
    - 所有 ADR 含 `## Context and Problem Statement`、`## Decision Drivers`、`## Considered Options`、`## Decision Outcome`、`### Consequences`、`## Pros and Cons of the Options` 章節：`for f in docs/decisions/0*.md; do grep -q '## Context and Problem Statement' "$f" && grep -q '## Decision Drivers' "$f" && grep -q '## Considered Options' "$f" && grep -q '## Decision Outcome' "$f" && grep -q '### Consequences' "$f" && grep -q '## Pros and Cons of the Options' "$f" || exit 1; done` exit 0
    - `grep -q 'bun#21934' docs/decisions/0010-postgres-driver-postgres-js.md && grep -q 'bun#22395' docs/decisions/0010-postgres-driver-postgres-js.md`（revisit 條件 per D-13）
    - `grep -qi 'Tier 1' docs/decisions/0009-rigidity-map.md && grep -qi 'Tier 2' docs/decisions/0009-rigidity-map.md && grep -qi 'Tier 3' docs/decisions/0009-rigidity-map.md`
    - `grep -q 'P3 spike' docs/decisions/0011-resolver-precedence-apikey-over-cookie.md`（P3 驗證計畫）
    - `grep -q 'API Key' docs/decisions/0011-resolver-precedence-apikey-over-cookie.md`
    - `grep -q '1.6.5' docs/decisions/0004-auth-betterauth.md`（BetterAuth exact pin）
    - `grep -q '0.45' docs/decisions/0005-orm-drizzle.md`（Drizzle non-beta）
    - `grep -q '1.4' docs/decisions/0002-web-framework-elysia.md`（Elysia ≥1.4）
    - `grep -q '1.3.12' docs/decisions/0001-runtime-bun.md`（Bun version）
    - `test -f docs/decisions/README.md`
    - `grep -q '\| Status \|' docs/decisions/README.md`（索引有 Status 欄）
    - `grep -q 'Supersedes' docs/decisions/README.md`（索引有 Supersedes 欄；per D-15）
    - `grep -c '| \[0' docs/decisions/README.md` 回 12（索引 12 列）
    - `grep -q 'Workflow' docs/decisions/README.md`（lifecycle section）
    - 無真實 secret 寫入 ADR：`! grep -qE '[A-F0-9]{32,}' docs/decisions/*.md`（32+ hex 字串啟發式）
  </acceptance_criteria>
  <done>
    12 條 MADR 4.0 full-variant ADR 就位、YAML front matter 格式統一、索引 README 列 12 條；所有 03-PLAN biome rule message 引用的檔名（0003/0006/0007/0004/0009）皆實際存在；0010 revisit 條件與 0011 P3 spike 計畫明寫。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立 PR template + adr-check GitHub Actions workflow</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: ".github/PULL_REQUEST_TEMPLATE.md 草稿" + "CI + Runtime Guard §D §adr-check workflow")
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (D-16 — PR checkbox + CI lint structure)
    - /Users/carl/Dev/CMG/Rigging/docs/decisions/README.md (Task 1 產出；PR template 引用索引更新)
  </read_first>
  <files>
    - .github/PULL_REQUEST_TEMPLATE.md
    - .github/workflows/adr-check.yml
  </files>
  <action>
### 1. `.github/PULL_REQUEST_TEMPLATE.md`

精確抄寫 RESEARCH.md §Code Examples §.github/PULL_REQUEST_TEMPLATE.md 草稿，**須含以下結構**（per D-16）：

```markdown
## Summary

<!-- What does this PR do? 1-3 sentences. -->

## Linked issues / requirements

<!-- e.g., Closes #12; addresses REQ-XXX -->

## ADR checkpoint

- [ ] This PR introduces a decision that requires a new ADR — I added `docs/decisions/NNNN-*.md`
- [ ] This PR references an existing ADR — link: <!-- e.g., docs/decisions/0003-ddd-layering.md -->
- [ ] This PR does NOT require an ADR — the change fits within existing locked decisions

If ADR checkbox is marked YES:
- [ ] Did you update `docs/decisions/README.md` index?

## Rigidity Map

Which Rigidity Map tier does this change touch? (see AGENTS.md `<!-- RIGGING:rigidity-map-start -->`)

- [ ] Tier 1 (must be rigid) — explain below why this is OK
- [ ] Tier 2 (ADR-escapable) — ADR linked above
- [ ] Tier 3 (convention) — no gate

## Testing

<!-- How did you verify? -->
- [ ] `bun run lint` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run test` passes
```

### 2. `.github/workflows/adr-check.yml`

依 RESEARCH.md §CI + Runtime Guard §D §adr-check workflow 精確抄寫。**本 plan 不 ship `ci.yml`（完整 lint/typecheck/test workflow 於 05-PLAN）；本 plan 只 ship 這支獨立 workflow**：

```yaml
name: ADR Check

on:
  pull_request:
    branches: [main]

jobs:
  adr-check:
    # D-16: 若 PR body 勾「需 ADR」但 diff 未新增 docs/decisions/*.md → fail
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: adr-required
        run: |
          body="${{ github.event.pull_request.body }}"
          if echo "$body" | grep -qE '\- \[x\] This PR introduces a decision that requires a new ADR'; then
            echo "required=true" >> $GITHUB_OUTPUT
          else
            echo "required=false" >> $GITHUB_OUTPUT
          fi
      - if: steps.adr-required.outputs.required == 'true'
        run: |
          added=$(git diff --name-only --diff-filter=A origin/${{ github.base_ref }}...HEAD -- 'docs/decisions/*.md' | wc -l)
          if [ "$added" -eq 0 ]; then
            echo "::error::PR marked as requiring an ADR but no new docs/decisions/*.md added."
            exit 1
          fi
          echo "ADR check passed: $added new ADR(s) added."
```

**Why separate workflow vs ci.yml:** Plan 05 ships `ci.yml`（lint / typecheck / test / migration-drift）。adr-check 邏輯與 lint 流程正交、且只需讀 PR body（不需跑 bun），拆開 workflow file 讓 CI 並行 + 關注點分離。
  </action>
  <verify>
    <automated>test -f .github/PULL_REQUEST_TEMPLATE.md && test -f .github/workflows/adr-check.yml && grep -q 'ADR' .github/PULL_REQUEST_TEMPLATE.md && grep -q 'docs/decisions' .github/workflows/adr-check.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/PULL_REQUEST_TEMPLATE.md`
    - `grep -q '## ADR checkpoint' .github/PULL_REQUEST_TEMPLATE.md`
    - `grep -c '- \[ \]' .github/PULL_REQUEST_TEMPLATE.md` ≥ 7（ADR 3 + 索引更新 1 + Rigidity tier 3 + Testing 3 ≈ 10）
    - `grep -q 'docs/decisions/NNNN' .github/PULL_REQUEST_TEMPLATE.md`
    - `grep -q 'docs/decisions/README.md' .github/PULL_REQUEST_TEMPLATE.md`
    - `grep -q 'Tier 1' .github/PULL_REQUEST_TEMPLATE.md && grep -q 'Tier 2' .github/PULL_REQUEST_TEMPLATE.md && grep -q 'Tier 3' .github/PULL_REQUEST_TEMPLATE.md`
    - `grep -q 'bun run lint' .github/PULL_REQUEST_TEMPLATE.md && grep -q 'bun run typecheck' .github/PULL_REQUEST_TEMPLATE.md && grep -q 'bun run test' .github/PULL_REQUEST_TEMPLATE.md`
    - `test -f .github/workflows/adr-check.yml`
    - `grep -q 'name: ADR Check' .github/workflows/adr-check.yml`
    - `grep -q 'pull_request' .github/workflows/adr-check.yml`
    - `grep -q 'docs/decisions/\*.md' .github/workflows/adr-check.yml`
    - `grep -q 'diff-filter=A' .github/workflows/adr-check.yml`
    - `grep -q 'requiring an ADR' .github/workflows/adr-check.yml`
    - YAML 有效（executor 可用 `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/adr-check.yml"))' || yq .github/workflows/adr-check.yml > /dev/null` 驗；若無 python/yq 則由 CI 本身 parse 驗）
  </acceptance_criteria>
  <done>
    PR template 含 ADR checkbox + Rigidity tier + testing 三段；adr-check workflow 在 PR body 勾需 ADR 但無新 ADR 時 fail；兩檔為 05-PLAN 的 ci.yml 與未來 CI 操作奠基。
  </done>
</task>

<task type="auto">
  <name>Task 3: AGENTS.md 插入 RIGGING:rigidity-map + RIGGING:anti-features 自管理段落</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/AGENTS.md (現行內容；理解 GSD 自管理區塊 `<!-- GSD:*-start/end -->` 結構；推薦落點為 `<!-- GSD:workflow-end -->` 之後、`<!-- GSD:profile-start -->` 之前)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "AGENTS.md Rigidity Map 段落 draft" — 完整 RIGGING:rigidity-map-start ... anti-features-end 段落模板)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (D-01..D-03 — Rigidity Map 三級內容；D-04 — detection 落點)
    - /Users/carl/Dev/CMG/Rigging/.planning/PROJECT.md (Out of Scope — 對齊 anti-features 清單；12 項)
    - /Users/carl/Dev/CMG/Rigging/.planning/REQUIREMENTS.md (v2 requirements — 供 anti-features "v2 feature" 回應 template 交叉確認)
  </read_first>
  <files>
    - AGENTS.md
  </files>
  <action>
在 AGENTS.md 現有內容中插入兩段 RIGGING 自管理段落：

1. **定位**：在 `<!-- GSD:workflow-end -->` 之後、`<!-- GSD:profile-start -->` 之前。
2. **RIGGING 段落結構**：用 `<!-- RIGGING:<name>-start source:<canonical> -->` 與 `<!-- RIGGING:<name>-end -->` 包裹，便於未來 `/gsd-edit-region` 或類似工具保護。
3. **雙段落**：
   - `RIGGING:rigidity-map-start` (source: `docs/decisions/0009-rigidity-map.md`) → Tier 1 / Tier 2 / Tier 3 + Detection
   - `RIGGING:anti-features-start` (source: `.planning/PROJECT.md`) → 12 項 v2 out-of-scope 清單 + 回應 template

### 插入內容（精確抄寫自 RESEARCH.md §AGENTS.md Rigidity Map 段落 draft）

於 `<!-- GSD:workflow-end -->` 行之後、`<!-- GSD:profile-start -->` 行之前插入：

```markdown
<!-- RIGGING:rigidity-map-start source:docs/decisions/0009-rigidity-map.md -->
## Rigging Rigidity Map (AI Agent: read this first)

Rigging enforces a three-tier strictness model. Know the tier before proposing changes.

### Tier 1 — Must Be Rigid (NO escape hatch, NO exceptions)

1. **AuthContext is the only path to any Domain service**
   - No `new UserServiceImpl(...)` from outside the factory barrel.
   - `getXxxService(ctx: AuthContext)` factory is the only legal entry point.
   - Runtime guard throws `AuthContextMissingError` on null ctx (ADR 0006, 0007).

2. **Domain layer is framework-free**
   - `src/**/domain/**` MUST NOT import `drizzle-orm`, `elysia`, `better-auth`, `postgres`, `@bogeychan/elysia-logger`, `pino`.
   - Enforced by Biome `noRestrictedImports` (biome.json overrides).
   - Violation = CI fail, no review bypass.

3. **Core stack versions are pinned**
   - `bun@^1.3.12`, `elysia@^1.4.28`, `better-auth@1.6.5` (exact), `drizzle-orm@^0.45.2`, `postgres@^3.4.9`.
   - Upgrade requires a new ADR with `Supersedes:` referencing 0001 / 0002 / 0004 / 0005 / 0010.

### Tier 2 — Default Rigid, Escapable via ADR

Changes below are allowed — but require a new ADR that supersedes the relevant record:

- **Validator choice** (TypeBox vs Zod) — currently TypeBox (see research/STACK.md).
- **Postgres driver** — currently `postgres-js` (ADR 0010); revisit only when bun#21934 AND bun#22395 close.
- **Logger format / library** — currently Pino via `@bogeychan/elysia-logger`.
- **Migration strategy** — currently `drizzle-kit generate` + `migrate`; `push` local-only.
- **Resolver precedence** — currently API Key > cookie (ADR 0011); supersede to change.

### Tier 3 — Convention Only (no automated check)

Consistent-but-not-enforced. AI Agent should follow existing patterns, but no CI gate:

- Variable naming (camelCase for functions/vars, PascalCase for types/classes).
- Error code naming (SCREAMING_SNAKE_CASE).
- Log field naming (camelCase).
- Git commit format (follows repo history).
- Branch naming (follows repo history).

### Detection (where violations are caught)

- **CI (biome + tsc + bun test)** — Tier 1 rules 1 & 2; Tier 2 migration drift.
- **Runtime (`getXxxService` factory)** — Tier 1 rule 1 (AuthContextMissingError).
- **No pre-commit hook** — by design (ADR 0009, CONTEXT D-04). Keeps dev loop fast; CI is the authoritative gate.
<!-- RIGGING:rigidity-map-end -->

<!-- RIGGING:anti-features-start source:.planning/PROJECT.md -->
## Anti-Features (DO NOT propose extending)

AI Agent: if a user (or you) wants to add any of these, STOP. Rigging v1 explicitly excludes them. See `.planning/PROJECT.md` "Out of Scope" for full reasoning.

- **Frontend UI (React/Vue/SvelteKit/any client app)** — v1 is API-first. UI is delegated to downstream consumers.
- **`npx rigging` / `create-rigging` CLI generator** — v1 is Reference App; scaffold extraction is v2 (SCAF-01).
- **Real email providers (Resend/SMTP)** — v1 ships `ConsoleEmailAdapter` (logs to stdout). Swap adapter in v2 PROD-01.
- **OAuth / SSO / 2FA / Magic Link / Passkeys** — each new identity path is another resolver branch; violates "one rail" thesis. v2 IDN-*.
- **MCP server / A2A (Agent-to-Agent) / Multi-agent orchestration** — Rigging is "where agents write code", not "where agents run". v2 AGT-*.
- **OpenTelemetry / distributed tracing / Prometheus metrics** — v1 is community-grade (structured logs only). v2 PROD-03.
- **Multi-tenancy / Organization / RBAC** — next abstraction layer; v1 nails single-user AuthContext boundary. v2 TEN-*.
- **NPM package publishing (`@rigging/core` etc.)** — API still evolving. v2 SCAF-03.
- **WebSocket / SSE / real-time events** — domain feature addition, not harness core.
- **GraphQL API** — REST + Swagger demonstrates harness; GraphQL is presentation swap.
- **Docker image publishing (Hub / ghcr.io)** — v1 supplies compose for dev; prod deploy is user's choice.
- **Production-grade migration tooling (zero-downtime)** — left to deployment team.

If a user asks to add any of the above, respond with:
"That's a v2 feature (see `.planning/PROJECT.md` Out of Scope). v1 scope is locked; adding it requires a PROJECT.md amendment with reasoning for why now."
<!-- RIGGING:anti-features-end -->
```

**Important (per RESEARCH.md Pitfall #7):** 兩段落「canonical 源」分別為 `docs/decisions/0009-rigidity-map.md` 與 `.planning/PROJECT.md`；若未來 GSD 工具 regenerate AGENTS.md 覆蓋本段，canonical 源仍為事實依據。executor **不要**在 AGENTS.md 第一行加額外的 CAUTION 註解（Pitfall #7 提到但未必必要）——雙寫已足夠。

完成後驗證：
- 原有 GSD 自管理段落完全保留（`<!-- GSD:project-start -->` 到 `<!-- GSD:profile-end -->` 結構完整）
- 兩段 RIGGING 區塊於正確位置
- AGENTS.md grep 驗證條目

**自動 Wave-end gate：** `bun run lint && bun run typecheck && bun test`（本 plan 只改 markdown / yml，不影響三項）。
  </action>
  <verify>
    <automated>grep -q 'RIGGING:rigidity-map-start' AGENTS.md && grep -q 'RIGGING:rigidity-map-end' AGENTS.md && grep -q 'RIGGING:anti-features-start' AGENTS.md && grep -q 'RIGGING:anti-features-end' AGENTS.md && grep -q 'GSD:workflow-start' AGENTS.md && grep -q 'GSD:profile-start' AGENTS.md && grep -q 'Tier 1' AGENTS.md && grep -q 'Tier 2' AGENTS.md && grep -q 'Tier 3' AGENTS.md && grep -q "That's a v2 feature" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<!-- RIGGING:rigidity-map-start source:docs/decisions/0009-rigidity-map.md -->' AGENTS.md`
    - `grep -q '<!-- RIGGING:rigidity-map-end -->' AGENTS.md`
    - `grep -q '<!-- RIGGING:anti-features-start source:.planning/PROJECT.md -->' AGENTS.md`
    - `grep -q '<!-- RIGGING:anti-features-end -->' AGENTS.md`
    - 所有原 GSD 區塊保留：`grep -q 'GSD:project-start' AGENTS.md && grep -q 'GSD:stack-start' AGENTS.md && grep -q 'GSD:conventions-start' AGENTS.md && grep -q 'GSD:architecture-start' AGENTS.md && grep -q 'GSD:skills-start' AGENTS.md && grep -q 'GSD:workflow-start' AGENTS.md && grep -q 'GSD:profile-start' AGENTS.md`
    - RIGGING 段落位置正確（rigidity-map 在 workflow-end 之後、profile-start 之前）：`awk '/GSD:workflow-end/{w=NR} /RIGGING:rigidity-map-start/{r=NR} /GSD:profile-start/{p=NR} END{exit (w<r && r<p) ? 0 : 1}' AGENTS.md` exit 0
    - Tier 段全齊：`grep -q '### Tier 1 — Must Be Rigid' AGENTS.md && grep -q '### Tier 2 — Default Rigid, Escapable via ADR' AGENTS.md && grep -q '### Tier 3 — Convention Only' AGENTS.md`
    - Tier 1 三條：`grep -q 'AuthContext is the only path' AGENTS.md && grep -q 'Domain layer is framework-free' AGENTS.md && grep -q 'Core stack versions are pinned' AGENTS.md`
    - Tier 2 含 API Key precedence：`grep -q 'API Key > cookie' AGENTS.md`
    - Detection 段提 no pre-commit：`grep -q 'No pre-commit hook' AGENTS.md`
    - Anti-features 含 12 項（grep count 項目符號）：`grep -c '^- \*\*' AGENTS.md` ≥ 12（含 Tier 2 5 項 + anti 12 項 ≈ 17+）
    - v2 回應 template：`grep -q "That's a v2 feature" AGENTS.md`
    - 對齊 .planning/PROJECT.md Out of Scope 關鍵字：`grep -q 'Frontend UI' AGENTS.md && grep -q 'OAuth' AGENTS.md && grep -q 'Multi-tenancy' AGENTS.md && grep -q 'GraphQL' AGENTS.md`
    - ADR cross-reference：`grep -q 'ADR 0006' AGENTS.md && grep -q 'ADR 0007' AGENTS.md && grep -q 'ADR 0009' AGENTS.md && grep -q 'ADR 0010' AGENTS.md && grep -q 'ADR 0011' AGENTS.md`
    - Wave-end gate：`bun run lint && bun run typecheck && bun test` exit 0
  </acceptance_criteria>
  <done>
    AGENTS.md 保留原 GSD 區塊 + 新增 Rigidity Map（3 tier + Detection）+ Anti-features（12 項 + 回應 template）；ADR 0009 / PROJECT.md canonical 源雙寫；位置正確於 GSD:workflow-end 後、GSD:profile-start 前。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI | PR body 與 docs/decisions/ 檔案透過 adr-check workflow 驗證一致性 |
| GSD 工具 → AGENTS.md | GSD 可能 regenerate AGENTS.md 覆蓋 RIGGING:* 自管理段落（Pitfall #7）|
| ADR markdown → 讀者 | ADR 範例字串若含真實 secret 會洩漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | ADR 文字含真實 `BETTER_AUTH_SECRET` 範例 | mitigate | Task 1 acceptance `! grep -qE '[A-F0-9]{32,}' docs/decisions/*.md` heuristic；文中用 `<32-byte random hex, see .env.example>` 明示 placeholder；與 05-PLAN 的 .env.example 格式一致 |
| T-01-04-02 | Tampering | GSD 工具 regenerate AGENTS.md 覆蓋 RIGGING 段落（Pitfall #7）| mitigate | Double-source: rigidity-map 內容同步於 `docs/decisions/0009-rigidity-map.md`（canonical）；anti-features 同步於 `.planning/PROJECT.md` Out of Scope；若 AGENTS.md RIGGING 段被覆蓋可用 Task 3 內容重建 |
| T-01-04-03 | Tampering | PR author 勾 ADR checkbox 但繞過 adr-check（將檔案命名為非 `docs/decisions/` 路徑）| mitigate | adr-check workflow `git diff --diff-filter=A origin/${base}...HEAD -- 'docs/decisions/*.md'` 精確 path filter；非該路徑的檔案不算 |
| T-01-04-04 | Elevation of Privilege | ADR 被事後修改（推翻 accepted decision 不寫 supersede）| mitigate | `docs/decisions/README.md` Workflow 段明寫「Never edit an accepted ADR's Decision Outcome section」；PR reviewer 對該路徑的 modify diff 警覺 |
</threat_model>

<verification>
```bash
# ADR-01 / ADR-02 / ADR-03: 12 條 ADR ship
test $(ls docs/decisions/{0000,0001,0002,0003,0004,0005,0006,0007,0008}-*.md | wc -l) -eq 9
test $(ls docs/decisions/{0009,0010,0011}-*.md | wc -l) -eq 3

# ADR-01: MADR 4.0 format + YAML front matter
for f in docs/decisions/0*.md; do head -7 "$f" | grep -q "^status: accepted$" || exit 1; done

# ADR-04: README index 含 Status + Supersedes 欄
grep -q 'Status' docs/decisions/README.md && grep -q 'Supersedes' docs/decisions/README.md

# ADR-05: PR template 含 ADR checkbox
grep -q 'ADR' .github/PULL_REQUEST_TEMPLATE.md

# AGM-01: AGENTS.md 含 Rigidity Map 三級
grep -q 'RIGGING:rigidity-map-start' AGENTS.md && grep -q 'Tier 1' AGENTS.md && grep -q 'Tier 2' AGENTS.md && grep -q 'Tier 3' AGENTS.md

# AGM-02: AGENTS.md 含 anti-features
grep -q 'RIGGING:anti-features' AGENTS.md && grep -q 'DO NOT propose' AGENTS.md

# adr-check workflow YAML 有效
grep -q 'docs/decisions' .github/workflows/adr-check.yml && grep -q 'diff-filter=A' .github/workflows/adr-check.yml

# Wave 2 full gate
bun run lint && bun run typecheck && bun test
```
</verification>

<success_criteria>
1. **ADR-01** — `docs/decisions/` 就位、12 條皆 MADR 4.0 full-variant、檔名 `NNNN-kebab-case.md`、YAML front matter 統一 `status: accepted` + `date: 2026-04-19`
2. **ADR-02** — 9 條起始 ADR（0000-0008）ship；內容對齊 RESEARCH.md §ADR Seed Content 摘要
3. **ADR-03** — 3 追加 ADR ship；0009 三 tier 清楚、0010 revisit 條件 `bun#21934 AND bun#22395`、0011 P1 accepted + P3 spike 計畫
4. **ADR-04** — README 索引 12 列、欄位 `編號 / 標題 / Status / 日期 / Supersedes`（per D-15）、含 Workflow lifecycle 段
5. **ADR-05** — PR template 含 ADR checkbox 三選一 + 索引更新副條件 + Rigidity tier selection
6. **AGM-01** — AGENTS.md 含 `<!-- RIGGING:rigidity-map-start -->` 段、三 tier 全列、Detection 段對齊 D-04（no pre-commit）
7. **AGM-02** — AGENTS.md 含 `<!-- RIGGING:anti-features-start -->` 段、12 項 out-of-scope、`That's a v2 feature` 回應 template
8. adr-check workflow 就位，YAML 有效
9. 原 GSD 區塊 100% 保留
10. `bun run lint && bun run typecheck && bun test` 全綠
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` summarising:
- 12 ADR files shipped + index
- YAML front matter convention（stable across all 12）
- 0009 Tier 1/2/3 內容與 CONTEXT.md §D-01..D-03 對應表
- 0010 revisit condition 精確字串位置
- 0011 P3 spike plan 精確字串位置
- PR template structure 與 D-16 對齊點
- adr-check workflow trigger/check 邏輯
- AGENTS.md 插入位置（line range）
- Hand-off: biome.json (Plan 03) rule messages 引用的 ADR 檔名全部實際存在 + 檔名一字不差
- Hand-off to Plan 05: adr-check.yml 獨立存在，ci.yml（lint/typecheck/test/migration-drift）由 Plan 05 ship
</output>
