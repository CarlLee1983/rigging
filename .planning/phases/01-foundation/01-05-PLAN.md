---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-foundation/01", "01-foundation/02", "01-foundation/03", "01-foundation/04"]
files_modified:
  - package.json
  - src/bootstrap/config.ts
  - src/main.ts
  - tests/unit/bootstrap/config.test.ts
  - docker-compose.yml
  - .env.example
  - drizzle.config.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04]

must_haves:
  truths:
    - "`docker-compose up -d postgres` 啟動 PostgreSQL 16-alpine 並於 10 秒內 healthcheck 通過"
    - "`.env.example` 含 6 個必要環境變數：DATABASE_URL / BETTER_AUTH_SECRET / BETTER_AUTH_URL / PORT / NODE_ENV / LOG_LEVEL，且 BETTER_AUTH_SECRET 為 placeholder 非真實值"
    - "`src/bootstrap/config.ts` 使用 TypeBox schema 驗證 env；缺值立即 throw '"Invalid environment variables"' 並 process exit 非 0"
    - "`src/main.ts` 啟動時呼叫 `loadConfig()`；env 缺值時 `bun run start` 立即失敗"
    - "`drizzle.config.ts` 使用 `dialect: 'postgresql'` + `dbCredentials.url: process.env.DATABASE_URL`；schema glob 指向 `src/**/infrastructure/schema/*.ts`；**不**引用 `drizzle-orm/bun-sql`"
    - "`.github/workflows/ci.yml` 於 PR 跑 `bun install --frozen-lockfile` → `bun run lint` → `bun run typecheck` → `bun run test`；使用 oven-sh/setup-bun@v2 with bun-version: 1.3.12"
    - "clean checkout 下執行 `bun install && bun run lint && bun run typecheck && bun run test` 皆 exit 0"
  artifacts:
    - path: "src/bootstrap/config.ts"
      provides: "TypeBox env schema + loadConfig() 啟動即驗"
      contains: "Value.Errors"
    - path: "docker-compose.yml"
      provides: "postgres:16-alpine + healthcheck + named volume"
      contains: "postgres:16-alpine"
    - path: ".env.example"
      provides: "6 項 env var + dev-only password 標註"
      contains: "DATABASE_URL="
    - path: "drizzle.config.ts"
      provides: "postgresql dialect 配置"
      contains: "dialect: 'postgresql'"
    - path: ".github/workflows/ci.yml"
      provides: "PR CI: lint + typecheck + test; clean Bun env"
      contains: "oven-sh/setup-bun"
  key_links:
    - from: "src/main.ts"
      to: "src/bootstrap/config.ts"
      via: "import loadConfig"
      pattern: "from '@/bootstrap/config'|from './bootstrap/config'"
    - from: ".env.example"
      to: "src/bootstrap/config.ts ConfigSchema"
      via: "欄位 1:1 對應"
      pattern: "DATABASE_URL"
    - from: "drizzle.config.ts"
      to: "process.env.DATABASE_URL"
      via: "dbCredentials.url"
      pattern: "process.env.DATABASE_URL"
---

<objective>
Phase 1 Plan 05 — **Env + Docker + Drizzle Config + CI**：最後一 plan，把 Foundation 從「可 lint / test」推到「可 `bun install && docker-compose up -d && bun run dev` 起 process」。交付 `docker-compose.yml`（postgres:16-alpine）、`.env.example`（6 欄 env vars，dev-only secrets 明示）、`src/bootstrap/config.ts`（TypeBox env schema 啟動即驗）、`drizzle.config.ts`（postgres-js-ready，per ADR 0010）、`.github/workflows/ci.yml`（lint + typecheck + test CI gate）。這是 FND-01..FND-04 的落地。

Purpose: 讓 Phase 1 Success Criteria 第 1 條（`bun install && docker-compose up -d && bun run dev` 10 分鐘內 process 啟動 + Postgres 可連線）成立；讓 CI 在 PR 上跑 lint/typecheck/test 形成 D-04 CI 檢測落點；讓 Phase 2 的 `src/shared/infrastructure/db/client.ts` 可直接消費 `loadConfig().DATABASE_URL`；讓 Phase 3 的 BetterAuth schema generate 有 drizzle.config.ts 可用。
Output: 1 個 docker-compose、1 個 .env.example、1 個 drizzle config、1 個 config.ts + 對應 unit test、1 個 main.ts 升級（loadConfig 取代 stub）、1 個 CI workflow、package.json 加 db:* / elysia 等 P1 尚未安裝但後續需要的 **最小 P1 dep set**。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<rationale>
- `RESEARCH.md §docker-compose + env schema` 提供三份完整草稿（docker-compose.yml / .env.example / src/bootstrap/config.ts）；executor 移植。
- `RESEARCH.md §Open Questions §1` + §Pitfall #4 警告 `drizzle.config.ts` 的 `driver` 欄位於 drizzle-kit 0.31.10 行為不確定；**本 plan 採「省略 driver 欄位，僅留 dialect + dbCredentials.url」**以避開 [ASSUMED A1] 風險——這是 Pitfall #4 的建議 mitigation。Phase 2 若需 driver 再補 ADR 0005a。
- `RESEARCH.md §Pitfall #2` 警告 `process.env.PORT` coerce：TypeBox 前手動 `Number(process.env.PORT)`；config.ts 草稿已處理。
- `RESEARCH.md §Pitfall #8` 警告 `.env.example` 與 schema drift；本 plan 的 unit test（`tests/unit/bootstrap/config.test.ts`）含契約驗證：parse `.env.example` 的 keys 對比 ConfigSchema properties。
- `RESEARCH.md §CI + Runtime Guard §D §ci.yml` 提供 CI workflow 草稿（oven-sh/setup-bun@v2 + lint + typecheck + test）。
- `RESEARCH.md §Bootstrap Ordering §8-§13` 確認本 plan 在 kernel / biome rules / ADRs 皆就位後落地。
- `RESEARCH.md §Environment Availability` 提供 probe protocol（Bun / Docker / git 版本）—— CI 用固定 `bun-version: 1.3.12` 即可，dev 環境文件於 README（Plan 01 已有最小版）。
- **安裝 elysia / drizzle-orm / better-auth / postgres deps**：CONTEXT.md §D-01 要求 core stack 版本 pin，且 AGENTS.md Rigidity Map Tier 1 rule 3 已聲明。本 plan 安裝完整 P1 dep set（包括 Phase 2-3 才用的 deps），讓 `bun.lockb` 一次鎖定。這與 RESEARCH.md §Standard Stack §Installation 的「P1 一次到位」策略一致。
</rationale>

<interfaces>
<!-- Contracts this plan creates, consumed downstream -->

```typescript
// src/bootstrap/config.ts — loadConfig() is called from src/main.ts and (Phase 2+) from bootstrap/app.ts
export type Config = {
  DATABASE_URL: string
  BETTER_AUTH_SECRET: string       // minLength 32
  BETTER_AUTH_URL: string          // URI format
  PORT: number                     // 1-65535
  NODE_ENV: 'development' | 'production' | 'test'
  LOG_LEVEL: 'debug' | 'info' | 'warn' | 'error'
}
export function loadConfig(): Config  // throws 'Invalid environment variables' with per-field summary on any missing/malformed field
```

```yaml
# docker-compose.yml — services.postgres image: postgres:16-alpine, port 5432, healthcheck pg_isready
# .env.example — 6 keys; DATABASE_URL defaults to postgresql://rigging:rigging_dev_password@localhost:5432/rigging
```

```typescript
// drizzle.config.ts
// dialect: 'postgresql', schema: './src/**/infrastructure/schema/*.ts', out: './drizzle'
// dbCredentials.url: process.env.DATABASE_URL ?? ''
// driver field deliberately omitted (Pitfall #4 mitigation; Phase 2+ may add ADR 0005a if needed)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 擴充 package.json deps + 安裝 elysia/drizzle/postgres/betterauth 與 db:* scripts</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/package.json (01-PLAN 產出的 minimum devDeps；本 Task 擴充)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (sections: "Standard Stack §Installation"、"Code Examples §package.json scripts")
    - /Users/carl/Dev/CMG/Rigging/.planning/research/STACK.md (版本矩陣 + Version Compatibility Matrix)
    - /Users/carl/Dev/CMG/Rigging/AGENTS.md (RIGGING Rigidity Map Tier 1 rule 3 — core stack version pin)
  </read_first>
  <files>
    - package.json
  </files>
  <action>
於現有 package.json 基礎上（01-PLAN 已裝：@biomejs/biome@^2.4.12 / @sinclair/typebox@^0.34.49 / @types/bun / typescript@^5.9）：

### 1. dependencies 新增（核心 stack，per RESEARCH.md §Installation）

執行命令：
```bash
bun add elysia@^1.4.28 \
        @elysiajs/cors@^1.4 \
        @elysiajs/swagger@^1.4 \
        @bogeychan/elysia-logger@^0.1.10 \
        pino@^10.3.1 \
        drizzle-orm@^0.45.2 \
        postgres@^3.4.9 \
        better-auth@1.6.5 \
        @better-auth/drizzle-adapter@1.6.5
```

**`better-auth@1.6.5` 必須 exact pin 無 caret**（per D-01、ADR 0004）。`bun add` 預設加 caret，executor 必須在完成後檢查 package.json，將 `"better-auth": "^1.6.5"` 改為 `"better-auth": "1.6.5"`，`@better-auth/drizzle-adapter` 同改為 exact `1.6.5`。

### 2. devDependencies 新增

```bash
bun add -d drizzle-kit@^0.31.10 pino-pretty@^13.1.3
```

### 3. scripts 新增（db:*）

修改 `package.json` scripts 段落增加：

```json
"db:generate": "drizzle-kit generate",
"db:migrate": "drizzle-kit migrate",
"db:push": "drizzle-kit push",
"db:studio": "drizzle-kit studio"
```

### 4. lockfile commit

`bun install` 會自動更新 `bun.lockb`；必須 commit。

### 5. 驗證 ARCH-04（無 IoC）不被 regress

執行 `bun install` 後立即 `! grep -qE '"(tsyringe|inversify|reflect-metadata)"' package.json`，確保無誤加 IoC 套件。
  </action>
  <verify>
    <automated>bun install --frozen-lockfile && grep -q '"elysia": "\^1.4.28"' package.json && grep -q '"better-auth": "1.6.5"' package.json && grep -q '"@better-auth/drizzle-adapter": "1.6.5"' package.json && grep -q '"drizzle-orm": "\^0.45.2"' package.json && grep -q '"postgres": "\^3.4.9"' package.json && grep -q '"drizzle-kit": "\^0.31.10"' package.json && grep -q '"db:generate"' package.json && ! grep -qE '"(tsyringe\|inversify\|reflect-metadata)"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"elysia": "\^1.4.28"' package.json`
    - `grep -q '"@elysiajs/cors"' package.json && grep -q '"@elysiajs/swagger"' package.json && grep -q '"@bogeychan/elysia-logger"' package.json`
    - `grep -q '"pino": "\^10.3.1"' package.json`
    - `grep -q '"drizzle-orm": "\^0.45.2"' package.json`
    - `grep -q '"postgres": "\^3.4.9"' package.json`
    - `grep -q '"better-auth": "1.6.5"' package.json`（**exact，無 caret**）
    - `grep -q '"@better-auth/drizzle-adapter": "1.6.5"' package.json`（**exact**）
    - `grep -q '"drizzle-kit": "\^0.31.10"' package.json`
    - `grep -q '"pino-pretty": "\^13.1.3"' package.json`
    - `grep -q '"db:generate": "drizzle-kit generate"' package.json`
    - `grep -q '"db:migrate": "drizzle-kit migrate"' package.json`
    - `grep -q '"db:push": "drizzle-kit push"' package.json`
    - `grep -q '"db:studio": "drizzle-kit studio"' package.json`
    - `test -f bun.lockb && test -s bun.lockb`（lockfile 存在且非空）
    - `! grep -qE '"(tsyringe\|inversify\|reflect-metadata)"' package.json`（ARCH-04 regression guard）
    - `bun install --frozen-lockfile` exit 0
    - `bun run lint` exit 0（新增 dep 不影響現有 lint）
    - `bun run typecheck` exit 0
  </acceptance_criteria>
  <done>
    P1 完整 dep set 安裝完成；better-auth exact pin 驗證；db:* scripts 就位；lockfile commit；ARCH-04 不回歸。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立 docker-compose.yml + .env.example + drizzle.config.ts</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (sections: "docker-compose + env schema" — docker-compose.yml 草稿 + .env.example 草稿 + drizzle.config.ts 草稿 + §⚠️ Important clarification; "Pitfall 4 — driver 欄位")
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (docker-compose / env schema 決策點在 Claude's Discretion — 可採 RESEARCH 建議)
    - /Users/carl/Dev/CMG/Rigging/.gitignore (01-PLAN 產出；確認 .env* 被忽略但 .env.example 例外)
    - /Users/carl/Dev/CMG/Rigging/docs/decisions/0010-postgres-driver-postgres-js.md (04-PLAN 產出；drizzle.config.ts 註解須引用本 ADR)
  </read_first>
  <files>
    - docker-compose.yml
    - .env.example
    - drizzle.config.ts
  </files>
  <action>
### 1. `docker-compose.yml`（per RESEARCH.md §docker-compose + env schema）

精確抄寫：

```yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: rigging-postgres
    environment:
      POSTGRES_USER: rigging
      POSTGRES_PASSWORD: rigging_dev_password
      POSTGRES_DB: rigging
    ports:
      - '5432:5432'
    volumes:
      - rigging-pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U rigging -d rigging']
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  # Optional: Adminer for quick DB browsing (uncomment to enable)
  # adminer:
  #   image: adminer:latest
  #   container_name: rigging-adminer
  #   ports:
  #     - '8080:8080'
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  rigging-pg-data:
    driver: local
```

**Security note**: `rigging_dev_password` 明示 dev-only；README.md 與 .env.example 皆須提醒「不要用於 production」（per threat model T-01-05-01）。

### 2. `.env.example`（per RESEARCH.md §.env.example 草稿）

精確抄寫 + 增強 secret hygiene 註解：

```bash
# Rigging — local development environment variables.
# Copy this file to .env.local and fill in real values.
# DO NOT commit .env.local. The `.gitignore` keeps all .env* except .env.example.

# Database (matches docker-compose.yml dev credentials — dev-only, DO NOT reuse in production)
DATABASE_URL=postgresql://rigging:rigging_dev_password@localhost:5432/rigging

# BetterAuth (Phase 3 will consume; P1 validates presence at startup)
# Generate with: openssl rand -hex 32  (or: bun eval 'console.log(crypto.randomUUID().replaceAll("-","") + crypto.randomUUID().replaceAll("-",""))')
# Must be at least 32 characters. PLACEHOLDER below — replace before running.
BETTER_AUTH_SECRET=change-me-to-32-byte-random-hex-before-committing
BETTER_AUTH_URL=http://localhost:3000

# HTTP server
PORT=3000

# Runtime mode: development | production | test
NODE_ENV=development

# Log level: debug | info | warn | error
LOG_LEVEL=debug
```

### 3. `drizzle.config.ts`（per RESEARCH.md §drizzle.config.ts 草稿 + §Important clarification + Pitfall #4）

**採 Pitfall #4 推薦策略：省略 `driver` 欄位，保留 `dialect: 'postgresql'` + `dbCredentials.url`**。App runtime driver 選擇（`drizzle-orm/postgres-js`）在 Phase 2 的 `src/shared/infrastructure/db/client.ts` 明示。

```typescript
import { defineConfig } from 'drizzle-kit'

// Per ADR 0010 (docs/decisions/0010-postgres-driver-postgres-js.md):
//   - App runtime driver is 'postgres-js' (via `drizzle-orm/postgres-js`), chosen to avoid
//     bun#21934 / bun#22395 transaction hang bugs in `drizzle-orm/bun-sql`.
//   - App runtime wiring lives in src/shared/infrastructure/db/client.ts (Phase 2 ships this).
//
// This file configures drizzle-kit CLI only. We intentionally OMIT the `driver` field
// per CONTEXT 01-RESEARCH.md Pitfall #4 mitigation — drizzle-kit 0.31.x infers the
// driver for 'postgresql' dialect from `dbCredentials.url`. If this proves wrong at
// Phase 2/3 migration time, write ADR 0005a / 0010a to add the field with the
// correct value; do not silently edit here.
//
// Schema glob: feature slices own their schema under {feature}/infrastructure/schema/.
// Phase 1 ships no schema files; Phase 2 + 3 will begin populating.

export default defineConfig({
  dialect: 'postgresql',
  schema: './src/**/infrastructure/schema/*.ts',
  out: './drizzle',
  dbCredentials: {
    url: process.env.DATABASE_URL ?? '',
  },
  verbose: true,
  strict: true,
})
```

完成後驗證：
- `bun run lint && bun run typecheck` 仍 exit 0（drizzle.config.ts 會被 biome lint 涵蓋；tsc strict 須通過）。
- `bunx drizzle-kit --help` 應 exit 0（config 可被 drizzle-kit load；若 drizzle-kit 抱怨缺 driver 則 RESEARCH A1 假設錯誤，須 fallback：加 `driver: 'pglite'` 或 `'pg'`——但先試省略）。
  </action>
  <verify>
    <automated>test -f docker-compose.yml && test -f .env.example && test -f drizzle.config.ts && grep -q 'postgres:16-alpine' docker-compose.yml && grep -q 'pg_isready' docker-compose.yml && grep -q 'DATABASE_URL=postgresql://' .env.example && grep -q "dialect: 'postgresql'" drizzle.config.ts && ! grep -q 'bun-sql' drizzle.config.ts && bun run lint drizzle.config.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docker-compose.yml`
    - `grep -q 'postgres:16-alpine' docker-compose.yml`（FND-03 image 鎖定 per STACK.md）
    - `grep -q 'pg_isready -U rigging -d rigging' docker-compose.yml`（healthcheck 正確）
    - `grep -q 'rigging-pg-data' docker-compose.yml`（named volume）
    - `grep -q "container_name: rigging-postgres" docker-compose.yml`
    - `grep -q "5432:5432" docker-compose.yml`（port mapping）
    - `test -f .env.example`
    - `grep -q '^DATABASE_URL=postgresql://rigging:rigging_dev_password@localhost:5432/rigging$' .env.example`
    - `grep -q '^BETTER_AUTH_SECRET=change-me' .env.example`（placeholder；非真 secret）
    - `grep -q '^BETTER_AUTH_URL=http://localhost:3000$' .env.example`
    - `grep -q '^PORT=3000$' .env.example`
    - `grep -q '^NODE_ENV=development$' .env.example`
    - `grep -q '^LOG_LEVEL=debug$' .env.example`
    - `grep -q 'DO NOT reuse in production\|dev-only' .env.example`（security hint）
    - `test -f drizzle.config.ts`
    - `grep -q "dialect: 'postgresql'" drizzle.config.ts`
    - `grep -q 'process.env.DATABASE_URL' drizzle.config.ts`
    - `grep -q 'src/\*\*/infrastructure/schema' drizzle.config.ts`
    - `grep -q '0010-postgres-driver-postgres-js.md' drizzle.config.ts`（ADR 0010 cross-reference）
    - `! grep -q 'bun-sql' drizzle.config.ts`（ADR 0010 硬性：NOT bun:sql）
    - `! grep -qE "driver: '(bun-sql|bun:sql)'" drizzle.config.ts`
    - `.gitignore` 未被 regress：`grep -q '!.env.example' .gitignore`
    - `bun run lint` exit 0
    - `bun run typecheck` exit 0
  </acceptance_criteria>
  <done>
    docker-compose（postgres 16-alpine + healthcheck + named volume）+ .env.example（6 欄，dev 標註清楚）+ drizzle.config.ts（postgresql dialect，省 driver 欄位 per Pitfall #4）皆就位；lint + typecheck 通過。
  </done>
</task>

<task type="auto">
  <name>Task 3: 建立 src/bootstrap/config.ts + config.test.ts；升級 src/main.ts 取代 stub</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/src/main.ts (01-PLAN 產出 stub；本 Task 升級)
    - /Users/carl/Dev/CMG/Rigging/.env.example (Task 2 產出；config.ts schema 必須 1:1 對應)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (sections: "src/bootstrap/config.ts 草稿" + "Code Examples §src/main.ts (P1 stub)" + "Pitfall 2 — PORT coerce" + "Pitfall 8 — drift contract test")
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements — tests/unit/bootstrap/config.test.ts)
    - /Users/carl/Dev/CMG/Rigging/tsconfig.json (paths alias `@/*` 指向 src/*)
  </read_first>
  <files>
    - src/bootstrap/config.ts
    - src/main.ts
    - tests/unit/bootstrap/config.test.ts
  </files>
  <action>
### 1. `src/bootstrap/config.ts`（per RESEARCH.md §src/bootstrap/config.ts 草稿）

精確抄寫，**注意 PORT coerce 於 TypeBox validate 前**（per Pitfall #2）：

```typescript
import { Type, type Static } from '@sinclair/typebox'
import { Value } from '@sinclair/typebox/value'

// Startup env schema — fail fast if any required var missing / malformed.
// TypeBox is the single validator (Elysia peerDep; no Zod layered on top).
// See docs/decisions/0003-ddd-layering.md — config lives in bootstrap/, not in any feature.
// See docs/decisions/0004-auth-betterauth.md — BETTER_AUTH_SECRET must be ≥32 chars.

const ConfigSchema = Type.Object({
  DATABASE_URL: Type.String({ pattern: '^postgresql://.+' }),
  BETTER_AUTH_SECRET: Type.String({ minLength: 32 }),
  BETTER_AUTH_URL: Type.String({ format: 'uri' }),
  PORT: Type.Integer({ minimum: 1, maximum: 65535 }),
  NODE_ENV: Type.Union([
    Type.Literal('development'),
    Type.Literal('production'),
    Type.Literal('test'),
  ]),
  LOG_LEVEL: Type.Union([
    Type.Literal('debug'),
    Type.Literal('info'),
    Type.Literal('warn'),
    Type.Literal('error'),
  ]),
})

export type Config = Static<typeof ConfigSchema>

// Bun auto-loads .env / .env.local / .env.development etc. — process.env is populated.
// Pitfall #2: env vars are strings; coerce numeric fields BEFORE validating.
function rawEnv() {
  return {
    ...process.env,
    PORT: process.env.PORT ? Number(process.env.PORT) : undefined,
  }
}

export function loadConfig(): Config {
  const source = rawEnv()
  const errors = [...Value.Errors(ConfigSchema, source)]
  if (errors.length > 0) {
    const summary = errors
      .map((e) => `  - ${e.path}: ${e.message} (got: ${JSON.stringify(e.value)})`)
      .join('\n')
    throw new Error(
      `Invalid environment variables. See .env.example for required fields.\n${summary}`,
    )
  }
  return Value.Decode(ConfigSchema, source)
}
```

### 2. `src/main.ts` 升級（取代 01-PLAN stub）

```typescript
import { loadConfig } from './bootstrap/config'

// Phase 1: validate env on startup (FND-02) and exit.
// Phase 2 replaces this with Elysia app.listen().

const config = loadConfig()
console.log('[rigging] P1 foundation ready.')
console.log(`[rigging] env loaded: NODE_ENV=${config.NODE_ENV}, PORT=${config.PORT}`)
console.log('[rigging] Next: Phase 2 will mount Elysia root app.')
```

### 3. `tests/unit/bootstrap/config.test.ts`（FND-02 驗證 + Pitfall #8 drift test）

```typescript
import { afterEach, beforeEach, describe, expect, test } from 'bun:test'
import { readFileSync } from 'node:fs'
import { loadConfig } from '@/bootstrap/config'

const VALID_ENV = {
  DATABASE_URL: 'postgresql://u:p@localhost:5432/db',
  BETTER_AUTH_SECRET: 'x'.repeat(32),
  BETTER_AUTH_URL: 'http://localhost:3000',
  PORT: '3000',
  NODE_ENV: 'test',
  LOG_LEVEL: 'info',
}

function withEnv(patch: Record<string, string | undefined>, fn: () => void) {
  const ORIG_KEYS = Object.keys(VALID_ENV)
  const saved: Record<string, string | undefined> = {}
  for (const k of [...ORIG_KEYS, ...Object.keys(patch)]) {
    saved[k] = process.env[k]
  }
  try {
    for (const k of ORIG_KEYS) process.env[k] = VALID_ENV[k as keyof typeof VALID_ENV]
    for (const [k, v] of Object.entries(patch)) {
      if (v === undefined) delete process.env[k]
      else process.env[k] = v
    }
    fn()
  } finally {
    for (const [k, v] of Object.entries(saved)) {
      if (v === undefined) delete process.env[k]
      else process.env[k] = v
    }
  }
}

describe('loadConfig: happy path (FND-02)', () => {
  test('returns typed config when all env vars present and valid', () => {
    withEnv({}, () => {
      const c = loadConfig()
      expect(c.DATABASE_URL).toBe(VALID_ENV.DATABASE_URL)
      expect(c.PORT).toBe(3000)
      expect(c.NODE_ENV).toBe('test')
    })
  })
})

describe('loadConfig: fail-fast on missing / invalid env (FND-02)', () => {
  test('missing BETTER_AUTH_SECRET throws', () => {
    withEnv({ BETTER_AUTH_SECRET: undefined }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })

  test('BETTER_AUTH_SECRET shorter than 32 chars throws', () => {
    withEnv({ BETTER_AUTH_SECRET: 'too-short' }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })

  test('DATABASE_URL without postgresql:// prefix throws', () => {
    withEnv({ DATABASE_URL: 'mysql://bad' }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })

  test('invalid PORT (non-numeric) throws', () => {
    withEnv({ PORT: 'nope' }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })

  test('out-of-range PORT throws', () => {
    withEnv({ PORT: '70000' }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })

  test('invalid NODE_ENV throws', () => {
    withEnv({ NODE_ENV: 'staging' }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })

  test('invalid LOG_LEVEL throws', () => {
    withEnv({ LOG_LEVEL: 'verbose' }, () => {
      expect(() => loadConfig()).toThrow(/Invalid environment variables/)
    })
  })
})

describe('Config contract: schema keys match .env.example keys (Pitfall #8 drift guard)', () => {
  test('.env.example declares exactly the keys ConfigSchema expects', () => {
    const envText = readFileSync('.env.example', 'utf8')
    const exampleKeys = new Set<string>()
    for (const line of envText.split('\n')) {
      const trimmed = line.trim()
      if (!trimmed || trimmed.startsWith('#')) continue
      const m = trimmed.match(/^([A-Z][A-Z0-9_]*)=/)
      if (m) exampleKeys.add(m[1]!)
    }

    const schemaKeys = new Set([
      'DATABASE_URL',
      'BETTER_AUTH_SECRET',
      'BETTER_AUTH_URL',
      'PORT',
      'NODE_ENV',
      'LOG_LEVEL',
    ])

    // Symmetric diff must be empty
    for (const k of schemaKeys) {
      expect(exampleKeys.has(k)).toBe(true)
    }
    for (const k of exampleKeys) {
      expect(schemaKeys.has(k)).toBe(true)
    }
  })
})
```

完成後執行：
- `bun test tests/unit/bootstrap/` exit 0
- `bun run lint && bun run typecheck && bun test` 全 exit 0
- Smoke：`BETTER_AUTH_SECRET= timeout 5 bun src/main.ts 2>&1 | grep -q 'Invalid environment variables'`（FND-02 fail-fast 人工驗證）——加到 README.md 或 Task SUMMARY 作記錄
- Happy smoke：確保 executor 的 dev 環境有 `.env.local`（內容從 .env.example 複製並將 `BETTER_AUTH_SECRET` 改為 32+ char），跑 `timeout 5 bun src/main.ts` 應印 `[rigging] P1 foundation ready.`
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun test tests/unit/bootstrap/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/bootstrap/config.ts`
    - `grep -q 'ConfigSchema' src/bootstrap/config.ts`
    - `grep -q 'export function loadConfig' src/bootstrap/config.ts`
    - `grep -q 'Value.Errors' src/bootstrap/config.ts`
    - `grep -q 'Value.Decode' src/bootstrap/config.ts`
    - `grep -q 'Invalid environment variables' src/bootstrap/config.ts`
    - `grep -q "pattern: '\^postgresql://" src/bootstrap/config.ts`
    - `grep -q 'minLength: 32' src/bootstrap/config.ts`
    - `grep -q "format: 'uri'" src/bootstrap/config.ts`
    - `grep -q 'minimum: 1' src/bootstrap/config.ts && grep -q 'maximum: 65535' src/bootstrap/config.ts`
    - `grep -q 'Number(process.env.PORT)' src/bootstrap/config.ts`（Pitfall #2 coerce）
    - `test -f src/main.ts`
    - `grep -q "from './bootstrap/config'" src/main.ts`
    - `grep -q 'loadConfig()' src/main.ts`
    - `grep -q 'P1 foundation ready' src/main.ts`
    - `test -f tests/unit/bootstrap/config.test.ts`
    - `grep -q "from '@/bootstrap/config'" tests/unit/bootstrap/config.test.ts`
    - `grep -q 'Pitfall #8' tests/unit/bootstrap/config.test.ts || grep -q 'drift' tests/unit/bootstrap/config.test.ts`（drift guard test）
    - `bun test tests/unit/bootstrap/config.test.ts 2>&1 | grep -cE '\(pass\)|PASS'` ≥ 8（1 happy + 7 fail-fast + 1 drift ≈ 9）
    - `bun test tests/unit/bootstrap/config.test.ts` exit 0
    - `bun run lint && bun run typecheck && bun test` exit 0
  </acceptance_criteria>
  <done>
    config.ts TypeBox schema + loadConfig fail-fast + main.ts upgrade + drift guard test 就位；所有 7 種 malformed env + 1 happy + 1 drift 案例通過；FND-02 實現。
  </done>
</task>

<task type="auto">
  <name>Task 4: 建立 .github/workflows/ci.yml（lint + typecheck + test gate）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-RESEARCH.md (section: "CI + Runtime Guard §D §ci.yml" — 完整 CI workflow 草稿)
    - /Users/carl/Dev/CMG/Rigging/.github/workflows/adr-check.yml (04-PLAN 產出；確認不衝突；兩 workflow 並存)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/01-foundation/01-CONTEXT.md (D-04 — CI 檢測落點)
    - /Users/carl/Dev/CMG/Rigging/package.json (Task 1 產出；CI 會呼叫 bun run lint/typecheck/test)
  </read_first>
  <files>
    - .github/workflows/ci.yml
  </files>
  <action>
建立 `.github/workflows/ci.yml`（D-04 CI 檢測落點；Phase 5 會追加 migration drift check，本 plan 先 ship 最小版 lint + typecheck + test）：

```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.12
      - name: Install dependencies (frozen)
        run: bun install --frozen-lockfile
      - name: Lint (biome check .)
        run: bun run lint
      - name: Typecheck (tsc --noEmit)
        run: bun run typecheck
      - name: Test (bun test)
        run: bun run test
```

**Why 不包 migration drift check:** Phase 5 CI phase 會加 `drizzle-kit generate --name=ci-drift` + fail-if-new-file 步驟（per REQUIREMENTS CI-02）。P1 repo 尚無 schema 檔案，drift check 無意義且會 error（`drizzle-kit generate` 面對空 schema 行為不穩）。

**Why Bun 版本硬編碼 `1.3.12`:** per D-01 + ADR 0001 pin；與 AGENTS.md Tier 1 rule 3 一致。

**Why CI 不跑 docker-compose:** FND-03（Postgres healthcheck）歸類為 manual verification（VALIDATION.md §Manual-Only Verifications），因 CI runner 上 docker-compose 延遲 + 與 bun:test 相依性不值得；Phase 5 integration tests 會用 testcontainers 取代。

驗證 YAML 有效性：若 executor 環境有 `yq`（Homebrew）：`yq '.' .github/workflows/ci.yml > /dev/null`；否則 GitHub 本身會 parse。
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'name: CI' .github/workflows/ci.yml && grep -q 'oven-sh/setup-bun@v2' .github/workflows/ci.yml && grep -q 'bun-version: 1.3.12' .github/workflows/ci.yml && grep -q 'bun install --frozen-lockfile' .github/workflows/ci.yml && grep -q 'bun run lint' .github/workflows/ci.yml && grep -q 'bun run typecheck' .github/workflows/ci.yml && grep -q 'bun run test' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml`
    - `grep -q 'name: CI' .github/workflows/ci.yml`
    - `grep -q 'pull_request' .github/workflows/ci.yml`
    - `grep -q 'branches: \[main\]' .github/workflows/ci.yml`
    - `grep -q 'actions/checkout@v4' .github/workflows/ci.yml`
    - `grep -q 'oven-sh/setup-bun@v2' .github/workflows/ci.yml`
    - `grep -q 'bun-version: 1.3.12' .github/workflows/ci.yml`（D-01 版本 pin）
    - `grep -q 'bun install --frozen-lockfile' .github/workflows/ci.yml`
    - `grep -q 'bun run lint' .github/workflows/ci.yml`
    - `grep -q 'bun run typecheck' .github/workflows/ci.yml`
    - `grep -q 'bun run test' .github/workflows/ci.yml`
    - `test -f .github/workflows/adr-check.yml`（04-PLAN 產出仍存在）
    - Phase-end gate：`bun install --frozen-lockfile && bun run lint && bun run typecheck && bun run test` exit 0
    - Smoke Docker（manual OK）：`docker compose config 2>/dev/null | grep -q 'postgres:16-alpine' || docker-compose config | grep -q 'postgres:16-alpine'`（驗 compose file 結構有效）
  </acceptance_criteria>
  <done>
    ci.yml 就位並含 lint/typecheck/test 三項 + clean Bun env + frozen lockfile；Phase-end 整體 quality gate 全綠；adr-check.yml 與 ci.yml 兩 workflow 並存。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.example` → developer | developer 會用此檔 scaffold `.env.local`；若範例含真實 secret 會洩漏 |
| `docker-compose.yml` → dev 環境 | 寫死的 dev 密碼若被誤用於 production 會洩漏 |
| `process.env` → `loadConfig()` | 惡意 env 注入可能使 app startup 繞過 validation |
| GitHub Actions CI → repo | CI 在 ubuntu-latest 跑；若 workflow 有 `secrets.*` 誤用會洩漏 token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | `docker-compose.yml` 寫死 `rigging_dev_password` 被誤用於 production | mitigate | Task 2 `.env.example` 加 "dev-only, DO NOT reuse in production" 註解；docker-compose container_name 冠 `rigging-` prefix 避免與 prod 混淆；Phase 5 quickstart 文件再強調 |
| T-01-05-02 | Information Disclosure | `BETTER_AUTH_SECRET` 範例長度 < 32 而實際使用者照抄 | mitigate | config.ts schema `minLength: 32`；.env.example placeholder 明寫「change-me-to-32-byte-random-hex-before-committing」+ 生成指令；若照抄，loadConfig fail-fast exit 非 0 |
| T-01-05-03 | Information Disclosure | `.env.local` / `.env` 被誤 commit 進 git | mitigate | `.gitignore` (01-PLAN) 已 `.env*` + `!.env.example`；Phase 5 可加 CI `git ls-files` grep 擋 `.env` 非 example 檔 |
| T-01-05-04 | Tampering | `process.env` 被注入繞過 schema（例如 PORT 為 string `"3000"` 但 coerce 路徑漏掉）| mitigate | Task 3 `Number(process.env.PORT)` 顯式 coerce（Pitfall #2）；config.test.ts 的 "invalid PORT (non-numeric)" 案例做 regression |
| T-01-05-05 | Denial of Service | postgres 容器被無限制 restart | accept | `restart: unless-stopped` 策略可接受於 dev 環境；production 場景非 v1 scope |
| T-01-05-06 | Elevation of Privilege | CI workflow 被 PR author 修改引入惡意 run step（e.g., upload secrets） | mitigate | 本 plan workflow 無 `secrets.*` 引用；GitHub `pull_request` trigger 對 fork PR 有保護；未來 Phase 5 若加 secrets 應改 `pull_request_target` 或 require approval |
</threat_model>

<verification>
```bash
# FND-01 bootstrap
bun install --frozen-lockfile
timeout 5 bun run dev 2>&1 | head -5  # 需先有 .env.local；否則展示 Invalid environment variables

# FND-02 env schema fail-fast
BETTER_AUTH_SECRET= timeout 5 bun src/main.ts 2>&1 | grep -q 'Invalid environment variables'

# FND-03 postgres 啟動 + healthy
docker compose up -d postgres
timeout 30 bash -c 'until docker compose exec -T postgres pg_isready -U rigging -d rigging; do sleep 1; done'
docker compose down  # cleanup

# FND-04 Drizzle postgres-js（非 bun:sql）
! grep -rE "drizzle-orm/bun-sql" src/ drizzle.config.ts
grep -q "dialect: 'postgresql'" drizzle.config.ts

# Phase-end full gate
bun run lint && bun run typecheck && bun run test
```
</verification>

<success_criteria>
1. **FND-01** — `bun install` 於 clean checkout 成功；`bun run dev` 可啟動（有 `.env.local` 情況下）並印 "P1 foundation ready"
2. **FND-02** — `.env.example` 6 欄就位；`loadConfig()` 於缺值 / 格式錯時 throw "Invalid environment variables" 並 process exit 非 0；TypeBox schema 與 .env.example keys 1:1（drift guard test 通過）
3. **FND-03** — `docker-compose.yml` 啟動 `postgres:16-alpine` 容器；healthcheck 用 `pg_isready`；10 秒內 healthy（manual smoke，非 CI）
4. **FND-04** — `drizzle.config.ts` 使用 `dialect: 'postgresql'`；不引用 `drizzle-orm/bun-sql`；Phase 2 runtime driver（`drizzle-orm/postgres-js`）由該 phase 的 client.ts 實作
5. Phase-end：`bun install --frozen-lockfile && bun run lint && bun run typecheck && bun run test` 全 exit 0
6. CI workflow `.github/workflows/ci.yml` 與 `adr-check.yml` 並存
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` summarising:
- package.json 終態（completed dep set + exact pin versions for better-auth）
- docker-compose.yml / .env.example / drizzle.config.ts 結構
- config.ts schema + loadConfig contract + 9 test 案例
- ci.yml trigger + steps + Bun 版本 pin
- Manual smoke 結果：
  - `docker compose up -d postgres` healthy within 10s
  - `bun run dev` 於有 `.env.local` 時印 "P1 foundation ready"
  - `BETTER_AUTH_SECRET= bun src/main.ts` 於缺 secret 時印 "Invalid environment variables"
- Phase 1 closure readiness 檢查表：
  - [ ] Success Criteria #1（10 分鐘內 process 啟動）
  - [ ] Success Criteria #2（biome rule 擋 domain 違規 import — 由 03-PLAN 契約測試證明）
  - [ ] Success Criteria #3（12 ADR + README 索引 — 由 04-PLAN 驗證）
  - [ ] Success Criteria #4（AGENTS.md Rigidity Map + anti-features — 由 04-PLAN 驗證）
  - [ ] Success Criteria #5（shared kernel 可被任層 import 且 zero framework — 由 02-PLAN + 03-PLAN override 4 證明）
- Hand-off to Phase 2：src/bootstrap/app.ts (new) + `src/shared/infrastructure/db/client.ts` 消費 `loadConfig().DATABASE_URL` + 加 migration drift check 到 ci.yml（Phase 5）
</output>
