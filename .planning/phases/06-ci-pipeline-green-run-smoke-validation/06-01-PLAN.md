---
phase: 06-ci-pipeline-green-run-smoke-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke-health.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: false
requirements: [CI-04, OBS-01]
tags: [ci, smoke, obs, github-actions, createApp, elysia, bun]

must_haves:
  truths:
    - "`bun run smoke` 在本地（有 postgres + 正確 env）以 exit 0 結束，stdout 含 `✓ Smoke OK` 字樣（OBS-01 本地 sanity）"
    - "`bun run smoke` 在 CI 的 test job 尾端被執行，且該 step 以 success 收尾（OBS-01 CI sanity）"
    - "`.github/workflows/ci.yml` 的 test job `env` 區塊含 `PORT: 3000` 且 `DATABASE_URL` 使用 `postgresql://` scheme（R1 + R10）"
    - "一次非 main 分支的 PR 在 GitHub Actions 上 run 完成後，Checks tab 顯示 lint / typecheck / test+coverage+drift+smoke / adr-check 皆為 success（CI-04 SC#1）"
    - "Plan 1 PR 已 merge 進 main；合併後 main 上的 `scripts/smoke-health.ts`、`package.json`、`.github/workflows/ci.yml` 三檔含本 plan 所有變更"
    - "`06-01-SUMMARY.md` 記錄 PR URL、merged commit SHA、以及至少一個綠燈 check run URL 符合 `https://github.com/.+/actions/runs/\\d+` 格式（D-03 shippable evidence）"
  artifacts:
    - path: "scripts/smoke-health.ts"
      provides: "In-process smoke script: createApp(loadConfig()).handle(Request('/health')) + exit-code gate"
      min_lines: 30
      contains: "createApp(config)"
    - path: "package.json"
      provides: "`smoke` npm script wiring"
      contains: "\"smoke\":"
    - path: ".github/workflows/ci.yml"
      provides: "test job env + smoke step injection"
      contains: "bun run smoke"
    - path: ".planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md"
      provides: "CI-04 + OBS-01 shippable evidence (PR URL, merged SHA, green check run URL)"
      contains: "https://github.com"
  key_links:
    - from: "scripts/smoke-health.ts"
      to: "src/bootstrap/app.ts createApp"
      via: "import { createApp } from '../src/bootstrap/app'"
      pattern: "from '../src/bootstrap/app'"
    - from: "scripts/smoke-health.ts"
      to: "src/bootstrap/config.ts loadConfig"
      via: "import { loadConfig } from '../src/bootstrap/config'"
      pattern: "from '../src/bootstrap/config'"
    - from: "package.json scripts.smoke"
      to: "scripts/smoke-health.ts"
      via: "bun run scripts/smoke-health.ts"
      pattern: "bun run scripts/smoke-health.ts"
    - from: ".github/workflows/ci.yml jobs.test.steps[Smoke ...]"
      to: "package.json scripts.smoke"
      via: "run: bun run smoke"
      pattern: "run: bun run smoke"
    - from: ".github/workflows/ci.yml jobs.test.env.DATABASE_URL"
      to: "src/bootstrap/config.ts ConfigSchema.DATABASE_URL pattern"
      via: "`^postgresql://.+` (R10 — fix scheme)"
      pattern: "postgresql://postgres"
---

<objective>
交付 Phase 6 的 green baseline — 在 repo 內加入「in-process smoke script」並把它注入現有 CI pipeline，之後開一個非 main 分支的 PR，等 GitHub Actions 首次全綠（lint / typecheck / test+coverage+drift+smoke），把該 PR 的 check run URL 記錄進 06-01-SUMMARY.md 作為 CI-04 + OBS-01 的 shippable evidence，最後把 PR merge 進 main。

Purpose: v1.0 已把 3 parallel jobs + drift 寫進 ci.yml 但從未在真 PR 上跑過（deferred 項目）；本 plan 把 createApp + /health smoke gate 這最後一層補上，並用一次真實 CI run 證明整條 pipeline 可運作，為 Plan 2 fail-mode matrix 建立「已知 baseline 綠」的對照組（D-02）。

Output:
  - `scripts/smoke-health.ts`（新檔；mirror `scripts/coverage-gate.ts` 風格）
  - `package.json` scripts 區塊加一行 `"smoke"`
  - `.github/workflows/ci.yml` test job env 修 DATABASE_URL scheme + 加 PORT + 注入 smoke step
  - 一個 merged PR（base=main, head=feature branch），Checks tab 全綠
  - 06-01-SUMMARY.md 含 PR URL + merged commit SHA + green check run URL（D-03）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-CONTEXT.md
@.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-RESEARCH.md
@.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-PATTERNS.md
@.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-VALIDATION.md
@AGENTS.md

<interfaces>
<!-- 以下是 smoke-health.ts 會 import 的兩個 symbol 實際型別 / 簽名，executor 不必再讀 src -->

From src/bootstrap/app.ts:
```typescript
export interface AppDeps {
  db?: DrizzleDb
  probe?: IDbHealthProbe
  authInstance?: AuthInstance
}

export function createApp(config: Config, deps: AppDeps = {}): Elysia
// 同步返回（ADR 0012）；Elysia instance 有 .handle(req: Request): Promise<Response>
```

From src/bootstrap/config.ts:
```typescript
export const ConfigSchema = Type.Object({
  DATABASE_URL: Type.String({ pattern: '^postgresql://.+' }),   // ⚠️ 必 postgresql://
  BETTER_AUTH_SECRET: Type.String({ minLength: 32 }),
  BETTER_AUTH_URL: Type.String({ format: 'uri' }),
  PORT: Type.Integer({ minimum: 1, maximum: 65535 }),           // ⚠️ 無 default
  NODE_ENV: Type.Union([Type.Literal('development'), Type.Literal('production'), Type.Literal('test')]),
  LOG_LEVEL: Type.Union([Type.Literal('debug'), Type.Literal('info'), Type.Literal('warn'), Type.Literal('error')]),
})
export type Config = Static<typeof ConfigSchema>
export function loadConfig(): Config   // invalid env → throw new Error('Invalid environment variables...')
```

From src/health/presentation/controllers/health.controller.ts (response shape):
```typescript
// GET /health
// 200 → { ok: true, db: 'up', checkedAt: ISO string }
// 503 → { ok: false, db: 'down', checkedAt: ISO string }
```

From scripts/coverage-gate.ts (style analog — C1..C12):
- `#!/usr/bin/env bun` shebang
- Block JSDoc `/** filename — purpose (REQ-ID / Phase) */`
- `async function main()` + explicit `process.exit(1)` on fail, implicit exit(0) on success
- `console.error('✗ ...')` for fail / `console.log('✓ ...')` for success
- Bottom: `main()` — smoke 版**加** `.catch((err) => { console.error('✗ Smoke threw:', err); process.exit(1) })`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 建立 scripts/smoke-health.ts 並新增 package.json `smoke` script</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/scripts/coverage-gate.ts (風格先例：shebang + JSDoc + async main + exit code + ✓/✗ prefix；PATTERNS C1..C12)
    - /Users/carl/Dev/CMG/Rigging/tests/integration/app-skeleton-smoke.test.ts (in-process `app.handle(new Request(...))` pattern；PATTERNS §2 canonical ref)
    - /Users/carl/Dev/CMG/Rigging/src/bootstrap/app.ts (createApp 同步簽名、AppDeps 型別、plugin chain 順序)
    - /Users/carl/Dev/CMG/Rigging/src/bootstrap/config.ts (ConfigSchema required fields、loadConfig throw 行為)
    - /Users/carl/Dev/CMG/Rigging/src/health/presentation/controllers/health.controller.ts (response body shape `{ ok, db, checkedAt }`)
    - /Users/carl/Dev/CMG/Rigging/package.json (scripts 區塊既有命名慣例 — PATTERNS §4 + P1)
    - /Users/carl/Dev/CMG/Rigging/biome.json (semicolons: asNeeded / quoteStyle: single / indentWidth: 2)
  </read_first>
  <action>
建立 `scripts/smoke-health.ts`，內容完全依以下骨架（RESEARCH §2 推薦、沿用 coverage-gate.ts 風格、PATTERNS C1..C12）：

```typescript
#!/usr/bin/env bun
/**
 * smoke-health.ts — Phase 6 CI smoke gate (OBS-01 / CI-04)
 *
 * 驗證 createApp(loadConfig()) 能完整 boot 並對 /health 回 200。
 * Walks 全 ADR 0012 plugin chain（requestLogger → cors → errorHandler → swagger → auth → agents → health）。
 * Exit 0 → green；Exit 非零 → red。CI 會把 stderr/stdout 貼進 Actions run log。
 *
 * Landmine note (RESEARCH R1 + R10): test job env 必須含 PORT=3000 且 DATABASE_URL 使用
 * `postgresql://` scheme（非 `postgres://`），否則 loadConfig() 會在 entry 就 throw，
 * smoke 紅燈語意會變成「env 沒設對」而不是「app 壞掉」。Plan 1 Task 2 已在 ci.yml 修妥。
 */
import { createApp } from '../src/bootstrap/app'
import { loadConfig } from '../src/bootstrap/config'

async function main() {
  const config = loadConfig()
  const app = createApp(config)
  const res = await app.handle(new Request('http://localhost/health'))

  if (res.status !== 200) {
    const body = await res.text()
    console.error(`✗ Smoke failed: /health returned ${res.status}`)
    console.error(`  Body: ${body}`)
    process.exit(1)
  }

  const body = (await res.json()) as { ok: boolean; db: string }
  if (!body.ok || body.db !== 'up') {
    console.error(`✗ Smoke failed: body.ok=${body.ok} body.db=${body.db}`)
    process.exit(1)
  }

  console.log('✓ Smoke OK — createApp boot + /health 200 + db up')
}

main().catch((err) => {
  console.error('✗ Smoke threw:', err)
  process.exit(1)
})
```

要點（executor 落檔時嚴守）：
- 第 1 行必為 shebang `#!/usr/bin/env bun`（C1）
- **不加 semicolons、single quotes、2-space indent**（C5/C6/C7；biome asNeeded）
- imports 用相對路徑 `../src/bootstrap/app` 與 `../src/bootstrap/config`（C3/C4；不用 `@/*` alias）
- `✓` / `✗` 使用 U+2713 / U+2717（非 ASCII；C9/C10）
- `main().catch(...)` 是**必加**（C12；coverage-gate 無 catch，smoke 必須有因 loadConfig/createApp/app.handle 三處皆可 throw — RESEARCH §2 + R3 語意）
- 不引入新 npm dependency（C17）；不 export 任何 symbol（C18）；檔案 ≤ 80 行（C19）

同一個 commit 修改 `package.json`，在既有 `"coverage:gate"` 一行之後（line 22 右括弧前）加入新項（記得上一行末補 comma）：

```json
    "coverage:gate": "bun run scripts/coverage-gate.ts",
    "smoke": "bun run scripts/smoke-health.ts"
```

key 必為單字 `smoke`（非 `smoke:health` / `ci:smoke`）— PATTERNS P1；維持 2-space indent + double-quote keys（P4）。不動其他 scripts / dependencies / devDependencies / metadata。

Commit 建議：`feat: [ci] 新增 scripts/smoke-health.ts + bun run smoke npm script (OBS-01)`（AGENTS.md + 全域 git-workflow.md `<type>: [ <scope> ] <subject>`）。
  </action>
  <verify>
    <automated>
test -f /Users/carl/Dev/CMG/Rigging/scripts/smoke-health.ts \
  && head -1 /Users/carl/Dev/CMG/Rigging/scripts/smoke-health.ts | grep -q '^#!/usr/bin/env bun$' \
  && grep -q "from '../src/bootstrap/app'" /Users/carl/Dev/CMG/Rigging/scripts/smoke-health.ts \
  && grep -q "from '../src/bootstrap/config'" /Users/carl/Dev/CMG/Rigging/scripts/smoke-health.ts \
  && grep -q "new Request('http://localhost/health')" /Users/carl/Dev/CMG/Rigging/scripts/smoke-health.ts \
  && grep -q "main().catch" /Users/carl/Dev/CMG/Rigging/scripts/smoke-health.ts \
  && grep -q '"smoke": "bun run scripts/smoke-health.ts"' /Users/carl/Dev/CMG/Rigging/package.json \
  && cd /Users/carl/Dev/CMG/Rigging && bun run lint && bun run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-health.ts` 存在且第 1 行為 `#!/usr/bin/env bun`
    - `scripts/smoke-health.ts` import `createApp` 自 `../src/bootstrap/app`、`loadConfig` 自 `../src/bootstrap/config`
    - `scripts/smoke-health.ts` 含 literal `new Request('http://localhost/health')`
    - `scripts/smoke-health.ts` 含 `main().catch` 處理 throw
    - `scripts/smoke-health.ts` 檔案行數 ≤ 80 行（`wc -l scripts/smoke-health.ts` ≤ 80）
    - `package.json` scripts 區塊含一行 `"smoke": "bun run scripts/smoke-health.ts"`
    - `bun run lint` pass（無 biome 違規；若 semicolons / quotes 寫錯 biome 會紅）
    - `bun run typecheck` pass（tsc --noEmit 無錯）
    - 無新 `dependencies` / `devDependencies` 項目（`git diff package.json` 只動 scripts 區塊）
  </acceptance_criteria>
  <done>
`scripts/smoke-health.ts` 落地、`package.json` 多一個 `"smoke"` script；biome + tsc 皆綠；git 工作區只有這兩個檔 + 本 task 尚未包到其他 commit 的邊角。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修正 ci.yml test job env（PORT + DATABASE_URL scheme）並注入 smoke step</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.github/workflows/ci.yml (現況 110 行；lint job line 15-30、typecheck job line 32-47、test job line 49-109、concurrency line 9-12)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-RESEARCH.md §Implementation Risks R1 / R6 / R8（PORT 缺、smoke 放 drift 後的 skip 語意、DB 依賴）
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-PATTERNS.md §W1..W7 modification checklist
    - /Users/carl/Dev/CMG/Rigging/src/bootstrap/config.ts line 16（`DATABASE_URL: Type.String({ pattern: '^postgresql://.+' })` — 既有 CI env `postgres://` 會被 loadConfig reject；本 task 必同時修）
  </read_first>
  <action>
修改 `.github/workflows/ci.yml` 的 **test job**（line 49-109）做三件事，不動 lint / typecheck jobs / concurrency / services 區塊（PATTERNS W5/W6/W7）：

**變更 1（R10 / R1）— test job env 區塊：**

把 line 66：
```yaml
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/rigging_test
```
改為：
```yaml
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rigging_test
```

理由：`src/bootstrap/config.ts` ConfigSchema 的 DATABASE_URL pattern 為 `^postgresql://.+`，`postgres://...` 無法通過；loadConfig 會 throw「Expected string to match '^postgresql://.+'」。postgres.js driver 兩種 scheme 都接，改成 `postgresql://` 對既有 test job 行為（migrate / test / coverage / drift）零副作用；但**不改就是 smoke step 100% 紅**。此問題 RESEARCH 原列為 R1 延伸（本 task executor 已於 local 驗證確認）。

**變更 2（R1）— 在同 env 區塊（line 66-70 後）加一行：**

```yaml
      PORT: 3000
```

理由：ConfigSchema `PORT: Type.Integer({ minimum: 1, maximum: 65535 })` 無 default 無 optional；loadConfig 缺 PORT 會 throw。既有 lint / typecheck 不 load config 所以沒踩到，smoke 是第一個在 CI 呼叫 loadConfig 的 step。

**變更 3（W2/W3/W4）— 在 `Migration drift check` 之後、`Upload coverage artifact` 之前插入 smoke step：**

在現有 line 101（`exit 1` 之後空行）與 line 103（`- name: Upload coverage artifact`）之間插入：

```yaml
      - name: Smoke (createApp boot + /health 200)
        run: bun run smoke
```

要點：
- step name 必對齊 sibling 風格（`Test (with coverage)` / `Coverage gate (>=80% on ...)`）— 加括號標 scope（W3）
- body 只一行 `run: bun run smoke`（W4；不 inline script、不 inline env、env 從 test job 繼承）
- YAML 2-space indent，`- name:` 開頭與 sibling steps 同縮排等級（W7）
- **不**在 smoke step 加 `if:`、`continue-on-error:`、`env:`；讓它 fail-fast、讓它繼承 test job env（R6：drift fail 時 smoke 自動 skip 符合「先擋 drift」的期望；Plan 2 fail-mode #4 會利用此行為）
- **不**動 `Upload coverage artifact`（line 103-109，有 `if: always()` 保證 coverage artifact 仍會上傳即便 smoke red）

最終 test job `env` 區塊應有 6 行（DATABASE_URL / BETTER_AUTH_SECRET / BETTER_AUTH_URL / NODE_ENV / LOG_LEVEL / PORT）；steps 列表尾端順序為：Apply migrations → Test (with coverage) → Coverage gate → Migration drift check → Smoke (createApp boot + /health 200) → Upload coverage artifact。

Commit 建議：`feat: [ci] 注入 smoke step 與修正 test job env (CI-04 + OBS-01 + R1/R10)`。
  </action>
  <verify>
    <automated>
cd /Users/carl/Dev/CMG/Rigging \
  && grep -q 'PORT: 3000' .github/workflows/ci.yml \
  && grep -q 'DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rigging_test' .github/workflows/ci.yml \
  && ! grep -q 'DATABASE_URL: postgres://postgres:postgres' .github/workflows/ci.yml \
  && grep -q 'name: Smoke (createApp boot + /health 200)' .github/workflows/ci.yml \
  && grep -q 'run: bun run smoke' .github/workflows/ci.yml \
  && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); steps=[s.get('name','') for s in d['jobs']['test']['steps']]; assert steps.index('Smoke (createApp boot + /health 200)') > steps.index('Migration drift check'), 'smoke must follow drift'; assert steps.index('Smoke (createApp boot + /health 200)') < steps.index('Upload coverage artifact'), 'smoke must precede upload'; print('YAML order OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` test job env 含 `PORT: 3000`
    - `.github/workflows/ci.yml` test job env 的 DATABASE_URL 以 `postgresql://` 開頭（非 `postgres://`）
    - `.github/workflows/ci.yml` 含 literal `name: Smoke (createApp boot + /health 200)` 且對應 step body 為 `run: bun run smoke`
    - Smoke step 位於 `Migration drift check` 之後、`Upload coverage artifact` 之前（YAML 解析順序驗證通過）
    - lint / typecheck jobs、`concurrency` 區塊、`services.postgres` 區塊皆未變動（`git diff .github/workflows/ci.yml` 不觸及 line 1-48、line 52-64、line 103-109）
    - 不新增 job；test job 仍為 1 個 `services:postgres` + 其他 3 jobs（lint / typecheck / test）共 3 jobs（`yq '.jobs | keys' .github/workflows/ci.yml` 為 `[lint, test, typecheck]`）
  </acceptance_criteria>
  <done>
ci.yml test job 三項變更落地（env 補 PORT / 改 DATABASE_URL scheme / 插 smoke step），其餘結構不動；git diff 精確對應預期位置。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 推 feature branch、開 PR、等 CI 全綠、把 check URL 寫進 06-01-SUMMARY.md、merge PR（CI-04 + OBS-01 shippable evidence；D-03 + D-04）</name>
  <read_first>
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-CONTEXT.md §decisions D-03 / D-04
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-VALIDATION.md §Per-Task Verification Map (CI-04 / OBS-01)
    - /Users/carl/Dev/CMG/Rigging/.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-RESEARCH.md §R9（PR body 不勾 ADR box）+ §Open Question #1（PR body 可選 smoke 預期 log）
    - /Users/carl/Dev/CMG/Rigging/AGENTS.md §GSD workflow enforcement + 全域 git-workflow.md §Pull Request Workflow
    - /Users/carl/Dev/CMG/Rigging/.github/workflows/adr-check.yml（確認 adr-check trigger 邏輯；本 PR 不含 ADR 新增）
  </read_first>
  <what-built>
Task 1-2 的 commits（smoke script + package.json + ci.yml）已在 local branch。本 task 把這些 commits 推到 remote、開 PR、等 CI 全綠、把 evidence 寫進 SUMMARY、最後 merge 進 main。這是 CI-04 的核心 evidence 收集 + OBS-01 的 CI-side sanity check。
  </what-built>
  <how-to-verify>
**Step A — push feature branch（executor 自動，若 `gh` 可用）**

```bash
# 確認本 branch 不是 main
git rev-parse --abbrev-ref HEAD   # 預期非 `main`；若仍是 main 需 git checkout -b ci/phase-6-green-baseline 再推
git status --short                 # 預期 clean（Task 1-2 的 commits 都已 committed）
git log --oneline main..HEAD       # 預期看到 Task 1 + Task 2 的 commits
git push -u origin HEAD
```

若 branch 尚未建立（remain on main），先 `git checkout -b ci/phase-6-green-baseline` 再 commit Task 1/2 → 再推。branch 命名建議 `ci/phase-6-green-baseline`（kebab-case，明示用途）。

**Step B — 開 PR**

```bash
gh pr create --base main --head HEAD \
  --title "ci: [phase-6] CI green baseline + smoke step (CI-04 + OBS-01)" \
  --body "$(cat <<'EOF'
## Summary

- 新增 `scripts/smoke-health.ts`：in-process `createApp(loadConfig()).handle(Request('/health'))` smoke gate（OBS-01）
- 新增 `package.json` `"smoke"` npm script
- `.github/workflows/ci.yml` test job：補 `PORT: 3000`、修 `DATABASE_URL` scheme 為 `postgresql://`、在 drift 之後注入 `Smoke (createApp boot + /health 200)` step
- 此 PR 為 Phase 6 Plan 1 交付物；合併後 main 上 CI 新增 smoke gate

## Expected CI state

- Lint (biome check) ✓
- Typecheck (tsc --noEmit) ✓
- Test + coverage gate + migration drift ✓（含新加的 Smoke step，log 應有 `✓ Smoke OK — createApp boot + /health 200 + db up`）
- ADR check ✓（本 PR 未新增決策 — This PR does NOT introduce a decision requiring ADR）

## Phase reference

See `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-PLAN.md` for D-01..D-06 locked decisions.
EOF
)"
```

⚠️ PR body 明示「does NOT introduce a decision requiring ADR」— 避免誤勾引發 adr-check false negative（RESEARCH R9）。

**Step C — 等 CI finish 全綠**

```bash
PR_NUM=$(gh pr view --json number --jq .number)
gh pr checks $PR_NUM --watch                # 等到全部 checks 進入終態
gh pr checks $PR_NUM --json name,state,link  # 驗每個 state
```

預期 output：每個 check 的 `state` 為 `success`（或 `pass`），至少涵蓋：
- `CI / Lint (biome check)` → success
- `CI / Typecheck (tsc --noEmit)` → success
- `CI / Test + coverage gate + migration drift` → success（此 job 包含 smoke step）
- `adr-check / *` → success

若任一 red：不要 merge、不要繼續；回頭修本 plan 前兩個 task 或 bug fix；red 的成因是程式問題不是 test 問題（這是 green baseline 必須真綠）。

**Step D — 抓 green check run URL 寫進 SUMMARY**

```bash
CHECK_URL=$(gh pr checks $PR_NUM --json link --jq '.[] | select(.name == "CI / Test + coverage gate + migration drift") | .link')
RUN_ID=$(echo "$CHECK_URL" | grep -oE 'runs/[0-9]+' | head -1 | cut -d/ -f2)
echo "PR: https://github.com/$(gh repo view --json nameWithOwner -q .nameWithOwner)/pull/$PR_NUM"
echo "Green run: $CHECK_URL"
gh run view $RUN_ID --log | grep -E '✓ Smoke OK' || echo '⚠️ smoke OK line not found in log (check run view output for details)'
```

建立 `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md`（模板見下方 Step E）。

**Step E — SUMMARY.md 必含欄位（D-03 shippable evidence）：**

```markdown
# 06-01 SUMMARY — CI Green Baseline + Smoke Step

**Plan:** 06-01 (Phase 6 / v1.1 Release Validation)
**Requirements:** CI-04, OBS-01
**Status:** Complete
**Completed:** <YYYY-MM-DD>

## Evidence (D-03 shippable)

| Item | Value |
|------|-------|
| PR URL | https://github.com/<owner>/<repo>/pull/<PR_NUM> |
| PR state | MERGED |
| Merged commit SHA | <sha from `gh pr view --json mergeCommit --jq .mergeCommit.oid`> |
| Green CI run (Test+coverage+drift+smoke) | https://github.com/<owner>/<repo>/actions/runs/<RUN_ID> |
| Lint check URL | <gh pr checks --json 取 Lint link> |
| Typecheck check URL | <...> |
| ADR-check URL | <...> |
| Smoke step log line | `✓ Smoke OK — createApp boot + /health 200 + db up` |

## Success Criteria 對照（ROADMAP Phase 6 SC#1 + SC#2）

- [x] SC#1: 非 master 分支 PR 首跑 4 個 check item 全綠 — see table above
- [x] SC#2: smoke step 在同 PR run 中 green + log 含 `✓ Smoke OK` — see table above

## Files Changed

- scripts/smoke-health.ts (new, ~30 lines)
- package.json (scripts 區塊 +1 line)
- .github/workflows/ci.yml (test job env +1 line PORT、DATABASE_URL scheme 修正、steps +2 lines smoke)

## Notes

- ADR：本 plan 未新增 ADR（smoke script 落點屬 Tier 3 convention，RESEARCH §Architectural Responsibility Map 已論證；若後續 milestone close 認為值得留痕，Phase 8 再統一處理）
- R1 / R10（PORT 缺 + DATABASE_URL scheme）已於本 plan 修正；後人若改 ci.yml 務必保留這兩項
```

**Step F — merge PR（D-04）**

```bash
gh pr merge $PR_NUM --merge --delete-branch   # 或 --squash 視 repo 慣例；main 上必須有此 merge commit
```

merge 後驗證 main 上三檔都有對應變更：
```bash
git checkout main && git pull
test -f scripts/smoke-health.ts
grep -q '"smoke":' package.json
grep -q 'bun run smoke' .github/workflows/ci.yml
```

**Blocking 條件：** executor 必須等 CI 真綠（不能 bypass 成「假設會綠」）；SUMMARY 沒有真的 check URL 就不能宣告 plan complete。若 CI red，回頭修 code 再 push（不是改 plan）。
  </how-to-verify>
  <acceptance_criteria>
    - `gh pr view <PR> --json state --jq .state` 回傳 `MERGED`（D-04）
    - `gh pr view <PR> --json mergeCommit --jq .mergeCommit.oid` 回傳非空 SHA
    - `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md` 存在
    - SUMMARY.md 含至少一個符合 `https://github.com/.+/actions/runs/[0-9]+` 正則的 URL（D-03 shippable evidence）
    - SUMMARY.md 含字串 `MERGED` 與 `✓ Smoke OK`
    - main branch HEAD 含 `scripts/smoke-health.ts` 檔案（`git show main:scripts/smoke-health.ts > /dev/null` 不報錯）
    - main branch HEAD 的 `package.json` 含 `"smoke":` 與 `ci.yml` 含 `bun run smoke`
    - `gh pr checks <PR> --json state` 中所有 check `state` 皆為 `success`（或等效 pass；無任何 `failure` / `cancelled`）
  </acceptance_criteria>
  <resume-signal>
Executor：當上述 acceptance 全部成立後，把 Step D 抓的 check URL + merged SHA 貼進回報訊息並輸入 `approved`；若 CI red、需要回頭改 code，輸入 `need-fix: <錯誤摘要>` 並把該紅 CI run URL 附上以供 audit。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI secrets → workflow env | `CI_BETTER_AUTH_SECRET` / GitHub token 透過 `${{ secrets.* }}` / `GITHUB_TOKEN` 流入 runner；smoke 本身只 read env，不 echo |
| local repo → GitHub remote | Plan 1 推 feature branch 含 smoke script source；script 會被 PR reviewer 視讀 |
| Postgres service → smoke script | `SELECT 1` 於 runner 內 loopback，無外部網路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `scripts/smoke-health.ts` + ci.yml env echo | mitigate | smoke script **不** `console.log(config)` / **不** echo env；failure path 只印 `res.status` + body JSON（/health body 無 secret）。ci.yml smoke step 不加 `env:` block（繼承 test job env 即可），避免在 GitHub Actions log 中額外 expose 任何 secret 名稱 |
| T-06-02 | Tampering | Plan 2 sacrificial PR 若被誤 merge 進 main | mitigate | Plan 1 不處理（Plan 2 責任）；但 Plan 1 merge 到 main 後，main branch protection + PR 審查慣例可擋；Plan 2 PR 會在 title 前加 `[DO NOT MERGE]`（見 Plan 2） |
| T-06-03 | Tampering | force-push 覆蓋 main race condition | mitigate | Plan 1 PR base = main 但 feature branch 非 main / master / release/*；推 feature branch 不使用 force；若需修正 review feedback 用正常 commit（非 force-push） |
| T-06-04 | Denial of Service | smoke script 打 Postgres SELECT 1 失敗造成 CI red | accept | 這正是 gate 正常行為 — DB 無法連線時 /health 回 503 → smoke exit 1 → CI red 合理；非 security issue |
| T-06-05 | Elevation of Privilege | `ci.yml` 注入不可信 env 造成 smoke 執行惡意邏輯 | accept | env 全由 repo 維護者宣告；不從 PR fork 繼承（`pull_request` default 不 expose secrets to forks）；低風險 |
</threat_model>

<verification>
**Plan 1 phase-level verification（在執行 Task 3 最後一步之後 + 在宣告 plan complete 之前跑）：**

```bash
cd /Users/carl/Dev/CMG/Rigging

# 1. 本地 smoke script sanity（需 docker-compose Postgres 起著、env 正確）
docker compose up -d postgres 2>/dev/null || true
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/rigging_test" \
  BETTER_AUTH_SECRET="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  BETTER_AUTH_URL="http://localhost:3000" \
  NODE_ENV="test" LOG_LEVEL="error" PORT="3000" \
  bun run smoke
# 預期 exit 0，stdout 有 `✓ Smoke OK — createApp boot + /health 200 + db up`

# 2. lint / typecheck / test 全綠（local pre-push 檢查）
bun run lint && bun run typecheck && bun run test:ci

# 3. main branch HEAD 三檔齊全
git fetch origin main && git show origin/main:scripts/smoke-health.ts > /dev/null
git show origin/main:package.json | grep -q '"smoke":'
git show origin/main:.github/workflows/ci.yml | grep -q 'bun run smoke'

# 4. SUMMARY.md 與 evidence
grep -E 'https://github.com/.+/actions/runs/[0-9]+' \
  .planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md
grep -q 'MERGED' .planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md
grep -q '✓ Smoke OK' .planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md
```

**PR 層驗證**：`gh pr view <PR> --json state,mergeCommit` 回傳 `{"state":"MERGED", "mergeCommit":{"oid":"<sha>"}}`。
</verification>

<success_criteria>
**Plan 1 complete when ALL are TRUE（goal-backward must_haves 匯總）：**

1. `scripts/smoke-health.ts` 在 main 上存在、包含 `createApp(config)` 與 `new Request('http://localhost/health')`、檔案 ≤ 80 行
2. `package.json` 在 main 上含 `"smoke": "bun run scripts/smoke-health.ts"`
3. `.github/workflows/ci.yml` 在 main 上含 `PORT: 3000` + `DATABASE_URL: postgresql://...` + `name: Smoke (createApp boot + /health 200)` step（位於 drift 之後、upload coverage 之前）
4. Plan 1 PR state 為 `MERGED`，merge commit SHA 可於 `gh pr view --json mergeCommit` 取得
5. `06-01-SUMMARY.md` 含 PR URL + merged SHA + 至少一個符合 `/actions/runs/\d+` 的綠 CI run URL + `✓ Smoke OK` 字串
6. PR 的 4 個 checks（Lint / Typecheck / Test+coverage+drift+smoke / adr-check）state 皆為 success
7. `bun run lint && bun run typecheck` 在 main HEAD 綠（驗本 plan 沒打破 repo）
</success_criteria>

<output>
After completion, create `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md` per Task 3 Step E template.

**Also backfill `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-VALIDATION.md`：**
- 在 §Per-Task Verification Map 把 CI-04 / OBS-01 兩 row 的 `Status` 從 `⬜ pending` 改為 `✅ green`
- 在 §Validation Sign-Off 勾掉前兩項（smoke script 落地 + PORT 補上）— DATABASE_URL scheme fix 也寫一行補註
</output>
