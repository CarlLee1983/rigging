---
phase: 07-phase-04-security-audit-back-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-demo-domain/04-SECURITY.md
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "04-SECURITY.md 包含 CVE-2025-61928 的專屬小節，引用 ZeroPath 連結、測試指令、v1.1 HEAD commit SHA"
    - "04-SECURITY.md 包含 API Key timing-safe 驗證小節，引用 timingSafeEqual 實作位置與 1000-iter ratio 測試"
    - "04-SECURITY.md 包含 cross-user 404 全動詞矩陣（read/update/delete/post/list），list 動詞以 list-prompt-versions.usecase.ts:17–21 程式碼層級証明補足"
    - "Security Audit Trail 有新一行，含執行日期與 git rev-parse HEAD SHA"
    - "現有 19 行威脅登記、已接受風險 log、既有稽核記錄全數保留"
  artifacts:
    - path: ".planning/phases/04-demo-domain/04-SECURITY.md"
      provides: "Phase 04 security contract with SEC-01 compliance evidence"
      contains: "SEC-01 Compliance Evidence"
    - path: ".planning/phases/04-demo-domain/04-SECURITY.md"
      provides: "CVE regression evidence"
      contains: "CVE-2025-61928"
    - path: ".planning/phases/04-demo-domain/04-SECURITY.md"
      provides: "Timing-safe verification evidence"
      contains: "timingSafeEqual"
    - path: ".planning/phases/04-demo-domain/04-SECURITY.md"
      provides: "Cross-user 404 matrix with all verbs"
      contains: "list-prompt-versions.usecase.ts"
  key_links:
    - from: ".planning/phases/04-demo-domain/04-SECURITY.md"
      to: "tests/integration/auth/cve-2025-61928.regression.test.ts"
      via: "section reference"
      pattern: "cve-2025-61928\\.regression\\.test\\.ts"
    - from: ".planning/phases/04-demo-domain/04-SECURITY.md"
      to: "src/auth/infrastructure/better-auth/identity-service.adapter.ts"
      via: "timingSafeEqual implementation citation"
      pattern: "identity-service\\.adapter\\.ts"
    - from: ".planning/phases/04-demo-domain/04-SECURITY.md"
      to: "src/agents/application/usecases/list-prompt-versions.usecase.ts"
      via: "code-level proof for list verb (D-07 Branch B)"
      pattern: "list-prompt-versions\\.usecase\\.ts"
---

<objective>
Phase 04 SECURITY.md 補票：在不替換既有內容的前提下，新增三個 SEC-01 合規佐證小節（CVE-2025-61928 regression、API Key timing-safe 驗證、cross-user 404 全動詞矩陣），並在執行時實際跑測試、記錄 HEAD SHA，使 v1.0「理論 secure」升級為「文件 self-verified secure」。

Purpose: 滿足 ROADMAP Phase 7 成功標準 1–5 及 REQUIREMENTS.md SEC-01 §a–§d，讓外部 reviewer 可閱讀 04-SECURITY.md 並直接驗證安全覆蓋。

Output:
- `.planning/phases/04-demo-domain/04-SECURITY.md` — 新增 `## SEC-01 Compliance Evidence` 區段（三個子節）+ Security Audit Trail 新行
</objective>

<execution_context>
@/Users/carl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase-04-security-audit-back-fill/07-CONTEXT.md
@.planning/phases/07-phase-04-security-audit-back-fill/07-RESEARCH.md
@.planning/phases/04-demo-domain/04-SECURITY.md

<interfaces>
<!-- 執行者在動筆之前必須確認的程式碼事實 — 直接從 codebase 驗證，勿靠記憶 -->

From src/agents/application/usecases/list-prompt-versions.usecase.ts (lines 17–21):
```typescript
async execute(ctx: AuthContext, input: ListPromptVersionsInput): Promise<PromptVersion[]> {
  const agent = await this.agentRepo.findById(input.agentId)
  if (!agent || agent.ownerId !== ctx.userId) {
    throw new ResourceNotFoundError('Resource not found')
  }
  return this.promptVersionRepo.listByAgent(input.agentId)
}
```

From src/auth/infrastructure/better-auth/identity-service.adapter.ts:
- Import: `import { createHash, timingSafeEqual } from 'node:crypto'` (line 2)
- `DUMMY_HASH`: `createHash('sha256').update('dummy').digest()` — pre-computed Buffer
- Malformed key path (lines 43–53): `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)` + dummy findByKeyHash + dummy findByPrefix
- Valid format, hash not found (lines 58–62): `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)` + dummy findByPrefix
- Hash found but revoked/expired (lines 64–70): `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)`

Evidence test files (all confirmed to exist at HEAD `a131a872`):
- tests/integration/auth/cve-2025-61928.regression.test.ts
- tests/integration/auth/timing-safe-apikey.regression.test.ts
- tests/integration/agents/cross-user-404.test.ts
- tests/integration/agents/dogfood-self-prompt-read.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 執行佐證測試套件並記錄結果</name>
  <files>（僅執行 bun test，不修改檔案）</files>
  <read_first>
    - `.planning/phases/04-demo-domain/04-SECURITY.md` — 確認現有結構（Sign-Off 位置 + Audit Trail 格式），不可覆蓋
    - `tests/integration/auth/cve-2025-61928.regression.test.ts` — 確認測試名稱用於引用
    - `tests/integration/auth/timing-safe-apikey.regression.test.ts` — 確認測試名稱與 ratio gate 描述
    - `tests/integration/agents/cross-user-404.test.ts` — 確認動詞/路由覆蓋清單
    - `tests/integration/agents/dogfood-self-prompt-read.test.ts` — 確認 Variant 3 描述
    - `src/agents/application/usecases/list-prompt-versions.usecase.ts` — 確認 lines 17–21 ownership gate 原文
    - `src/auth/infrastructure/better-auth/identity-service.adapter.ts` — 確認 timingSafeEqual 使用行號
  </read_first>
  <action>
執行以下指令（需先確保 Docker container `rigging-postgres` 已啟動且 `.env` 已 source）：

```bash
# 1. 取得當前 HEAD SHA（將在 SECURITY.md 中記錄）
git rev-parse HEAD

# 2. 執行 CVE + timing regression 套件
bun run test:regression

# 3. 執行 agents 跨用戶測試
bun test tests/integration/agents/cross-user-404.test.ts
bun test tests/integration/agents/dogfood-self-prompt-read.test.ts
```

記錄以下資訊供 Task 2 使用：
- HEAD SHA（完整 40 字元）
- 每個測試檔案的 pass/fail 結果
- timing-safe 測試的 ratio 輸出（stdout JSON `{ test: 'timing-safe-apikey', malMean, wrongHashMean, ratio }`）

**若任一測試失敗：** 在繼續前調查失敗原因——環境問題（DATABASE_URL 未設定）與測試邏輯失敗必須區分。環境問題 → 修復環境再跑；測試邏輯失敗 → 停止並回報。

**D-06 re-run check:** 確認 `git log -- src/auth/ --since=v1.0` 是否有 src/auth/** 更動；若有則此 timing test 重跑為強制要求。研究資料顯示 src/auth/** 最後更動為 `9882c42`（Phase 3），故重跑為驗證性而非強制。
  </action>
  <verify>
    <automated>bun run test:regression 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - 四個測試檔案全數 pass（exit code 0）
    - HEAD SHA 已記錄
    - timing ratio 值已記錄（若 stdout 有輸出）
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 04-SECURITY.md 新增 SEC-01 合規佐證區段</name>
  <files>.planning/phases/04-demo-domain/04-SECURITY.md</files>
  <read_first>
    - `.planning/phases/04-demo-domain/04-SECURITY.md` — **完整讀取**，確認 `## Accepted Risks Log` 結尾與 `## Security Audit Trail` 開頭之間的確切插入位置；確認 Sign-Off section 格式
    - `.planning/phases/07-phase-04-security-audit-back-fill/07-CONTEXT.md` — D-03、D-04、D-05、D-07 決策細節
    - `src/agents/application/usecases/list-prompt-versions.usecase.ts` — lines 17–21，直接引用原文
  </read_first>
  <action>
在 `## Accepted Risks Log` 區段結尾與 `## Security Audit Trail` 區段之間，**插入**以下新區段（不替換任何現有內容）。將 `{HEAD_SHA}` 替換為 Task 1 取得的實際 SHA，將 `{RATIO}` 替換為 timing 測試實際輸出的 ratio 值（若無輸出則填寫 `< 0.20`）：

```markdown
---

## SEC-01 Compliance Evidence

> Added 2026-04-20 — Phase 7 SEC-01 back-fill. Addresses ROADMAP Phase 7 success criteria 3–5 and REQUIREMENTS.md SEC-01 §b–§d. Evidence re-verified at HEAD commit `{HEAD_SHA}`.

### CVE-2025-61928 Regression (SEC-01 §b)

**Attack pattern:** Unauthenticated `POST /api-keys` with victim `userId` in request body — [ZeroPath advisory](https://zeropath.com/blog/breaking-authentication-unauthenticated-api-key-creation-in-better-auth-cve-2025-61928)

**Defense location (Phase 3):** `CreateApiKeyUseCase` enforces `AUTH-15` — `body.userId` must equal the authenticated session's `userId`. Global `authContextPlugin` + `requireAuth: true` ensures all Phase 04 routes require authentication before any use-case is invoked.

**Phase 04 coverage:** Phase 04 APIs do not duplicate CVE regression tests (Phase 3 owns the auth path). Phase 04 routes remain protected because:
1. `createAgentsModule` assumes Phase 3 `authContextPlugin` is globally mounted (ADR 0012, `createApp` order enforced)
2. All 13 Phase 04 routes declare `requireAuth: true`
3. `AuthContext` is unreachable without a valid identity — routes that skip `requireAuth: true` cannot compile without a cast

**Regression test:**
- **File:** `tests/integration/auth/cve-2025-61928.regression.test.ts`
- **Describe block:** `[Regression] CVE-2025-61928 - unauth API key creation via body.userId`
- **Tests:**
  - `unauthenticated POST /api-keys with body.userId -> 401 + zero keys for victim`
  - `authenticated attacker cannot create API key for victim via body.userId -> 403 USER_ID_MISMATCH`
- **Run command:** `bun test tests/integration/auth/cve-2025-61928.regression.test.ts`  
  (or `bun run test:regression` to run all regression suites)
- **Status at v1.1 HEAD `{HEAD_SHA}`:** PASS

---

### API Key Timing-Safe Verification (SEC-01 §c)

**Threat:** Timing side-channel on API Key verification — malformed-path latency differs from valid-format-wrong-hash latency, revealing key format information to an attacker.

**Implementation:** `src/auth/infrastructure/better-auth/identity-service.adapter.ts` — class `BetterAuthIdentityService`, method `verifyApiKey(rawKey: string)`

All three rejection paths walk timing-aligned dummy operations via `crypto.timingSafeEqual` from `node:crypto`:

| Rejection path | Action |
|----------------|--------|
| Malformed key (bad prefix / wrong length / non-ASCII) | `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)` + dummy `findByKeyHash` + `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)` + dummy `findByPrefix` |
| Valid format, hash not found in DB | `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)` + dummy `findByPrefix` |
| Hash found but key revoked or expired | `timingSafeEqual(DUMMY_HASH, DUMMY_HASH)` |

`DUMMY_HASH` is `createHash('sha256').update('dummy').digest()` — pre-computed Buffer, not a per-call allocation.

**Benchmark test:**
- **File:** `tests/integration/auth/timing-safe-apikey.regression.test.ts`
- **Describe block:** `[Regression AUX-04 / D-10] API key timing alignment`
- **Test:** `1000-iteration latency: |t_malformed - t_valid_wrong_hash| / t_valid_wrong_hash < 0.2`
  - 100-iteration warm-up before measurement
  - Gate: ratio must be `< 0.2` (20% tolerance)
  - Logged output: `{ test: 'timing-safe-apikey', malMean, wrongHashMean, ratio }`
- **Run command:** `bun test tests/integration/auth/timing-safe-apikey.regression.test.ts`
- **Status at v1.1 HEAD `{HEAD_SHA}`:** PASS (ratio: {RATIO})

**Phase 3 decision reference:** Phase 3 D-10 (timing alignment requirement) and D-09 (API Key fast-reject 401 without cookie fallback).

---

### Cross-User 404 Matrix — All Verbs (SEC-01 §d)

**Threat:** HTTP response differences on cross-user access reveal resource existence (403 → resource exists; 404 → doesn't). Phase 04 mitigates via single-branch `ResourceNotFoundError` on all cross-user paths (D-09).

**Ownership gate pattern (D-10):** Implemented at use-case layer. All Phase 04 use cases follow:
```typescript
const agent = await this.agentRepo.findById(input.agentId)
if (!agent || agent.ownerId !== ctx.userId) {
  throw new ResourceNotFoundError('Resource not found')
}
```

**Evidence matrix:**

| Verb | Route | Coverage Type | Evidence | Status at `{HEAD_SHA}` |
|------|-------|---------------|----------|------------------------|
| GET (single) | `/agents/:id` | integration test | `cross-user-404.test.ts` — `User B GET /agents/:userAAgentId → 404 RESOURCE_NOT_FOUND` | PASS |
| GET (nested latest) | `/agents/:id/prompts/latest` | integration test | `dogfood-self-prompt-read.test.ts` — Variant 3: User B API Key → `GET /agents/:id/prompts/latest` → 404 `RESOURCE_NOT_FOUND` | PASS |
| GET (list) | `/agents/:id/prompts` | code-level proof | `src/agents/application/usecases/list-prompt-versions.usecase.ts:17–21` — identical ownership gate to single-read path (see below) | n/a — code proof |
| PATCH | `/agents/:id` | integration test | `cross-user-404.test.ts` — `User B PATCH /agents/:userAAgentId → 404 RESOURCE_NOT_FOUND` | PASS |
| DELETE | `/agents/:id` | integration test | `cross-user-404.test.ts` — `User B DELETE /agents/:userAAgentId → 404` | PASS |
| POST (nested) | `/agents/:id/prompts` | integration test | `cross-user-404.test.ts` — `User B POST /agents/:userAAgentId/prompts → 404 (ownership fails before scope)` | PASS |

**List verb code-level proof (D-07 Branch B):**

No dedicated integration test exists for `User B GET /agents/:userAAgentId/prompts` (list-prompts cross-user 404). However, the ownership gate in `ListPromptVersionsUseCase` is **identical** to the gates tested above:

```typescript
// src/agents/application/usecases/list-prompt-versions.usecase.ts:17–21
async execute(ctx: AuthContext, input: ListPromptVersionsInput): Promise<PromptVersion[]> {
  const agent = await this.agentRepo.findById(input.agentId)
  if (!agent || agent.ownerId !== ctx.userId) {
    throw new ResourceNotFoundError('Resource not found')
  }
  return this.promptVersionRepo.listByAgent(input.agentId)
}
```

The controller at `src/agents/presentation/controllers/prompt-version.controller.ts` calls `deps.listPromptVersions.execute(authContext, { agentId })` with `requireAuth: true`. The ownership check is not in the controller layer — it is in the use case, which is the same code path called by all routes. The two existing cross-user read-path tests (Variant 3 of `dogfood-self-prompt-read.test.ts` + `cross-user-404.test.ts` GET) confirm the gate fires correctly for the read path; the list path uses the same gate from the same use-case pattern. Additional test cross-referencing: `tests/integration/agents/prompt-version-crud.test.ts` covers the happy-path list route (same-user `GET /prompts` → 200), confirming the route resolves.

**Test run commands:**
```bash
bun test tests/integration/agents/cross-user-404.test.ts
bun test tests/integration/agents/dogfood-self-prompt-read.test.ts
```

```

次在 `## Security Audit Trail` 的表格中**新增一行**（加在最後一行之後）：

```markdown
| 2026-04-20 | 19 | 19 | 0 | Claude Code (Phase 7 SEC-01 back-fill: CVE + timing + cross-user matrix evidence added; `git rev-parse HEAD` = `{HEAD_SHA}`; `bun run test:regression` + agents cross-user suites all green) |
```

**CRITICAL — 禁止修改事項：**
- 禁止更改 frontmatter `created: 2026-04-20`
- 禁止刪除或修改 19 行 Threat Register 任何一行
- 禁止刪除或修改 Accepted Risks Log 三行
- 禁止刪除或修改既有兩筆 Security Audit Trail 記錄
- 禁止刪除 Sign-Off 區段
  </action>
  <acceptance_criteria>
    - `grep -c "SEC-01 Compliance Evidence" .planning/phases/04-demo-domain/04-SECURITY.md` 輸出 `1`
    - `grep -c "CVE-2025-61928" .planning/phases/04-demo-domain/04-SECURITY.md` 輸出 `≥ 2`（一在新區段，一在既有 T-04-08 可能無，但 ZeroPath 連結已在新區段）
    - `grep -c "timingSafeEqual" .planning/phases/04-demo-domain/04-SECURITY.md` 輸出 `≥ 1`
    - `grep -c "list-prompt-versions.usecase.ts" .planning/phases/04-demo-domain/04-SECURITY.md` 輸出 `≥ 1`（D-07 Branch B 程式碼層級証明）
    - `grep -c "zeropath.com" .planning/phases/04-demo-domain/04-SECURITY.md` 輸出 `1`
    - `grep "T-04-01" .planning/phases/04-demo-domain/04-SECURITY.md` 仍存在（確認既有 Threat Register 未被刪除）
    - `grep "AR-04-01" .planning/phases/04-demo-domain/04-SECURITY.md` 仍存在（確認 Accepted Risks Log 未被刪除）
    - `grep -c "^| 2026-04-20" .planning/phases/04-demo-domain/04-SECURITY.md` 輸出 `3`（兩筆既有 + 一筆新增）
    - `grep "git rev-parse HEAD" .planning/phases/04-demo-domain/04-SECURITY.md` 中的 SHA 不是 placeholder（不包含 `{HEAD_SHA}` 字面文字）
    - `grep "\- \[x\] All threats have a disposition" .planning/phases/04-demo-domain/04-SECURITY.md` 仍存在（Sign-Off 未被刪除）
  </acceptance_criteria>
  <verify>
    <automated>grep -c "SEC-01 Compliance Evidence" .planning/phases/04-demo-domain/04-SECURITY.md &amp;&amp; grep -c "list-prompt-versions.usecase.ts" .planning/phases/04-demo-domain/04-SECURITY.md &amp;&amp; grep -c "T-04-01" .planning/phases/04-demo-domain/04-SECURITY.md &amp;&amp; grep -c "AR-04-01" .planning/phases/04-demo-domain/04-SECURITY.md</automated>
  </verify>
  <done>
    - 04-SECURITY.md 包含 `## SEC-01 Compliance Evidence` 區段，其下有 CVE、timing-safe、cross-user 三個子節
    - 所有 `{HEAD_SHA}` placeholder 已替換為實際 SHA（40 字元 hex）
    - `{RATIO}` 已替換為實際測試輸出或 `< 0.20`
    - 既有 19 行威脅登記、3 筆接受風險、2 筆稽核記錄、Sign-Off 全數保留
    - Security Audit Trail 新增第 3 筆記錄（含 HEAD SHA）
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit SEC-01 佐證更新</name>
  <files>.planning/phases/04-demo-domain/04-SECURITY.md</files>
  <read_first>
    - `.planning/phases/04-demo-domain/04-SECURITY.md` — 最終狀態確認（所有三個子節存在、SHA 已填入）
  </read_first>
  <action>
執行 git commit，將更新後的 04-SECURITY.md 提交：

```bash
git add .planning/phases/04-demo-domain/04-SECURITY.md
git commit -m "docs: [07-01] Phase 04 SECURITY.md SEC-01 compliance evidence — CVE-2025-61928 + timing-safe + cross-user matrix (D-07 Branch B)"
```

commit message 格式說明：
- `docs` — 文件性質（無生產程式碼修改）
- `[07-01]` — Phase 7 / Plan 01
- 後半句描述三個新增小節與 D-07 Branch B 決策
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` 包含 `[07-01]` 與 `SEC-01`
    - `git diff HEAD~1 -- .planning/phases/04-demo-domain/04-SECURITY.md | grep "^+" | grep -c "SEC-01 Compliance Evidence"` 輸出 `1`（新增行存在於 diff 中）
    - `git status` 輸出為 clean（nothing to commit）
  </acceptance_criteria>
  <verify>
    <automated>git log --oneline -1 &amp;&amp; git status --short</automated>
  </verify>
  <done>
    - 單一 commit 包含 04-SECURITY.md 的全部更改
    - Working tree 乾淨
    - Commit message 符合專案格式
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → codebase | SECURITY.md citations reference actual code line numbers — stale citations are a documentation integrity threat |
| test runner → database | Integration tests require live PostgreSQL; environment misconfiguration causes silent false-negative evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Repudiation | 04-SECURITY.md audit trail | mitigate | Executor records actual `git rev-parse HEAD` SHA in audit trail entry — not a placeholder. Task 2 acceptance_criteria grep explicitly rejects `{HEAD_SHA}` literal. |
| T-07-02 | Tampering | 04-SECURITY.md existing content | mitigate | Task 2 action includes explicit "禁止修改事項" checklist + acceptance_criteria grep for T-04-01, AR-04-01, Sign-Off line existence. Edit tool diff makes accidental deletion visible. |
| T-07-03 | Information Disclosure | D-07 list-verb gap (code-level proof) | accept | Branch B chosen (per planning_context D-07 lock). Code-level proof in SECURITY.md is technically sound — `ListPromptVersionsUseCase:17–21` uses identical ownership gate. Risk: no dedicated integration test for this path. Rationale: use-case layer is the single ownership enforcement point (D-10); same gate tested via GET and nested-read paths. |
| T-07-04 | Repudiation | bun test evidence | mitigate | Task 1 requires actual test execution before Task 2 updates SECURITY.md. "Actually run bun test" instruction is explicit in Task 1 action. Task 1 done criteria requires exit code 0. |
| T-07-05 | Elevation of Privilege | CVE-2025-61928 documentation gap | mitigate | SECURITY.md CVE section explicitly states Phase 04 APIs remain protected via global authContextPlugin + requireAuth: true on all 13 routes. Regression test file cited with exact run command. |
</threat_model>

<verification>
Phase 7 complete when all three conditions hold:

1. `grep -c "SEC-01 Compliance Evidence" .planning/phases/04-demo-domain/04-SECURITY.md` = `1`
2. `grep -c "^| 2026-04-20" .planning/phases/04-demo-domain/04-SECURITY.md` = `3` (2 existing + 1 new)
3. `git log --oneline -3 | grep "07-01"` 存在

Full evidence suite still green:
```bash
bun run test:regression
bun test tests/integration/agents/cross-user-404.test.ts
bun test tests/integration/agents/dogfood-self-prompt-read.test.ts
```
</verification>

<success_criteria>
1. `.planning/phases/04-demo-domain/04-SECURITY.md` 存在於 repo，外部 reviewer 可直接閱讀（ROADMAP criterion 1 — 既有）
2. 文件包含 Phase 04 threat register 每條 threat 的 mitigation evidence（ROADMAP criterion 2 — 既有 + SEC-01 §a）
3. 文件記錄 CVE-2025-61928 attack pattern 在 v1.1 head commit 上的 regression 現況：test 名稱 + 通過狀態 + HEAD SHA（ROADMAP criterion 3 — SEC-01 §b）
4. 文件驗證 API Key hash verify path 為 timing-safe compare：引用 timingSafeEqual 實作位置 + 1000-iter ratio 測試 + HEAD SHA（ROADMAP criterion 4 — SEC-01 §c）
5. 文件確認 cross-user 404 matrix 在 Phase 04 代碼 6 個動詞（GET-single/GET-nested/GET-list/PATCH/DELETE/POST）上皆覆蓋；list 動詞以 list-prompt-versions.usecase.ts:17–21 程式碼層級証明補足（ROADMAP criterion 5 — SEC-01 §d，D-07 Branch B）
</success_criteria>

<output>
After completion, create `.planning/phases/07-phase-04-security-audit-back-fill/07-01-SUMMARY.md` following the template at `@/Users/carl/.claude/get-shit-done/templates/summary.md`.

Summary must include:
- Task completion status for all 3 tasks
- Actual HEAD SHA recorded in SECURITY.md
- timing-safe ratio value from test output (or "< 0.20" if not logged)
- Confirmation that all 4 evidence test files passed
- D-07 Branch B decision recorded (code-level proof, no new test)
- Note any deviation from plan (e.g., if timing test was unexpectedly flaky)
</output>
