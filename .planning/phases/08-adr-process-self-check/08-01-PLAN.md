---
phase: 08-adr-process-self-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/adr-check.yml
  - scripts/validate-adr-frontmatter.ts
  - package.json
  - docs/decisions/README.md
  - docs/decisions/0000-use-madr-for-adrs.md
  - docs/decisions/0001-runtime-bun.md
  - docs/decisions/0002-web-framework-elysia.md
  - docs/decisions/0003-ddd-layering.md
  - docs/decisions/0004-auth-betterauth.md
  - docs/decisions/0005-orm-drizzle.md
  - docs/decisions/0006-authcontext-boundary.md
  - docs/decisions/0007-runtime-guards-via-di.md
  - docs/decisions/0008-dual-auth-session-and-apikey.md
  - docs/decisions/0009-rigidity-map.md
  - docs/decisions/0010-postgres-driver-postgres-js.md
  - docs/decisions/0011-resolver-precedence-apikey-over-cookie.md
  - docs/decisions/0012-global-plugin-ordering.md
  - docs/decisions/0013-api-key-storage-hash-plus-index.md
  - docs/decisions/0014-api-key-hashing-sha256.md
  - docs/decisions/0015-rate-limit-memory-v1-persistent-v2.md
  - docs/decisions/0016-betterauth-defaults-trust.md
  - docs/decisions/0017-eval-dataset-shape-jsonb-immutable.md
  - docs/decisions/0018-testcontainers-deviation-via-docker-compose.md
  - .planning/phases/08-adr-process-self-check/08-01-SUMMARY.md
  - docs/decisions/0019-adr-check-madr-validation.md
autonomous: true
requirements:
  - ADR-06
tags: [adr, madr, adr-check, audit, readme, ci]

must_haves:
  truths:
    - "Every ADR file 0000..0018 has YAML `status` ∈ {accepted, superseded, deprecated} (lowercase, MADR convention) and README Status column documents the same lifecycle (or README explicitly maps display labels)"
    - "`docs/decisions/README.md` index row count matches 19 ADR files; spot-check ≥3 rows (0000, 0009, 0018) against file front matter + title line"
    - "`adr-check` workflow invokes validation on added `docs/decisions/*.md` when ADR checkbox is set; intentionally incomplete front matter fails CI"
    - "08-01-SUMMARY.md records ADR 0019 decision (new file path + title) OR explicit rationale that v1.1 needs no new ADR for CI hardening (per ROADMAP SC4)"
  artifacts:
    - path: ".planning/phases/08-adr-process-self-check/08-01-SUMMARY.md"
      provides: "Audit matrix + README spot-check + ADR 0019 / no-new-ADR decision"
      contains: "Spot-check"
    - path: ".github/workflows/adr-check.yml"
      provides: "MADR gate when PR requires ADR"
      contains: "validate-adr"
---

<objective>
Ship **ADR-06(b)** and the **prerequisite gate** for **ADR-06(a)**: audit ADR 0000–0018 and `docs/decisions/README.md`, fix any drift, extend `adr-check` so a malformed new ADR fails when the PR is marked as requiring an ADR, and record whether **ADR 0019** documents CI validation (or document “no new ADR” with rationale).

Purpose: CONTEXT **D-01** / **D-02** — audit + gate hardening **before** sacrificial PR evidence (08-02).

Output:
- Updated ADR files and/or README as needed
- `scripts/validate-adr-frontmatter.ts` + workflow wiring
- Optional `docs/decisions/0019-adr-check-madr-validation.md` (or equivalent) + README row
- `08-01-SUMMARY.md`
</objective>

<threat_model>
## Assets
- ADR corpus integrity (`docs/decisions/0000`–`0018`) and public index (`README.md`).
- CI truthfulness of `adr-check` (must not silently pass invalid ADRs when ADR is claimed).

## Threats (ASVS-aligned, process severity)
- **T1 (medium):** Index drift — README claims wrong status vs file → reviewers trust wrong lifecycle.
- **T2 (low):** Validation script false negative — malformed ADR merges.
- **T3 (low):** Validation script false positive — blocks valid ADRs.

## Mitigations
- Audit uses file-vs-README comparison; fixes are explicit edits with grep-verifiable strings.
- Validator keys copied from `docs/decisions/0000-use-madr-for-adrs.md` front matter; unit-test or `bun run` self-check on golden + negative fixture in Task 3.
- Workflow only runs strict validation when `adr-required` step is true and new ADR paths exist (see Task 4 conditions).
</threat_model>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-adr-process-self-check/08-CONTEXT.md
@.planning/phases/08-adr-process-self-check/08-RESEARCH.md
@.planning/phases/08-adr-process-self-check/08-VALIDATION.md
@.github/workflows/adr-check.yml
@docs/decisions/0000-use-madr-for-adrs.md
@docs/decisions/README.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit ADR 0000..0018 front matter + README rows</name>
  <read_first>
    - `docs/decisions/README.md` — full index table
    - `docs/decisions/0000-use-madr-for-adrs.md` — canonical front matter keys
    - `docs/decisions/0009-rigidity-map.md` — mid-range spot-check
    - `docs/decisions/0018-testcontainers-deviation-via-docker-compose.md` — high-number spot-check
  </read_first>
  <action>
For each file `docs/decisions/00NN-*.md` for NN=00..18:
1. Confirm file exists and first line after front matter is `# 00NN.` (regex `^# 00NN\.`).

2. Record YAML keys: `status`, `date`, `deciders`, `consulted`, `informed` — all must be present (values non-empty string).

3. Record `status` value; allowed set for this audit: `accepted`, `superseded`, `deprecated` (lowercase). If any other token appears, normalize or document exception in 08-01-SUMMARY.

Compare README table row for that ADR: link target, title snippet, Status column, date column — must match file `status` and `date` (same calendar day as file unless README convention says otherwise).

Build a markdown table in scratch space for **08-01-SUMMARY.md** §Audit with columns: ADR id | file status | README status | match? (Y/N).

**Minimum spot-checks (must appear in SUMMARY):** rows 0000, 0009, 0018 — paste one line each from file + README proving parity.
  </action>
  <acceptance_criteria>
    - `08-01-SUMMARY.md` contains substring `| 0000 |` in an audit table
    - Same file contains `| 0009 |` and `| 0018 |`
    - Executor documents README ADR row count equals 19
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix README / ADR drift found in Task 1</name>
  <read_first>
    - Task 1 audit table in draft form
    - Every `docs/decisions/00NN-*.md` file flagged Y/N ≠ Y
  </read_first>
  <action>
For each mismatch from Task 1: edit **either** `docs/decisions/README.md` **or** the ADR file so Status and Date columns match front matter. Do not rewrite decision prose.

If ROADMAP wording (“Accepted” Title Case) is required for human readers, update README **display** column only while keeping file `status: accepted` — add a footnote in 08-01-SUMMARY: “README displays Title Case; files remain lowercase per 0000.”

If zero mismatches: state explicitly in 08-01-SUMMARY under §Audit: `No drift — 19/19 match`.
  </action>
  <acceptance_criteria>
    - `bun run lint` exits 0 after edits
    - For each edited file, `git diff` shows only intentional lines (no mass reformat unrelated to ADR audit)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add `scripts/validate-adr-frontmatter.ts`</name>
  <read_first>
    - `docs/decisions/0000-use-madr-for-adrs.md` — exact front matter key list
    - `scripts/coverage-gate.ts` — project script style (shebang, exit codes)
  </read_first>
  <action>
Create `scripts/validate-adr-frontmatter.ts` that:
- Accepts one or more file paths as CLI args.
- For each path: read UTF-8 text; require leading `---\n` YAML block closed by `\n---\n`.
- Parse keys: require **exact** keys `status`, `date`, `deciders`, `consulted`, `informed` (string values non-empty after trim).
- After front matter, require regex `/^# \d{4}\.\s+\S/m` on first heading line.
- Exit code `1` with `::error::` line on stderr for first failure (GitHub Actions annotation friendly).

Add `package.json` script: `"validate:adr": "bun run scripts/validate-adr-frontmatter.ts"`.

**Local negative test:** copy `0000-use-madr-for-adrs.md` to `/tmp/malformed.md`, delete the `deciders:` line, run `bun run scripts/validate-adr-frontmatter.ts /tmp/malformed.md` → exit 1. Do not commit `/tmp` file.

**Local positive test:** `bun run scripts/validate-adr-frontmatter.ts docs/decisions/0000-use-madr-for-adrs.md` → exit 0.
  </action>
  <acceptance_criteria>
    - `package.json` contains `"validate:adr":`
    - `scripts/validate-adr-frontmatter.ts` exists and `bun run validate:adr docs/decisions/0000-use-madr-for-adrs.md` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire validator into `.github/workflows/adr-check.yml`</name>
  <read_first>
    - `.github/workflows/adr-check.yml` — current jobs/steps
    - `scripts/validate-adr-frontmatter.ts` — CLI contract
  </read_first>
  <action>
After the existing step that verifies `added != 0` (new ADR files), insert **Bun toolchain** (mirror `ci.yml`): `oven-sh/setup-bun@v2` with `bun-version: 1.3.12`, then `bun install --frozen-lockfile`. These steps run **only when** `steps.adr-required.outputs.required == 'true'` (same `if` as the “new ADR added” block).

Then add a step **only when** `steps.adr-required.outputs.required == 'true'`:

1. Compute list of **added** paths: reuse the same `git diff ... --diff-filter=A ... docs/decisions/*.md` pattern as the existing check.
2. For each added path, run `bun run scripts/validate-adr-frontmatter.ts <path>`.

If any validation fails, step must exit non-zero.

Preserve existing behavior when ADR checkbox is **not** set (no new validation runs that would surprise unrelated PRs).

Document in 08-01-SUMMARY §CI the exact step name order (checkout → adr-required → conditional add check → conditional validate).
  </action>
  <acceptance_criteria>
    - `.github/workflows/adr-check.yml` contains substring `validate-adr-frontmatter` OR `validate:adr`
    - `.github/workflows/adr-check.yml` contains `oven-sh/setup-bun` (required before `bun run` on ubuntu-latest)
    - YAML indentation valid (visual review)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: ADR 0019 — document CI validation decision (or explicit no-new-ADR)</name>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 8 SC4
    - `docs/decisions/0000-use-madr-for-adrs.md` — MADR template
    - `docs/decisions/README.md` — index format
  </read_first>
  <action>
**If** extending `adr-check` is judged a new architectural decision for the harness:
- Add `docs/decisions/0019-adr-check-madr-validation.md` with full MADR front matter + sections (Context, Decision Drivers, Considered Options, Decision Outcome, Consequences).
- Add README index row `0019` with matching Status/日期/Supersedes.

**Else:** In `08-01-SUMMARY.md` §ADR 0019, write 2–4 sentences: why hardening is operational follow-through to 0000 and does not warrant a new ADR, and point to merged workflow commit for traceability.

Pick **one** branch — do not leave ambiguous.
  </action>
  <acceptance_criteria>
    - Either `docs/decisions/0019-adr-check-madr-validation.md` exists **or** `08-01-SUMMARY.md` contains heading `## ADR 0019 decision` with explicit no-new-ADR rationale
    - If 0019 file exists: `docs/decisions/README.md` contains `[0019](0019-`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Write `08-01-SUMMARY.md` and mark validation complete</name>
  <read_first>
    - `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-01-SUMMARY.md` — tone/sections (adapt, do not copy CI specifics)
  </read_first>
  <action>
Create `.planning/phases/08-adr-process-self-check/08-01-SUMMARY.md` containing:
- **Metadata:** date, branch name (if any), PR link (if opened), merge commit SHA to `main` for 08-01 work
- **§Audit:** table from Task 1 + drift fixes summary
- **§CI:** adr-check extension summary + file paths touched
- **§ADR 0019 decision:** pointer to file or no-new-ADR text
- **§Ready for 08-02:** explicit checklist: “main contains validator; sacrificial PR may proceed”

Do not claim ADR-06(a) complete — that is 08-02.
  </action>
  <acceptance_criteria>
    - File path `.planning/phases/08-adr-process-self-check/08-01-SUMMARY.md` exists
    - File contains `## Ready for 08-02` (exact heading)
  </acceptance_criteria>
</task>

</tasks>
