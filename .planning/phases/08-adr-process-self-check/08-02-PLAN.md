---
phase: 08-adr-process-self-check
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-adr-process-self-check/08-02-SUMMARY.md
autonomous: false
requirements:
  - ADR-06
tags: [adr-check, fail-mode, sacrificial-pr, github-actions, evidence]

must_haves:
  truths:
    - "Sacrificial PR has PR body with `- [x] This PR introduces a decision that requires a new ADR` (same string adr-check greps in `.github/workflows/adr-check.yml` line 19)"
    - "Sacrificial PR adds **at least one** new `docs/decisions/*.md` file that **fails** `scripts/validate-adr-frontmatter.ts` (e.g. missing `deciders` or invalid title line)"
    - "`adr-check` workflow run ends in **failure** for that PR; check run URL recorded in `08-02-SUMMARY.md`"
    - "PR state is **CLOSED** with **merged=false**; `main` unchanged by this PR"
    - "Optional: remote branch deleted — note in SUMMARY"
  artifacts:
    - path: ".planning/phases/08-adr-process-self-check/08-02-SUMMARY.md"
      provides: "ADR-06(a) shippable evidence"
      contains: "adr-check"
    - path: ".planning/phases/08-adr-process-self-check/08-02-SUMMARY.md"
      provides: "Check run URL"
      contains: "https://github.com"
---

<objective>
Collect **ADR-06(a)** evidence: one sacrificial PR proves `adr-check` **fails** when a new ADR is required but the added ADR is **malformed** (MADR front matter incomplete), mirroring Phase 6 fail-mode discipline (CONTEXT **D-03**).

Purpose: ROADMAP Phase 8 success criterion 1 + REQUIREMENTS ADR-06(a). Depends on **08-01** merged so validation exists on `main`.

Output:
- Closed-not-merged PR + `08-02-SUMMARY.md` with check run URL table (style aligned with `06-02-SUMMARY.md`)
</objective>

<threat_model>
## Assets
- Evidence integrity (URL must point at failing job for the sacrificial commit).

## Threats
- **T1 (low):** Wrong URL pasted (passing run or different workflow).
- **T2 (low):** PR merged by mistake — pollutes `main` with bad ADR.

## Mitigations
- SUMMARY table includes PR number, commit SHA, job name `adr-check`, and expected error substring from validator.
- Executor uses `[DO NOT MERGE]` in PR title; merge disabled via process check before close.
</threat_model>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-adr-process-self-check/08-CONTEXT.md
@.planning/phases/08-adr-process-self-check/08-RESEARCH.md
@.planning/phases/08-adr-process-self-check/08-01-SUMMARY.md
@.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-02-SUMMARY.md
@.github/workflows/adr-check.yml
@AGENTS.md
</context>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Confirm 08-01 on main — branch from current main</name>
  <read_first>
    - `.planning/phases/08-adr-process-self-check/08-01-SUMMARY.md` — merge commit SHA
    - `.github/workflows/adr-check.yml` — confirm validator step present
  </read_first>
  <action>
```bash
git checkout main
git pull origin main
git rev-parse HEAD
```
Verify HEAD matches (or is descendant of) 08-01 merge commit documented in 08-01-SUMMARY.

Create branch: `phase-8-adr-failmode-demo` (or name documented in 08-02-SUMMARY).
  </action>
  <acceptance_criteria>
    - `08-02-SUMMARY.md` records `base_sha:` 40-char hex matching `git rev-parse HEAD` before sacrificial commits
    - `git branch --show-current` matches branch name written in SUMMARY
  </acceptance_criteria>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Add intentionally malformed ADR + open sacrificial PR</name>
  <read_first>
    - `scripts/validate-adr-frontmatter.ts` — failure conditions
    - `.github/workflows/adr-check.yml` — PR body grep pattern for ADR requirement
  </read_first>
  <action>
1. Add a **new** file under `docs/decisions/` with a **non-colliding** numeric prefix (e.g. `0099-...`) with incomplete front matter so `bun run validate:adr <path>` exits 1 locally.

2. Commit message: `chore(ci): [DO NOT MERGE] demonstrate adr-check fail-mode`.

3. Push branch; open PR to `main` with title `[DO NOT MERGE] Phase 8 adr-check fail-mode`.

4. PR body **must** include exact checkbox line:
   ```
   - [x] This PR introduces a decision that requires a new ADR
   ```
   (matches workflow `grep` in `adr-check.yml`.)

5. Wait until `adr-check` job completes **failure** (`gh pr checks --watch` or GitHub UI).

6. Copy **HTML URL** of the failing check run into scratch for SUMMARY.

**Do not merge.**
  </action>
  <acceptance_criteria>
    - Local pre-push: `bun run validate:adr docs/decisions/<newfile>.md` exits 1
    - GitHub: `adr-check` conclusion is `FAILURE` (not `SKIPPED`)
    - `08-02-SUMMARY.md` contains a line starting with `https://github.com/` pointing to the failed run
  </acceptance_criteria>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Close PR + document evidence</name>
  <read_first>
    - `.planning/phases/06-ci-pipeline-green-run-smoke-validation/06-02-SUMMARY.md` — evidence table format
  </read_first>
  <action>
1. Close PR without merging.

2. Write `.planning/phases/08-adr-process-self-check/08-02-SUMMARY.md` with:
   - PR link (`https://github.com/<org>/<repo>/pull/<n>`)
   - Branch name
   - Table: | Step | Expected | Check URL | Result |
   - Row: adr-check | FAILURE | `<url>` | red (expected)

3. Delete remote branch if team policy allows; note in SUMMARY.

4. REQUIREMENTS ADR-06 checkbox update is a **human** milestone action unless PM delegates — executor may leave note in SUMMARY only.
  </action>
  <acceptance_criteria>
    - `gh pr view <n> --json state,mergedAt` shows merged=false
    - `08-02-SUMMARY.md` exists and contains `FAILURE` or `red` in the evidence table
  </acceptance_criteria>
</task>

</tasks>
