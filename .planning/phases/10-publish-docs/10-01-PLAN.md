---
phase: 10-publish-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-rigging/package.json
  - README.md
autonomous: true
requirements:
  - SCAF-02
  - SCAF-08

must_haves:
  truths:
    - "packages/create-rigging/package.json contains version 0.1.0"
    - "README.md opens with a ## Getting Started section before ## Why Rigging"
    - "Getting Started section's first instruction is npx create-rigging <project-name>"
    - "README.md What NOT Included section no longer mentions a scaffolding CLI as v2"
  artifacts:
    - path: "packages/create-rigging/package.json"
      provides: "Version 0.1.0 for npm publish"
      contains: "\"version\": \"0.1.0\""
    - path: "README.md"
      provides: "Getting Started section as first section after H1"
      contains: "## Getting Started"
  key_links:
    - from: "README.md ## Getting Started"
      to: "docs/quickstart.md"
      via: "markdown link"
      pattern: "\\[docs/quickstart\\.md\\]"
    - from: "README.md"
      to: "npx create-rigging"
      via: "code block in Getting Started"
      pattern: "npx create-rigging"
---

<objective>
Bump the create-rigging package version to 0.1.0 (D-01, SCAF-02) and update README.md with a Getting Started section as the first section (D-03, SCAF-08) plus removal of the stale "v2" scaffolding CLI disclaimer (D-04, SCAF-08).

Purpose: Sets version for public npm publish and ensures the README opens with `npx create-rigging` as the primary entry point — satisfying ROADMAP Phase 10 success criteria #3.
Output: Updated `packages/create-rigging/package.json` (version 0.1.0) and updated `README.md` (Getting Started first, What NOT Included cleaned up).
</objective>

<execution_context>
@/Users/carl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-publish-docs/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump version to 0.1.0 in packages/create-rigging/package.json</name>
  <files>packages/create-rigging/package.json</files>
  <read_first>
    - packages/create-rigging/package.json — read current content before editing (see current state in interfaces block below)
  </read_first>
  <action>
    Edit `packages/create-rigging/package.json`: change `"version": "0.0.1"` to `"version": "0.1.0"`. All other fields remain unchanged exactly as-is (name, description, type, bin, scripts, files, engines, license).

    Per D-01: version 0.1.0 communicates a meaningful first public release. The value 0.0.1 was a placeholder for internal-only use.

    Do NOT change any other field. Do NOT reformat the JSON. The resulting file must be valid JSON.
  </action>
  <verify>
    <automated>node -e "const p=require('./packages/create-rigging/package.json'); if(p.version!=='0.1.0') throw new Error('Expected 0.1.0 got '+p.version)"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/create-rigging/package.json` contains exactly `"version": "0.1.0"` (grep: `"version": "0.1.0"`)
    - All other fields are unchanged: `"name": "create-rigging"`, `"type": "commonjs"`, `"license": "MIT"`, `"prepublishOnly": "node ../../scripts/build-template.js"`
    - File is valid JSON (node -e "require('./packages/create-rigging/package.json')" exits 0)
  </acceptance_criteria>
  <done>packages/create-rigging/package.json has version 0.1.0 and is valid JSON with all other fields intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update README.md — add Getting Started section + remove stale v2 disclaimer</name>
  <files>README.md</files>
  <read_first>
    - README.md — read full file before editing; current section order: H1 → Why Rigging → Quickstart → Stack → What NOT Included → Architecture → Decisions → Contributing → License
  </read_first>
  <action>
    Make two edits to README.md per D-03 and D-04:

    **Edit 1 — Prepend `## Getting Started` as the very first section (after the H1 + tagline block, before `## Why Rigging`).**

    Insert the following block between the closing `>` blockquote line (line 3: `> Core Value: any Domain operation...`) and the blank line before `## Why Rigging`:

    ```markdown

    ## Getting Started

    ```bash
    npx create-rigging <project-name>
    ```

    Then:

    - `cd <project-name>` → `bun install` → `docker compose up -d` → `bun test`
    - See [docs/quickstart.md](docs/quickstart.md) for the full walkthrough (session auth, API Key, dogfood story)
    - **Prerequisites:** Node 18+, Bun 1.3+, Docker
    ```

    The `## Getting Started` section must appear BEFORE `## Why Rigging`. Do not alter `## Why Rigging` or any subsequent section.

    **Edit 2 — Remove the stale "v2" bullet from `## What NOT Included`.**

    Find and remove this exact bullet (currently line 34):
    ```
    - A scaffolding CLI (`npx create-rigging` is v2)
    ```

    Remove the entire line. The remaining bullets in `## What NOT Included` stay unchanged:
    - An OAuth / 2FA / Magic Link / Passkey provider...
    - A real email gateway...
    - A multi-tenant / RBAC framework...
    - An MCP / A2A protocol implementation...
    - A production deployment toolkit...

    Per D-03: Getting Started section must be first (before Why Rigging). Per D-04: the v2 disclaimer is no longer accurate now that create-rigging ships in v1.2.

    Keep all other sections (Why Rigging, Quickstart, Stack, Architecture, Decisions, Contributing, License) exactly as-is. Do not change wording, links, or formatting in any other section.
  </action>
  <verify>
    <automated>grep -n "## Getting Started" README.md && grep -n "## Why Rigging" README.md && node -e "const fs=require('fs');const c=fs.readFileSync('README.md','utf8');const gs=c.indexOf('## Getting Started');const wr=c.indexOf('## Why Rigging');if(gs===-1)throw new Error('Missing Getting Started');if(wr===-1)throw new Error('Missing Why Rigging');if(gs>wr)throw new Error('Getting Started must come before Why Rigging')" && ! grep -q "npx create-rigging.*is v2\|scaffolding CLI.*v2" README.md && grep -q "npx create-rigging <project-name>" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Getting Started" README.md` returns a line number LOWER than `grep -n "## Why Rigging" README.md`
    - README.md contains `npx create-rigging <project-name>` within the Getting Started section (grep: `npx create-rigging <project-name>`)
    - README.md contains `docker compose up -d` (two words, no hyphen) in the Getting Started section
    - README.md contains a link to `docs/quickstart.md` in the Getting Started section (grep: `\[docs/quickstart\.md\]`)
    - README.md contains `**Prerequisites:** Node 18+, Bun 1.3+, Docker` in Getting Started
    - The bullet `A scaffolding CLI (\`npx create-rigging\` is v2)` does NOT appear in README.md (grep returns non-zero)
    - `## Why Rigging`, `## Quickstart`, `## Stack`, `## What NOT Included`, `## Architecture`, `## Decisions`, `## Contributing`, `## License` all still present (6 remaining What NOT Included bullets intact)
  </acceptance_criteria>
  <done>README.md opens with ## Getting Started as first section; first instruction is `npx create-rigging &lt;project-name&gt;`; What NOT Included no longer contains the v2 scaffolding CLI disclaimer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → npm registry | npm publish sends package contents to public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | packages/create-rigging/package.json | accept | Version bump is reviewed locally before publish; no automated push |
| T-10-02 | Information Disclosure | README.md | accept | Public documentation; no secrets or PII in Getting Started section |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. Version is 0.1.0
node -e "const p=require('./packages/create-rigging/package.json'); console.log(p.version); if(p.version!=='0.1.0') process.exit(1)"

# 2. Getting Started is first section, before Why Rigging
grep -n "## Getting Started\|## Why Rigging" README.md

# 3. npx create-rigging in Getting Started
grep -A 10 "## Getting Started" README.md | grep "npx create-rigging"

# 4. No stale v2 disclaimer
! grep "is v2\|npx create-rigging.*v2" README.md
```
</verification>

<success_criteria>
1. `packages/create-rigging/package.json` has `"version": "0.1.0"` — ready for npm publish
2. `README.md` section order: H1 → Getting Started → Why Rigging → ... (Getting Started is FIRST section)
3. Getting Started section contains `npx create-rigging <project-name>` as its first code block
4. What NOT Included section no longer mentions scaffolding CLI as "v2"
</success_criteria>

<interfaces>
<!-- Current state of files being modified — executor reads these before editing -->

From packages/create-rigging/package.json (current):
```json
{
  "name": "create-rigging",
  "version": "0.0.1",
  "description": "Scaffold a new Rigging DDD project (Bun + Elysia + BetterAuth + Drizzle)",
  "type": "commonjs",
  "bin": {
    "create-rigging": "./bin/create-rigging.js"
  },
  "scripts": {
    "build-template": "node ../../scripts/build-template.js",
    "prepublishOnly": "node ../../scripts/build-template.js"
  },
  "files": [
    "bin/",
    "lib/",
    "template/"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
```

From README.md current section order:
- Line 1: # Rigging (H1 + tagline)
- Line 3: > Core Value blockquote
- Line 8: ## Why Rigging  ← Getting Started must be inserted BEFORE this
- Line 17: ## Quickstart
- Line 21: ## Stack
- Line 31: ## What NOT Included
- Line 34:   - A scaffolding CLI (`npx create-rigging` is v2)  ← REMOVE this line
- Line 44: ## Architecture
- Line 47: ## Decisions
- Line 51: ## Contributing
- Line 55: ## License
</interfaces>

<output>
After completion, create `.planning/phases/10-publish-docs/10-01-SUMMARY.md` using the summary template.
</output>
