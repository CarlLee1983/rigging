---
phase: 10-publish-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/quickstart.md
autonomous: true
requirements:
  - SCAF-08

must_haves:
  truths:
    - "docs/quickstart.md opens with scaffold invocation before any git clone instructions"
    - "Scaffold section contains npx create-rigging followed by the exact CLI next-steps sequence"
    - "Existing git clone path is preserved but demoted to a contributor section at the bottom"
    - "Path A / Path B curl flows remain intact and unchanged"
  artifacts:
    - path: "docs/quickstart.md"
      provides: "Scaffold-first developer onboarding path"
      contains: "## Scaffold"
  key_links:
    - from: "docs/quickstart.md ## Scaffold"
      to: "npx create-rigging CLI next-steps"
      via: "bash code block"
      pattern: "npx create-rigging"
    - from: "docs/quickstart.md"
      to: "Path A / Path B flows"
      via: "internal anchor link in Scaffold section"
      pattern: "Path A"
---

<objective>
Restructure docs/quickstart.md so the scaffold onboarding path (D-05, SCAF-08) appears at the top — before any environment setup steps — and the existing git clone walkthrough is demoted to a `## Developing Rigging Itself` section at the bottom (D-06, SCAF-08).

Purpose: Satisfies ROADMAP Phase 10 success criterion #4: "docs/quickstart.md reflects the scaffold-first workflow — scaffold invocation precedes any environment setup steps."
Output: Updated `docs/quickstart.md` with scaffold path first, git clone path last, all existing curl flow content preserved intact.
</objective>

<execution_context>
@/Users/carl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-publish-docs/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure docs/quickstart.md — scaffold-first, git clone demoted</name>
  <files>docs/quickstart.md</files>
  <read_first>
    - docs/quickstart.md — read full file before editing; current section order: title + intro → Prerequisites → Setup (2 min) → Dev server → Path A → Path B → What just happened → Error shape → Next steps
    - packages/create-rigging/bin/create-rigging.js lines 96-102 — canonical next-steps strings (SCAF-07 verbatim requirement)
  </read_first>
  <action>
    Restructure docs/quickstart.md to produce the following section order. Read the full current file first, then rewrite it with the changes below.

    **Target section order:**
    1. Title + intro paragraph (lines 1-5) — UNCHANGED verbatim
    2. `## Prerequisites` (current lines 7-12) — UNCHANGED verbatim
    3. `## Scaffold (fastest path)` — NEW section, insert here (see content below)
    4. `## Dev server (30 sec)` (current lines 27-40) — UNCHANGED verbatim
    5. `## Path A — Human session (3 min)` (current lines 42-82) — UNCHANGED verbatim
    6. `## Path B — Create + read your own Agent (2 min, dogfood story)` (current lines 84-123) — UNCHANGED verbatim
    7. `## What just happened (1 min)` (current lines 125-134) — UNCHANGED verbatim
    8. `## Error shape — one example` (current lines 136-147) — UNCHANGED verbatim
    9. `## Next steps` (current lines 149-154) — UNCHANGED verbatim
    10. `## Developing Rigging Itself` — NEW section replacing the old `## Setup (2 min)` (see content below)
    11. Footer `---` + italics paragraph (current lines 156-159) — UNCHANGED verbatim

    **New `## Scaffold (fastest path)` section content (insert after Prerequisites, before Dev server):**

    ```markdown
    ## Scaffold (fastest path)

    New project? Use the CLI — one command, no clone needed:

    ```bash
    npx create-rigging <project-name>
    cd <project-name>
    bun install
    docker compose up -d
    bun test
    ```

    That's it. The scaffold includes a working `.env.example`, migrations, and CI workflow.
    See the [Path A / Path B curl flows](#path-a--human-session-3-min) below to dogfood the full Rigging surface.
    ```

    **New `## Developing Rigging Itself` section content (append at bottom, before footer):**

    ```markdown
    ## Developing Rigging Itself

    Contributing to Rigging or working on the scaffold source? Clone the reference app directly:

    ```bash
    git clone <this-repo> rigging && cd rigging
    cp .env.example .env
    docker-compose up -d
    bun install
    bun run db:migrate
    ```

    If `docker-compose up -d` fails with port conflict on 5432, stop the conflicting service or change `DATABASE_URL` in `.env` to a free port.
    ```

    **Critical constraints:**
    - The scaffold code block command sequence MUST be exactly (verbatim from SCAF-07 CLI output):
      1. `npx create-rigging <project-name>`
      2. `cd <project-name>`
      3. `bun install`
      4. `docker compose up -d`  (TWO WORDS — no hyphen — this is different from the old git clone path which uses `docker-compose`)
      5. `bun test`
    - The OLD `## Setup (2 min)` section is REMOVED from its current position (between Prerequisites and Dev server). Its content moves into `## Developing Rigging Itself` at the bottom.
    - Do NOT change any content in: Dev server, Path A, Path B, What just happened, Error shape, Next steps.
    - Preserve the `docker-compose` (hyphenated) in `## Developing Rigging Itself` — that section uses the old git clone workflow commands unchanged.
    - The internal anchor link `#path-a--human-session-3-min` in the Scaffold section must match the actual heading text of Path A.
  </action>
  <verify>
    <automated>
      # Scaffold section exists and precedes Dev server
      node -e "
        const fs = require('fs');
        const c = fs.readFileSync('docs/quickstart.md', 'utf8');
        const scaffold = c.indexOf('## Scaffold');
        const devserver = c.indexOf('## Dev server');
        const developing = c.indexOf('## Developing Rigging Itself');
        const setup = c.indexOf('## Setup (2 min)');
        if (scaffold === -1) throw new Error('Missing ## Scaffold section');
        if (devserver === -1) throw new Error('Missing ## Dev server section');
        if (developing === -1) throw new Error('Missing ## Developing Rigging Itself section');
        if (setup !== -1) throw new Error('Old ## Setup (2 min) must be removed');
        if (scaffold > devserver) throw new Error('Scaffold must precede Dev server');
        if (developing < devserver) throw new Error('Developing Rigging Itself must come after Dev server');
        console.log('Structure OK: scaffold=' + scaffold + ' devserver=' + devserver + ' developing=' + developing);
      "
      # Verbatim CLI next-steps in scaffold block
      grep -A 8 "## Scaffold" docs/quickstart.md | grep "docker compose up -d" && echo "docker compose (no hyphen) OK"
      grep -A 8 "## Scaffold" docs/quickstart.md | grep "bun test" && echo "bun test OK"
      # Path A and Path B intact
      grep -c "## Path A" docs/quickstart.md && grep -c "## Path B" docs/quickstart.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Scaffold" docs/quickstart.md` returns a line number present in the file
    - `grep -n "## Scaffold\|## Dev server\|## Developing Rigging Itself" docs/quickstart.md` shows Scaffold line number LESS THAN Dev server line number, and Developing Rigging Itself line number GREATER THAN Dev server line number
    - The Scaffold section code block contains `npx create-rigging <project-name>` (grep: `npx create-rigging`)
    - The Scaffold section code block contains `docker compose up -d` with NO hyphen in "compose" (grep: `docker compose up -d`)
    - The Scaffold section code block does NOT contain `docker-compose` (hyphenated)
    - `## Setup (2 min)` does NOT appear in the file (grep returns non-zero exit code)
    - `## Developing Rigging Itself` section contains `git clone` (grep: `git clone`)
    - `## Developing Rigging Itself` section contains `docker-compose up -d` (hyphenated — old contributor path)
    - `## Path A — Human session` still present and content unchanged (grep: `## Path A`)
    - `## Path B — Create + read` still present (grep: `## Path B`)
    - `## What just happened` still present (grep: `## What just happened`)
    - `## Error shape` still present (grep: `## Error shape`)
    - `## Next steps` still present (grep: `## Next steps`)
    - `## Prerequisites` still present (grep: `## Prerequisites`)
  </acceptance_criteria>
  <done>docs/quickstart.md has scaffold path as first setup section; git clone path moved to ## Developing Rigging Itself at bottom; all curl flow sections intact and unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs content → developer | Documentation read by developers following instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | docs/quickstart.md | accept | Documentation-only change; reviewed via git diff before commit |
| T-10-04 | Information Disclosure | docs/quickstart.md | accept | Public documentation; no secrets; curl examples use placeholder values |
</threat_model>

<verification>
After task completes:

```bash
# 1. Scaffold section exists and is before Dev server
grep -n "## Scaffold\|## Dev server\|## Developing Rigging Itself\|## Setup" docs/quickstart.md

# 2. Scaffold code block has exact CLI sequence (no hyphen in compose)
grep -A 8 "## Scaffold (fastest path)" docs/quickstart.md

# 3. Old Setup section removed
! grep -q "## Setup (2 min)" docs/quickstart.md && echo "Setup (2 min) removed OK"

# 4. Path A/B preserved
grep "## Path A\|## Path B\|## What just happened\|## Error shape\|## Next steps" docs/quickstart.md
```
</verification>

<success_criteria>
1. docs/quickstart.md section order: title → Prerequisites → Scaffold → Dev server → Path A → Path B → What just happened → Error shape → Next steps → Developing Rigging Itself → footer
2. Scaffold section code block commands are verbatim from CLI: `npx create-rigging <project-name>` / `cd <project-name>` / `bun install` / `docker compose up -d` / `bun test`
3. `## Setup (2 min)` section no longer exists
4. `## Developing Rigging Itself` section contains the git clone contributor path
5. All Path A, Path B, and remaining sections unchanged
</success_criteria>

<interfaces>
<!-- Current docs/quickstart.md section order for reference -->

Current section order (before edits):
1. # Rigging Quickstart (title + intro, lines 1-5)
2. ## Prerequisites (lines 7-12)
3. ## Setup (2 min) (lines 14-24)  ← REMOVE from here, content becomes ## Developing Rigging Itself at bottom
4. ## Dev server (30 sec) (lines 27-40)
5. ## Path A — Human session (3 min) (lines 42-82)
6. ## Path B — Create + read your own Agent (2 min, dogfood story) (lines 84-123)
7. ## What just happened (1 min) (lines 125-134)
8. ## Error shape — one example (lines 136-147)
9. ## Next steps (lines 149-154)
10. --- footer (lines 156-159)

CLI next-steps verbatim (from packages/create-rigging/bin/create-rigging.js lines 96-102):
  cd <project-name>
  bun install
  docker compose up -d    ← two words, no hyphen
  bun test

Note: existing Setup (2 min) uses `docker-compose up -d` (hyphenated, legacy Docker Compose v1).
The new Scaffold section MUST use `docker compose up -d` (no hyphen, Docker Compose v2) to match CLI output.
The Developing Rigging Itself section keeps `docker-compose` (hyphenated) since it's the unchanged contributor path.
</interfaces>

<output>
After completion, create `.planning/phases/10-publish-docs/10-02-SUMMARY.md` using the summary template.
</output>
