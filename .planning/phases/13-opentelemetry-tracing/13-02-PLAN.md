---
phase: 13-opentelemetry-tracing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/bootstrap/config.ts
  - src/bootstrap/otel-init.ts
  - .env.example
autonomous: true
requirements:
  - PROD-03

must_haves:
  truths:
    - "bun add 安裝了 3 個 OTel 套件（exact pin，無 caret）"
    - "ConfigSchema 包含 OTEL_EXPORTER_OTLP_ENDPOINT 為 Optional URI"
    - "initTracing(endpoint?) 函式存在，endpoint 有值時建立帶 OTLP exporter 的 provider，沒有值時建立 no-op provider"
    - "exactOptionalPropertyTypes 條件建構（兩個 NodeTracerProvider 分支，不傳 undefined）"
    - "bun run typecheck 退出碼 0"
  artifacts:
    - path: "src/bootstrap/otel-init.ts"
      provides: "TracerProvider 初始化工廠函式"
      exports: ["initTracing"]
      contains: "export function initTracing"
    - path: "src/bootstrap/config.ts"
      provides: "OTEL_EXPORTER_OTLP_ENDPOINT 環境變數 schema"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
  key_links:
    - from: "src/bootstrap/otel-init.ts"
      to: "@opentelemetry/sdk-trace-node"
      via: "NodeTracerProvider import"
      pattern: "NodeTracerProvider"
    - from: "src/bootstrap/otel-init.ts"
      to: "@opentelemetry/exporter-trace-otlp-http"
      via: "條件 OTLPTraceExporter 建立"
      pattern: "OTLPTraceExporter"
---

<objective>
安裝 OTel 套件（exact pin）、擴充 ConfigSchema、建立 `otel-init.ts` TracerProvider 工廠函式。

Purpose: 建立 OTel 基礎設施層，讓後續 plugin（Plan 03）和測試（Plan 04）可以使用。`initTracing` 是整個系統的入口點——有 endpoint 時 export spans，無 endpoint 時靜默 no-op。

Output:
- `package.json` — 3 個 OTel 套件 exact pin
- `src/bootstrap/config.ts` — `OTEL_EXPORTER_OTLP_ENDPOINT` Optional URI 欄位
- `src/bootstrap/otel-init.ts` — `initTracing(endpoint?)` factory function
- `.env.example` — `OTEL_EXPORTER_OTLP_ENDPOINT` 說明
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-opentelemetry-tracing/13-CONTEXT.md
@.planning/phases/13-opentelemetry-tracing/13-RESEARCH.md
@.planning/phases/13-opentelemetry-tracing/13-PATTERNS.md

<interfaces>
<!-- 現有 config.ts Optional 模式（lines 33-35）-->
```typescript
REDIS_URL: Type.Optional(Type.String({ pattern: '^(redis|rediss)://.+' })),
RESEND_API_KEY: Type.Optional(Type.String()),
RESEND_FROM_ADDRESS: Type.Optional(Type.String({ format: 'email' })),
```
<!-- FormatRegistry.Set('uri', ...) 已在 config.ts lines 4-10 定義，可直接用 format: 'uri' -->

<!-- app.ts exactOptionalPropertyTypes 條件展開模式（lines 63-66）-->
```typescript
const authDeps: AuthModuleDeps = {
  db,
  logger,
  config,
  ...(redis && { redis }),
  ...(deps.authInstance && { authInstance: deps.authInstance }),
}
```

<!-- redis/client.ts 基礎設施 factory 模式（完整簽名）-->
```typescript
export function createRedisClient(url: string, logger: Logger): Redis {
  const client = new Redis(url, { ... })
  // ...event listeners...
  return client
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 安裝 OTel 套件 + 擴充 config.ts</name>
  <files>package.json, src/bootstrap/config.ts, .env.example</files>
  <read_first>
    - package.json（確認現有 exact pin 格式，如 "ioredis": "5.6.1"，仿此加入 OTel）
    - src/bootstrap/config.ts（完整讀取，了解 FormatRegistry 和現有 Optional 欄位）
    - .env.example（了解現有格式，新增 OTEL_EXPORTER_OTLP_ENDPOINT 說明）
  </read_first>
  <behavior>
    - config.ts 的 ConfigSchema 包含 `OTEL_EXPORTER_OTLP_ENDPOINT: Type.Optional(Type.String({ format: 'uri' }))`
    - loadConfig() 在 OTEL_EXPORTER_OTLP_ENDPOINT 設為無效 URI（如 "not-a-url"）時拋出錯誤
    - loadConfig() 在 OTEL_EXPORTER_OTLP_ENDPOINT 未設定時成功（Optional）
    - package.json dependencies 包含 @opentelemetry/sdk-trace-node、exporter-trace-otlp-http、resources（exact pin，無 caret）
  </behavior>
  <action>
**Step 1: 安裝套件（exact pin，無 caret）**

執行：
```bash
bun add @opentelemetry/sdk-trace-node@2.7.0 @opentelemetry/exporter-trace-otlp-http@0.215.0 @opentelemetry/resources@2.7.0 @opentelemetry/semantic-conventions@1.40.0
```

安裝後確認 package.json 中版本號無 `^`。若 bun add 自動加了 caret，手動移除使其與 ioredis `"5.6.1"` 格式一致。

注意：`@opentelemetry/sdk-trace-base@2.7.0` 是 `sdk-trace-node` 的傳遞依賴，不需要明確列在 package.json。

**Step 2: 修改 config.ts**

在 `RESEND_FROM_ADDRESS` 行後方加入：
```typescript
OTEL_EXPORTER_OTLP_ENDPOINT: Type.Optional(Type.String({ format: 'uri' })),
```

`format: 'uri'` 使用已有的 `FormatRegistry.Set('uri', ...)` validator（lines 4-10），不需新增程式碼。

**Step 3: 更新 .env.example**

在 Redis 區塊後加入 OTel 區塊：
```
# OpenTelemetry (optional)
# Set to your OTLP collector endpoint to enable trace export.
# Leave unset for no-op (no spans exported).
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
```
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@opentelemetry/sdk-trace-node": "2.7.0"' package.json` 輸出符合（無 caret）
    - `grep '"@opentelemetry/exporter-trace-otlp-http": "0.215.0"' package.json` 輸出符合（無 caret）
    - `grep '"@opentelemetry/resources": "2.7.0"' package.json` 輸出符合（無 caret）
    - `grep '"@opentelemetry/semantic-conventions": "1.40.0"' package.json` 輸出符合（無 caret）
    - `grep "OTEL_EXPORTER_OTLP_ENDPOINT" src/bootstrap/config.ts` 輸出符合
    - `grep "format: 'uri'" src/bootstrap/config.ts` 輸出符合（至少一個，BETTER_AUTH_URL 已有）
    - `grep "OTEL_EXPORTER_OTLP_ENDPOINT" .env.example` 輸出符合
    - `bun run typecheck` 退出碼 0
  </acceptance_criteria>
  <done>3 個 OTel 套件安裝成功（exact pin），ConfigSchema 包含 OTEL_EXPORTER_OTLP_ENDPOINT Optional URI，.env.example 有說明，typecheck 通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 src/bootstrap/otel-init.ts</name>
  <files>src/bootstrap/otel-init.ts</files>
  <read_first>
    - src/shared/infrastructure/redis/client.ts（infrastructure factory 模式 analog）
    - src/bootstrap/app.ts（exactOptionalPropertyTypes 條件建構模式，lines 57-66）
    - .planning/phases/13-opentelemetry-tracing/13-RESEARCH.md（Pattern 1 完整 otel-init.ts 參考實作）
    - .planning/phases/13-opentelemetry-tracing/13-PATTERNS.md（Pattern Assignments for otel-init.ts）
  </read_first>
  <behavior>
    - `initTracing(undefined)` 建立 NodeTracerProvider（無 spanProcessors）並呼叫 provider.register()，回傳 provider
    - `initTracing('http://localhost:4318')` 建立帶 SimpleSpanProcessor + OTLPTraceExporter 的 provider
    - OTLPTraceExporter url 為 `${endpoint}/v1/traces`（不是直接用 endpoint）
    - `resourceFromAttributes` 呼叫設定 service.name = 'rigging'
    - 使用 ATTR_SERVICE_NAME 常數而非硬碼字串 'service.name'
    - 不使用已棄用的 API：不用 `new Resource()`、不用 `provider.addSpanProcessor()`
    - TypeScript `exactOptionalPropertyTypes: true`：兩個 provider 分支，endpoint 有值才傳 spanProcessors
  </behavior>
  <action>
建立 `src/bootstrap/otel-init.ts`，完整實作如下（per D-01、D-02）：

```typescript
import { NodeTracerProvider } from '@opentelemetry/sdk-trace-node'
import { SimpleSpanProcessor } from '@opentelemetry/sdk-trace-base'
import { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-http'
import { resourceFromAttributes } from '@opentelemetry/resources'
import { ATTR_SERVICE_NAME } from '@opentelemetry/semantic-conventions'

/**
 * initTracing — bootstrap-time TracerProvider factory.
 *
 * Called from main.ts AFTER loadConfig() but BEFORE createApp().
 * When endpoint is provided: attaches OTLPTraceExporter (HTTP/JSON to /v1/traces).
 * When endpoint is undefined: provider still registers as global provider, but no exporter
 * is attached — spans are silently dropped (no-op behavior, PROD-03 success criterion #5).
 *
 * OTel SDK 2.x API notes:
 *   - resourceFromAttributes() replaces new Resource() (removed in 2.x)
 *   - spanProcessors constructor option replaces addSpanProcessor() (removed in 2.x)
 *   - SimpleSpanProcessor instead of BatchSpanProcessor (Bun timeout issue #5260)
 *   - exactOptionalPropertyTypes: two conditional branches, never pass undefined
 */
export function initTracing(endpoint?: string): NodeTracerProvider {
  const resource = resourceFromAttributes({
    [ATTR_SERVICE_NAME]: 'rigging',
  })

  // exactOptionalPropertyTypes-safe: conditional construction, do not pass undefined
  const provider = endpoint
    ? new NodeTracerProvider({
        resource,
        spanProcessors: [
          new SimpleSpanProcessor(
            new OTLPTraceExporter({ url: `${endpoint}/v1/traces` }),
          ),
        ],
      })
    : new NodeTracerProvider({ resource })

  provider.register()
  return provider
}
```

檔案應在 50 行以內（per CLAUDE.md file size guideline）。
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/bootstrap/otel-init.ts` 存在
    - `grep "export function initTracing" src/bootstrap/otel-init.ts` 輸出符合
    - `grep "resourceFromAttributes" src/bootstrap/otel-init.ts` 輸出符合（使用 2.x API，不用 new Resource）
    - `grep "NodeTracerProvider" src/bootstrap/otel-init.ts` 輸出符合
    - `grep "spanProcessors" src/bootstrap/otel-init.ts` 輸出符合（constructor 選項，不用 addSpanProcessor）
    - `grep "SimpleSpanProcessor" src/bootstrap/otel-init.ts` 輸出符合（不用 BatchSpanProcessor）
    - `grep "OTLPTraceExporter" src/bootstrap/otel-init.ts` 輸出符合
    - `grep "v1/traces" src/bootstrap/otel-init.ts` 輸出符合（URL 加了 /v1/traces）
    - `grep "addSpanProcessor" src/bootstrap/otel-init.ts` 無輸出（禁止使用舊 API）
    - `bun run typecheck` 退出碼 0
  </acceptance_criteria>
  <done>otel-init.ts 存在並匯出 initTracing(endpoint?) factory function；使用 SDK 2.x API；exactOptionalPropertyTypes 安全；typecheck 通過</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → ConfigSchema | OTEL_EXPORTER_OTLP_ENDPOINT 經 TypeBox uri format 驗證再進入應用程式 |
| app → OTLP endpoint | initTracing 直接連接外部 endpoint，屬 SSRF 風險邊界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Spoofing | OTLP exporter endpoint | accept | endpoint 來自 env var（ops-controlled），非 user-controlled input；infrastructure 設定範疇 |
| T-13-02-02 | Tampering | ConfigSchema validation | mitigate | TypeBox `format: 'uri'` 驗證確保 OTEL_EXPORTER_OTLP_ENDPOINT 是合法 URI，啟動時 fail-fast |
| T-13-02-03 | Information Disclosure | OTLP endpoint URL logging | mitigate | otel-init.ts 不 log endpoint URL（不含 console.log 或 pino 呼叫）；endpoint 僅傳給 OTLPTraceExporter |
| T-13-02-04 | Elevation of Privilege | SSRF via OTLP endpoint | accept | endpoint 由 operator 設定（infra config），非 user input；SSRF 風險屬 ops 責任範疇；不做 URL allowlist（過度工程） |
</threat_model>

<verification>
```bash
# 套件安裝確認（無 caret）
grep -E "@opentelemetry/(sdk-trace-node|exporter-trace-otlp-http|resources|semantic-conventions)" package.json

# ConfigSchema 擴充確認
grep "OTEL_EXPORTER_OTLP_ENDPOINT" src/bootstrap/config.ts

# otel-init.ts 核心 API 確認
grep -E "resourceFromAttributes|NodeTracerProvider|SimpleSpanProcessor|OTLPTraceExporter|provider\.register" src/bootstrap/otel-init.ts

# 禁止使用舊 API
grep "addSpanProcessor\|new Resource(" src/bootstrap/otel-init.ts  # 應無輸出

# 型別檢查
bun run typecheck
```
</verification>

<success_criteria>
- 3 個 OTel 套件安裝成功（exact pin，無 caret），bun.lock 更新
- `src/bootstrap/config.ts` 包含 `OTEL_EXPORTER_OTLP_ENDPOINT: Type.Optional(Type.String({ format: 'uri' }))`
- `src/bootstrap/otel-init.ts` 匯出 `initTracing(endpoint?: string): NodeTracerProvider`
- 使用 SDK 2.x API（resourceFromAttributes、constructor spanProcessors、SimpleSpanProcessor）
- exactOptionalPropertyTypes 安全（兩個分支，不傳 undefined）
- `bun run typecheck` 退出碼 0
</success_criteria>

<output>
完成後建立 `.planning/phases/13-opentelemetry-tracing/13-02-SUMMARY.md`
</output>
