---
phase: 13-opentelemetry-tracing
plan: 03
type: execute
wave: 2
depends_on:
  - 13-02
files_modified:
  - src/shared/presentation/plugins/tracing.plugin.ts
  - src/bootstrap/app.ts
  - src/main.ts
autonomous: true
requirements:
  - PROD-03

must_haves:
  truths:
    - "tracingPlugin() 函式存在並可 .use() 掛入 Elysia app"
    - "每個 HTTP request 在 onBeforeHandle 建立 span，在 onAfterHandle 或 onError 結束 span"
    - "onError hook 獨立結束 span（不依賴 onAfterHandle）"
    - "app.ts 的 plugin 順序符合 ADR 0012：tracingPlugin 在 swaggerPlugin 後、feature modules 前"
    - "main.ts 在 createApp 前呼叫 initTracing(config.OTEL_EXPORTER_OTLP_ENDPOINT)"
    - "bun run typecheck 退出碼 0"
  artifacts:
    - path: "src/shared/presentation/plugins/tracing.plugin.ts"
      provides: "HTTP span lifecycle plugin"
      exports: ["tracingPlugin"]
      contains: "export function tracingPlugin"
    - path: "src/bootstrap/app.ts"
      provides: "plugin wiring（tracingPlugin 已加入）"
      contains: "tracingPlugin"
    - path: "src/main.ts"
      provides: "entrypoint 呼叫 initTracing"
      contains: "initTracing"
  key_links:
    - from: "src/main.ts"
      to: "src/bootstrap/otel-init.ts"
      via: "import + initTracing(config.OTEL_EXPORTER_OTLP_ENDPOINT) 呼叫"
      pattern: "initTracing"
    - from: "src/bootstrap/app.ts"
      to: "src/shared/presentation/plugins/tracing.plugin.ts"
      via: ".use(tracingPlugin())"
      pattern: "tracingPlugin"
    - from: "tracing.plugin.ts onBeforeHandle"
      to: "tracing.plugin.ts onAfterHandle/onError"
      via: "derive _span 在 context 中傳遞"
      pattern: "_span"
---

<objective>
建立 `tracing.plugin.ts` HTTP span lifecycle plugin，並將其 wire 進 `app.ts` 和 `main.ts`。

Purpose: 實現 PROD-03 成功標準 #1～#4 的核心功能。plugin 捕捉每個 request 的 span（含 route、method、status、latency），error path 獨立處理（onError hook），`main.ts` 保證 initTracing 在 createApp 前執行。

Output:
- `src/shared/presentation/plugins/tracing.plugin.ts` — HTTP span plugin
- `src/bootstrap/app.ts` — 加入 .use(tracingPlugin())
- `src/main.ts` — 加入 initTracing 呼叫
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-opentelemetry-tracing/13-CONTEXT.md
@.planning/phases/13-opentelemetry-tracing/13-RESEARCH.md
@.planning/phases/13-opentelemetry-tracing/13-PATTERNS.md
@.planning/phases/13-opentelemetry-tracing/13-02-SUMMARY.md

<interfaces>
<!-- request-logger.plugin.ts 完整結構（tracing.plugin.ts 的 exact analog）-->
<!-- From src/shared/presentation/plugins/request-logger.plugin.ts -->
```typescript
export function requestLoggerPlugin(logger: Logger) {
  return (
    new Elysia({ name: 'rigging/request-logger' })
      .decorate('log', logger)
      .derive({ as: 'global' }, ({ request, set }) => {
        // ...per-request derived values...
        return { requestId, startedAt: performance.now() }
      })
      .onAfterResponse({ as: 'global' }, (ctx) => {
        const status =
          typeof ctx.set.status === 'number' ? ctx.set.status : Number(ctx.set.status ?? 200)
        // ...
      })
  )
}
```

<!-- app.ts 現有 plugin chain（lines 68-90）-->
```typescript
const app = new Elysia({ name: 'rigging/app' })
  .use(requestLoggerPlugin(logger))
  .use(corsPlugin())
  .use(errorHandlerPlugin(logger))
  .use(swaggerPlugin())
  // ← tracingPlugin() 加在這裡（ADR 0012 位置 4.5）

if (config.NODE_ENV !== 'test') {
  if (redis) { app.use(rateLimit({ context: new RedisRateLimitContext(redis) })) }
  else { app.use(rateLimit({})) }
}

return app
  .use(createAuthModule(authDeps))
  .use(createAgentsModule({ db, logger }))
  .use(createHealthModule(healthDeps))
```

<!-- main.ts 現有結構 -->
```typescript
import { createApp } from './bootstrap/app'
import { loadConfig } from './bootstrap/config'

const config = loadConfig()
const app = createApp(config)
app.listen(config.PORT, ({ hostname, port }) => { ... })
```

<!-- initTracing 簽名（從 otel-init.ts，Plan 02 建立）-->
```typescript
export function initTracing(endpoint?: string): NodeTracerProvider
```

<!-- ATTR_* 常數（semantic-conventions 1.40）-->
```typescript
import { ATTR_HTTP_REQUEST_METHOD, ATTR_HTTP_RESPONSE_STATUS_CODE, ATTR_HTTP_ROUTE } from '@opentelemetry/semantic-conventions'
// ATTR_HTTP_REQUEST_METHOD → 字串值 'http.request.method'
// ATTR_HTTP_RESPONSE_STATUS_CODE → 字串值 'http.response.status_code'
// ATTR_HTTP_ROUTE → 字串值 'http.route'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 tracing.plugin.ts（HTTP span lifecycle plugin）</name>
  <files>src/shared/presentation/plugins/tracing.plugin.ts</files>
  <read_first>
    - src/shared/presentation/plugins/request-logger.plugin.ts（exact analog — derive + lifecycle hook 模式）
    - src/shared/presentation/plugins/error-handler.plugin.ts（onError cross-plugin context 存取模式）
    - .planning/phases/13-opentelemetry-tracing/13-RESEARCH.md（Pattern 2 完整 tracing.plugin.ts 參考實作）
    - .planning/phases/13-opentelemetry-tracing/13-PATTERNS.md（Pattern Assignments for tracing.plugin.ts）
  </read_first>
  <behavior>
    - tracingPlugin() 回傳 Elysia instance，name 為 'rigging/tracing'
    - .derive({ as: 'global' }) 建立 _span (Span | null) 和 _spanStart (number) per-request context
    - onBeforeHandle({ as: 'global' }) 建立 span，設定 ATTR_HTTP_REQUEST_METHOD 和 ATTR_HTTP_ROUTE，儲存到 ctx._span 和 ctx._spanStart
    - onAfterHandle({ as: 'global' }) 設定 ATTR_HTTP_RESPONSE_STATUS_CODE 和 'http.request.duration'，2xx/3xx 設 SpanStatusCode.OK，4xx/5xx 設 SpanStatusCode.ERROR，呼叫 span.end()
    - onError({ as: 'global' }) 獨立處理：設定 ATTR_HTTP_RESPONSE_STATUS_CODE 和 duration，設 SpanStatusCode.ERROR，呼叫 span.end()（不依賴 onAfterHandle）
    - span name 格式：`${ctx.request.method} ${ctx.route}`（如 "GET /health"）
    - 使用 ATTR_* 常數，不用硬碼字串或 SEMATTRS_* deprecated 常數
    - 使用 status 型別轉換模式（同 request-logger.plugin.ts）
    - 若 _span 為 null（onAfterHandle/onError 早於 onBeforeHandle 執行）提前 return（防禦性）
  </behavior>
  <action>
建立 `src/shared/presentation/plugins/tracing.plugin.ts`，完整實作：

```typescript
import { Elysia } from 'elysia'
import { trace, SpanStatusCode, SpanKind } from '@opentelemetry/api'
import {
  ATTR_HTTP_REQUEST_METHOD,
  ATTR_HTTP_RESPONSE_STATUS_CODE,
  ATTR_HTTP_ROUTE,
} from '@opentelemetry/semantic-conventions'

/**
 * tracingPlugin — Global HTTP tracing plugin (PROD-03, D-03).
 *
 * Records one OTel span per HTTP request with:
 *   - http.request.method   (ATTR_HTTP_REQUEST_METHOD)
 *   - http.route            (ATTR_HTTP_ROUTE — parametrized template, e.g. /api/agents/:id)
 *   - http.response.status_code (ATTR_HTTP_RESPONSE_STATUS_CODE)
 *   - http.request.duration (latency in ms)
 *
 * IMPORTANT: onError must end the span independently — onAfterHandle does NOT execute
 * when onError fires (Elysia lifecycle guarantee).
 *
 * WHY onBeforeHandle (not onRequest): context.route is only available from onBeforeHandle
 * onwards — PreContext (onRequest) does not have the `route` property.
 * [VERIFIED: Elysia dist/context.d.ts]
 */
export function tracingPlugin() {
  const tracer = trace.getTracer('rigging/http')

  return new Elysia({ name: 'rigging/tracing' })
    .derive({ as: 'global' }, () => ({
      _span: null as ReturnType<typeof tracer.startSpan> | null,
      _spanStart: 0,
    }))
    .onBeforeHandle({ as: 'global' }, (ctx) => {
      const span = tracer.startSpan(`${ctx.request.method} ${ctx.route}`, {
        kind: SpanKind.SERVER,
        attributes: {
          [ATTR_HTTP_REQUEST_METHOD]: ctx.request.method,
          [ATTR_HTTP_ROUTE]: ctx.route,
        },
      })
      ctx._span = span
      ctx._spanStart = performance.now()
    })
    .onAfterHandle({ as: 'global' }, (ctx) => {
      const span = ctx._span
      if (!span) return
      const status =
        typeof ctx.set.status === 'number' ? ctx.set.status : Number(ctx.set.status ?? 200)
      const durationMs = Math.round(performance.now() - ctx._spanStart)
      span.setAttribute(ATTR_HTTP_RESPONSE_STATUS_CODE, status)
      span.setAttribute('http.request.duration', durationMs)
      if (status >= 400) {
        span.setStatus({ code: SpanStatusCode.ERROR })
      } else {
        span.setStatus({ code: SpanStatusCode.OK })
      }
      span.end()
    })
    .onError({ as: 'global' }, (ctx) => {
      const span = ctx._span
      if (!span) return
      const status =
        typeof ctx.set.status === 'number' ? ctx.set.status : Number(ctx.set.status ?? 500)
      const durationMs = Math.round(performance.now() - ctx._spanStart)
      span.setAttribute(ATTR_HTTP_RESPONSE_STATUS_CODE, status)
      span.setAttribute('http.request.duration', durationMs)
      span.setStatus({ code: SpanStatusCode.ERROR, message: ctx.error?.toString() })
      span.end()
    })
}
```

檔案應在 80 行以內（per CLAUDE.md file size guideline）。
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/presentation/plugins/tracing.plugin.ts` 存在
    - `grep "export function tracingPlugin" src/shared/presentation/plugins/tracing.plugin.ts` 輸出符合
    - `grep "as: 'global'" src/shared/presentation/plugins/tracing.plugin.ts` 至少 4 個（derive、onBeforeHandle、onAfterHandle、onError）
    - `grep "ATTR_HTTP_REQUEST_METHOD\|ATTR_HTTP_RESPONSE_STATUS_CODE\|ATTR_HTTP_ROUTE" src/shared/presentation/plugins/tracing.plugin.ts` 輸出符合（使用 ATTR_* 常數）
    - `grep "SEMATTRS_" src/shared/presentation/plugins/tracing.plugin.ts` 無輸出（禁用 deprecated 常數）
    - `grep "SpanStatusCode.ERROR" src/shared/presentation/plugins/tracing.plugin.ts` 至少 2 個（onAfterHandle + onError）
    - `grep "onError" src/shared/presentation/plugins/tracing.plugin.ts` 輸出符合（error path 有獨立 span.end）
    - `grep "span\.end()" src/shared/presentation/plugins/tracing.plugin.ts` 至少 2 個（onAfterHandle + onError 各一）
    - `grep "BatchSpanProcessor" src/shared/presentation/plugins/tracing.plugin.ts` 無輸出
    - `bun run typecheck` 退出碼 0
  </acceptance_criteria>
  <done>tracingPlugin() 存在；onBeforeHandle 建立 span；onAfterHandle 和 onError 各自獨立結束 span；使用 ATTR_* 常數；typecheck 通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修改 app.ts（wire tracingPlugin）+ main.ts（呼叫 initTracing）</name>
  <files>src/bootstrap/app.ts, src/main.ts</files>
  <read_first>
    - src/bootstrap/app.ts（完整讀取 — 了解 plugin chain 和 import 清單，確認加入位置）
    - src/main.ts（完整讀取 — 了解現有 import 順序）
    - docs/decisions/0012-global-plugin-ordering.md（ADR 0012 canonical ordering）
    - .planning/phases/13-opentelemetry-tracing/13-PATTERNS.md（app.ts 和 main.ts 修改模式）
  </read_first>
  <behavior>
    - app.ts import 清單加入 tracingPlugin（從 '../shared/presentation/plugins/tracing.plugin'）
    - app.ts plugin chain：tracingPlugin() 在 swaggerPlugin() 後、rateLimit 條件區塊前（符合 ADR 0012 horizontal-before-feature 原則）
    - main.ts：loadConfig() → initTracing(config.OTEL_EXPORTER_OTLP_ENDPOINT) → createApp(config) 順序
    - main.ts import 加入 initTracing（從 './bootstrap/otel-init'）
    - bun test（現有測試全部通過，不破壞既有行為）
  </behavior>
  <action>
**修改 src/bootstrap/app.ts：**

在現有 import 區塊加入一行（仿照現有 plugin import 格式）：
```typescript
import { tracingPlugin } from '../shared/presentation/plugins/tracing.plugin'
```

在 plugin chain 中，在 `.use(swaggerPlugin())` 後加入：
```typescript
.use(tracingPlugin())   // ADR 0012 position 4.5: horizontal plugins before feature modules
```

即修改後的 chain 為：
```typescript
const app = new Elysia({ name: 'rigging/app' })
  .use(requestLoggerPlugin(logger))
  .use(corsPlugin())
  .use(errorHandlerPlugin(logger))
  .use(swaggerPlugin())
  .use(tracingPlugin())   // ← 新增此行
```

**修改 src/main.ts：**

加入 initTracing import，並在 loadConfig() 後、createApp() 前呼叫：

```typescript
import { loadConfig } from './bootstrap/config'
import { initTracing } from './bootstrap/otel-init'
import { createApp } from './bootstrap/app'

const config = loadConfig()
initTracing(config.OTEL_EXPORTER_OTLP_ENDPOINT)  // no-op if undefined; PROD-03 D-02

const app = createApp(config)
app.listen(config.PORT, ({ hostname, port }) => {
  // Startup log uses console.log (pino isn't in scope here; it's bound to the Elysia context).
  // Request logs use pino internally via requestLoggerPlugin.
  console.log(`[rigging] listening on http://${hostname}:${port}`)
  console.log(`[rigging] health:  http://${hostname}:${port}/health`)
  console.log(`[rigging] swagger: http://${hostname}:${port}/swagger`)
})
```

注意：保留原有的 console.log 啟動訊息（main.ts 現有慣例）。
  </action>
  <verify>
    <automated>bun run typecheck && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { tracingPlugin" src/bootstrap/app.ts` 輸出符合
    - `grep "tracingPlugin()" src/bootstrap/app.ts` 輸出符合
    - `grep "import { initTracing" src/main.ts` 輸出符合
    - `grep "initTracing(config.OTEL_EXPORTER_OTLP_ENDPOINT)" src/main.ts` 輸出符合
    - `grep "createApp" src/main.ts` 出現在 `initTracing` 之後（main.ts 中 initTracing 先於 createApp）
    - `bun run typecheck` 退出碼 0
    - `bun test` 退出碼 0（所有現有測試通過，不破壞 PROD-03 success criterion #5）
  </acceptance_criteria>
  <done>app.ts 的 plugin chain 包含 tracingPlugin()（正確位置）；main.ts 在 createApp 前呼叫 initTracing；所有現有測試通過</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elysia context → span attributes | ctx.route 和 ctx.request.method 為 framework-controlled，非 user input |
| span.setStatus message → OTLP backend | onError 中的 ctx.error?.toString() 可能含敏感資訊 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | span attributes | mitigate | D-04 最小屬性集：只記錄 route/method/status/duration；不記錄 request body、headers、auth tokens、query params（ASVS V5.1.1）|
| T-13-03-02 | Information Disclosure | onError span.setStatus message | mitigate | `ctx.error?.toString()` 可能含 internal error 詳情；error message 只送到 OTLP backend（非 response body）；ops 責任確保 OTLP backend 存取控制 |
| T-13-03-03 | Tampering | ctx.route 高基數攻擊 | accept | Elysia `context.route` 是 registered route template（如 `/api/agents/:id`），非 raw URL；攻擊者無法注入任意 route template |
| T-13-03-04 | Denial of Service | span creation overhead | accept | SimpleSpanProcessor 同步寫出，延遲為 OTLP HTTP call；無 endpoint 時為 no-op drop；可接受 |
| T-13-03-05 | Information Disclosure | PII in URL paths | mitigate | span 使用 ctx.route（template）而非 ctx.path（actual URL）；user IDs 在 path params 不會進入 span |
</threat_model>

<verification>
```bash
# Plugin 存在和核心 API
grep -E "export function tracingPlugin|as: 'global'|ATTR_HTTP|SpanStatusCode|span\.end\(\)" src/shared/presentation/plugins/tracing.plugin.ts

# app.ts plugin chain 正確
grep "tracingPlugin" src/bootstrap/app.ts

# main.ts initTracing 呼叫順序
grep -n "initTracing\|createApp\|loadConfig" src/main.ts

# 全套測試（確保不破壞現有）
bun run typecheck && bun test
```
</verification>

<success_criteria>
- `tracing.plugin.ts` 存在，匯出 `tracingPlugin()`，onBeforeHandle/onAfterHandle/onError 各有 `{ as: 'global' }`
- `app.ts` 的 `.use(tracingPlugin())` 在 swaggerPlugin 後、feature modules 前
- `main.ts` 呼叫 `initTracing(config.OTEL_EXPORTER_OTLP_ENDPOINT)` 在 `createApp` 前
- `bun run typecheck` 退出碼 0
- `bun test` 退出碼 0（所有現有測試不受影響）
</success_criteria>

<output>
完成後建立 `.planning/phases/13-opentelemetry-tracing/13-03-SUMMARY.md`
</output>
