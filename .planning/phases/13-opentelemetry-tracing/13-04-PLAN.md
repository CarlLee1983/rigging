---
phase: 13-opentelemetry-tracing
plan: 04
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
files_modified:
  - tests/unit/shared/plugins/tracing.plugin.test.ts
  - tests/integration/app-otel-tracing.test.ts
autonomous: true
requirements:
  - PROD-03

must_haves:
  truths:
    - "單元測試驗證 tracingPlugin 建立 span 並設定正確 attributes（2xx 路徑）"
    - "單元測試驗證 4xx/5xx 請求產生 SpanStatusCode.ERROR span"
    - "整合測試驗證透過 createApp() 的完整 request 產生 span（InMemorySpanExporter）"
    - "整合測試驗證 /health GET 的 span 含正確 http.route 和 http.response.status_code"
    - "整合測試驗證 error route 的 span 有 ERROR status"
    - "bun test 退出碼 0（所有新舊測試通過）"
  artifacts:
    - path: "tests/unit/shared/plugins/tracing.plugin.test.ts"
      provides: "tracingPlugin 單元測試"
      contains: "InMemorySpanExporter"
    - path: "tests/integration/app-otel-tracing.test.ts"
      provides: "OTel integration 測試（完整 createApp stack）"
      contains: "ATTR_HTTP_ROUTE"
  key_links:
    - from: "tests/integration/app-otel-tracing.test.ts"
      to: "src/bootstrap/app.ts"
      via: "createApp(TEST_CONFIG, { db: fakeDb, ... })"
      pattern: "createApp"
    - from: "tests/unit/shared/plugins/tracing.plugin.test.ts"
      to: "src/shared/presentation/plugins/tracing.plugin.ts"
      via: "tracingPlugin() import + InMemorySpanExporter"
      pattern: "tracingPlugin"
---

<objective>
建立 `tracing.plugin.ts` 的單元測試和 `app-otel-tracing.test.ts` 整合測試，驗證 PROD-03 成功標準 #1、#2、#4。

Purpose: 機器可驗證 span 確實被建立、包含正確 attributes、error path 有 ERROR status。使用 InMemorySpanExporter 避免真實 OTLP endpoint 需求（PROD-03 success criterion #5）。

Output:
- `tests/unit/shared/plugins/tracing.plugin.test.ts` — plugin 單元測試
- `tests/integration/app-otel-tracing.test.ts` — 完整 app stack 整合測試
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-opentelemetry-tracing/13-CONTEXT.md
@.planning/phases/13-opentelemetry-tracing/13-RESEARCH.md
@.planning/phases/13-opentelemetry-tracing/13-PATTERNS.md
@.planning/phases/13-opentelemetry-tracing/13-03-SUMMARY.md

<interfaces>
<!-- 現有 integration test 完整模式（analog）-->
<!-- From tests/integration/app-skeleton-smoke.test.ts -->
```typescript
import { describe, expect, test } from 'bun:test'
import { createApp } from '../../src/bootstrap/app'
import type { Config } from '../../src/bootstrap/config'
import type { IDbHealthProbe } from '../../src/health/application/ports/db-health-probe.port'
import { createFakeAuthInstance } from './auth/_helpers'

const TEST_CONFIG: Config = {
  DATABASE_URL: 'postgresql://test:test@localhost:5432/test',
  BETTER_AUTH_SECRET: 'x'.repeat(32),
  BETTER_AUTH_URL: 'http://localhost:3000',
  PORT: 3000,
  NODE_ENV: 'test',
  LOG_LEVEL: 'error',
}
const fakeDb = {} as never
function stubProbe(impl: () => Promise<'up' | 'down'>): IDbHealthProbe {
  return { probe: impl }
}
```

<!-- unit test analog 結構 -->
<!-- From tests/unit/shared/plugins/request-logger.plugin.test.ts -->
```typescript
import { describe, expect, test } from 'bun:test'
import { Elysia } from 'elysia'
// plugin-specific setup...
test('...', async () => {
  const app = new Elysia()
    .use(requestLoggerPlugin(logger))
    .get('/ping', ({ requestId }) => ({ requestId }))
  const res = await app.handle(new Request('http://localhost/ping'))
  expect(res.headers.get('x-request-id')).not.toBeNull()
})
```

<!-- InMemorySpanExporter + NodeTracerProvider 設定（from RESEARCH.md Pattern 3）-->
```typescript
import { InMemorySpanExporter, SimpleSpanProcessor } from '@opentelemetry/sdk-trace-base'
import { NodeTracerProvider } from '@opentelemetry/sdk-trace-node'
import { resourceFromAttributes } from '@opentelemetry/resources'
import { ATTR_HTTP_ROUTE, ATTR_HTTP_RESPONSE_STATUS_CODE } from '@opentelemetry/semantic-conventions'

const exporter = new InMemorySpanExporter()
const provider = new NodeTracerProvider({
  resource: resourceFromAttributes({ 'service.name': 'rigging-test' }),
  spanProcessors: [new SimpleSpanProcessor(exporter)],
})
// beforeAll: provider.register()
// afterEach: exporter.reset()
```

<!-- span 斷言 API -->
```typescript
const spans = exporter.getFinishedSpans()
expect(spans).toHaveLength(1)
expect(spans[0]?.attributes[ATTR_HTTP_ROUTE]).toBe('/health')
expect(spans[0]?.attributes[ATTR_HTTP_RESPONSE_STATUS_CODE]).toBe(200)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 tracing.plugin.test.ts（單元測試）</name>
  <files>tests/unit/shared/plugins/tracing.plugin.test.ts</files>
  <read_first>
    - tests/unit/shared/plugins/request-logger.plugin.test.ts（exact analog — test 結構和 app.handle 模式）
    - src/shared/presentation/plugins/tracing.plugin.ts（測試目標，了解實際行為）
    - .planning/phases/13-opentelemetry-tracing/13-RESEARCH.md（Pattern 3 InMemorySpanExporter 完整模式）
    - .planning/phases/13-opentelemetry-tracing/13-PATTERNS.md（tracing.plugin.test.ts Pattern Assignments）
  </read_first>
  <behavior>
    - beforeAll: NodeTracerProvider + InMemorySpanExporter 初始化，呼叫 provider.register()
    - afterEach: exporter.reset()（清空 spans，防止 test 污染）
    - Test 1: GET /ping → 200 → 1 個 span，http.route = '/ping'，http.response.status_code = 200，SpanStatusCode.OK
    - Test 2: 自訂 route 如 '/items/:id' → span 的 http.route 為 '/items/:id'（parametrized template，非實際 URL）
    - Test 3: handler throw → span 有 SpanStatusCode.ERROR，http.response.status_code 是 4xx 或 5xx
    - Test 4: 無 OTel provider 時（或 no-op provider）plugin 不 crash（防禦性）
    - 每個 test 用獨立的小 Elysia app（new Elysia().use(tracingPlugin()).get(...)）
    - 使用 ATTR_* 常數做斷言（不用字串 'http.route' 等）
  </behavior>
  <action>
建立 `tests/unit/shared/plugins/tracing.plugin.test.ts`：

**核心設計原則：**
- 每個 test 中獨立建立 Elysia app（與 request-logger 測試相同模式）
- 使用 `app.handle(new Request(...))` 進行 in-process 請求（不需真實 listen）
- SpanStatusCode 在斷言時可直接用數字（OK=1, ERROR=2）或引用 SpanStatusCode enum

**Import 區塊：**
```typescript
import { beforeAll, afterEach, describe, expect, test } from 'bun:test'
import { Elysia } from 'elysia'
import { SpanStatusCode } from '@opentelemetry/api'
import { InMemorySpanExporter, SimpleSpanProcessor } from '@opentelemetry/sdk-trace-base'
import { NodeTracerProvider } from '@opentelemetry/sdk-trace-node'
import { resourceFromAttributes } from '@opentelemetry/resources'
import {
  ATTR_HTTP_ROUTE,
  ATTR_HTTP_RESPONSE_STATUS_CODE,
  ATTR_HTTP_REQUEST_METHOD,
} from '@opentelemetry/semantic-conventions'
import { tracingPlugin } from '../../../../src/shared/presentation/plugins/tracing.plugin'
```

**Setup：**
```typescript
const exporter = new InMemorySpanExporter()
const provider = new NodeTracerProvider({
  resource: resourceFromAttributes({ 'service.name': 'rigging-unit-test' }),
  spanProcessors: [new SimpleSpanProcessor(exporter)],
})

beforeAll(() => {
  provider.register()
})

afterEach(() => {
  exporter.reset()
})
```

**Tests：**

Test 1 (GET /ping → 200 → 正確 attributes)：
```typescript
describe('tracingPlugin', () => {
  test('GET /ping produces span with http.route and status 200', async () => {
    const app = new Elysia()
      .use(tracingPlugin())
      .get('/ping', () => ({ pong: true }))

    await app.handle(new Request('http://localhost/ping'))

    const spans = exporter.getFinishedSpans()
    expect(spans).toHaveLength(1)
    expect(spans[0]?.attributes[ATTR_HTTP_ROUTE]).toBe('/ping')
    expect(spans[0]?.attributes[ATTR_HTTP_RESPONSE_STATUS_CODE]).toBe(200)
    expect(spans[0]?.attributes[ATTR_HTTP_REQUEST_METHOD]).toBe('GET')
    expect(spans[0]?.status.code).toBe(SpanStatusCode.OK)
  })
```

Test 2 (parametrized route template)：
```typescript
  test('parametrized route template is used (not actual URL)', async () => {
    const app = new Elysia()
      .use(tracingPlugin())
      .get('/items/:id', ({ params }) => ({ id: params.id }))

    await app.handle(new Request('http://localhost/items/42'))

    const spans = exporter.getFinishedSpans()
    expect(spans).toHaveLength(1)
    expect(spans[0]?.attributes[ATTR_HTTP_ROUTE]).toBe('/items/:id')
    // actual URL '/items/42' must NOT appear in the span
    expect(spans[0]?.attributes[ATTR_HTTP_ROUTE]).not.toBe('/items/42')
  })
```

Test 3 (error route → SpanStatusCode.ERROR)：
```typescript
  test('route that throws error produces span with ERROR status', async () => {
    const app = new Elysia()
      .use(tracingPlugin())
      .get('/fail', () => {
        throw new Error('intentional failure')
      })

    await app.handle(new Request('http://localhost/fail'))

    const spans = exporter.getFinishedSpans()
    expect(spans).toHaveLength(1)
    expect(spans[0]?.status.code).toBe(SpanStatusCode.ERROR)
  })
})
```

確保 `afterEach(() => { exporter.reset() })` 在每個 test 後清空 spans。
  </action>
  <verify>
    <automated>bun test tests/unit/shared/plugins/tracing.plugin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/shared/plugins/tracing.plugin.test.ts` 存在
    - `grep "InMemorySpanExporter" tests/unit/shared/plugins/tracing.plugin.test.ts` 輸出符合
    - `grep "exporter.reset()" tests/unit/shared/plugins/tracing.plugin.test.ts` 輸出符合（afterEach 清空）
    - `grep "ATTR_HTTP_ROUTE\|ATTR_HTTP_RESPONSE_STATUS_CODE" tests/unit/shared/plugins/tracing.plugin.test.ts` 輸出符合（使用常數，不用字串）
    - `grep "SpanStatusCode.ERROR" tests/unit/shared/plugins/tracing.plugin.test.ts` 輸出符合
    - `grep "'/items/:id'" tests/unit/shared/plugins/tracing.plugin.test.ts` 輸出符合（parametrized route test）
    - `bun test tests/unit/shared/plugins/tracing.plugin.test.ts` 退出碼 0，所有 tests PASS
  </acceptance_criteria>
  <done>tracing.plugin.test.ts 存在，至少 3 個測試案例（200 路徑、parametrized route、error 路徑），全部通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 app-otel-tracing.test.ts（整合測試）</name>
  <files>tests/integration/app-otel-tracing.test.ts</files>
  <read_first>
    - tests/integration/app-skeleton-smoke.test.ts（exact analog — createApp + fakeDb + stubProbe 模式）
    - tests/integration/auth/_helpers.ts（createFakeAuthInstance 可用）
    - src/bootstrap/config.ts（Config 型別，TEST_CONFIG 需要的欄位）
    - .planning/phases/13-opentelemetry-tracing/13-RESEARCH.md（Pattern 3 整合測試完整模式）
    - .planning/phases/13-opentelemetry-tracing/13-PATTERNS.md（app-otel-tracing.test.ts Pattern Assignments）
  </read_first>
  <behavior>
    - beforeAll: NodeTracerProvider + InMemorySpanExporter 初始化，provider.register()
    - afterEach: exporter.reset()
    - TEST_CONFIG 不含 OTEL_EXPORTER_OTLP_ENDPOINT（測試用 InMemorySpanExporter，不走 OTLP）
    - Test 1: GET /health → 200 → 1 個 span，ATTR_HTTP_ROUTE = '/health'，ATTR_HTTP_RESPONSE_STATUS_CODE = 200
    - Test 2: 觸發 error route → span 有 SpanStatusCode.ERROR（驗證 PROD-03 success criterion #4）
    - Test 3: 驗證 span name 格式為 'GET /health'（method + route）
    - Test 4: 驗證 'http.request.duration' 屬性存在且為數字
    - 使用 createApp(TEST_CONFIG, { db: fakeDb, authInstance: createFakeAuthInstance(), probe: stubProbe(...) })
    - 使用 ATTR_* 常數做斷言，不用硬碼字串
  </behavior>
  <action>
建立 `tests/integration/app-otel-tracing.test.ts`：

**Import 區塊（仿 app-skeleton-smoke.test.ts 格式）：**
```typescript
import { beforeAll, afterEach, describe, expect, test } from 'bun:test'
import { SpanStatusCode } from '@opentelemetry/api'
import { InMemorySpanExporter, SimpleSpanProcessor } from '@opentelemetry/sdk-trace-base'
import { NodeTracerProvider } from '@opentelemetry/sdk-trace-node'
import { resourceFromAttributes } from '@opentelemetry/resources'
import {
  ATTR_HTTP_ROUTE,
  ATTR_HTTP_RESPONSE_STATUS_CODE,
  ATTR_HTTP_REQUEST_METHOD,
} from '@opentelemetry/semantic-conventions'
import { createApp } from '../../src/bootstrap/app'
import type { Config } from '../../src/bootstrap/config'
import type { IDbHealthProbe } from '../../src/health/application/ports/db-health-probe.port'
import { createFakeAuthInstance } from './auth/_helpers'
```

**Setup（完全仿 app-skeleton-smoke.test.ts）：**
```typescript
// TEST_CONFIG: no OTEL_EXPORTER_OTLP_ENDPOINT — tests use InMemorySpanExporter, not OTLP
const TEST_CONFIG: Config = {
  DATABASE_URL: 'postgresql://test:test@localhost:5432/test',
  BETTER_AUTH_SECRET: 'x'.repeat(32),
  BETTER_AUTH_URL: 'http://localhost:3000',
  PORT: 3000,
  NODE_ENV: 'test',
  LOG_LEVEL: 'error',
}
const fakeDb = {} as never
function stubProbe(impl: () => Promise<'up' | 'down'>): IDbHealthProbe {
  return { probe: impl }
}

const exporter = new InMemorySpanExporter()
const provider = new NodeTracerProvider({
  resource: resourceFromAttributes({ 'service.name': 'rigging-integration-test' }),
  spanProcessors: [new SimpleSpanProcessor(exporter)],
})

beforeAll(() => {
  provider.register()
})

afterEach(() => {
  exporter.reset()
})
```

**Tests：**

Test 1 (GET /health → span 驗證 PROD-03 #1, #2)：
```typescript
describe('OTel HTTP spans — integration (PROD-03)', () => {
  test('GET /health produces span with correct route, method and status (PROD-03 #1, #2)', async () => {
    const app = createApp(TEST_CONFIG, {
      db: fakeDb,
      authInstance: createFakeAuthInstance(),
      probe: stubProbe(() => Promise.resolve('up')),
    })

    await app.handle(new Request('http://localhost/health'))

    const spans = exporter.getFinishedSpans()
    expect(spans.length).toBeGreaterThanOrEqual(1)
    const healthSpan = spans.find(
      (s) => s.attributes[ATTR_HTTP_ROUTE] === '/health',
    )
    expect(healthSpan).toBeDefined()
    expect(healthSpan?.attributes[ATTR_HTTP_REQUEST_METHOD]).toBe('GET')
    expect(healthSpan?.attributes[ATTR_HTTP_RESPONSE_STATUS_CODE]).toBe(200)
    expect(typeof healthSpan?.attributes['http.request.duration']).toBe('number')
  })
```

Test 2 (error route → ERROR status，PROD-03 #4)：
```typescript
  test('error route produces span with ERROR status (PROD-03 #4)', async () => {
    const app = createApp(TEST_CONFIG, {
      db: fakeDb,
      authInstance: createFakeAuthInstance(),
      probe: stubProbe(() => Promise.resolve('up')),
    }).get('/demo-error', () => {
      throw new Error('test error')
    })

    await app.handle(new Request('http://localhost/demo-error'))

    const spans = exporter.getFinishedSpans()
    const errorSpan = spans.find(
      (s) => s.attributes[ATTR_HTTP_ROUTE] === '/demo-error',
    )
    expect(errorSpan).toBeDefined()
    expect(errorSpan?.status.code).toBe(SpanStatusCode.ERROR)
  })
```

Test 3 (span name 格式)：
```typescript
  test('span name format is "METHOD /route"', async () => {
    const app = createApp(TEST_CONFIG, {
      db: fakeDb,
      authInstance: createFakeAuthInstance(),
      probe: stubProbe(() => Promise.resolve('up')),
    })

    await app.handle(new Request('http://localhost/health'))

    const spans = exporter.getFinishedSpans()
    const healthSpan = spans.find(
      (s) => s.attributes[ATTR_HTTP_ROUTE] === '/health',
    )
    expect(healthSpan?.name).toBe('GET /health')
  })
})
```

注意：`NODE_ENV: 'test'` 時 rateLimit 被跳過（app.ts 現有邏輯），因此測試 span 計數更可預期。
  </action>
  <verify>
    <automated>bun test tests/integration/app-otel-tracing.test.ts && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/app-otel-tracing.test.ts` 存在
    - `grep "InMemorySpanExporter" tests/integration/app-otel-tracing.test.ts` 輸出符合
    - `grep "exporter.reset()" tests/integration/app-otel-tracing.test.ts` 輸出符合（afterEach）
    - `grep "ATTR_HTTP_ROUTE\|ATTR_HTTP_RESPONSE_STATUS_CODE" tests/integration/app-otel-tracing.test.ts` 輸出符合
    - `grep "SpanStatusCode.ERROR" tests/integration/app-otel-tracing.test.ts` 輸出符合（error path test）
    - `grep "createFakeAuthInstance" tests/integration/app-otel-tracing.test.ts` 輸出符合
    - `grep "http.request.duration" tests/integration/app-otel-tracing.test.ts` 輸出符合（latency attribute 驗證）
    - `bun test tests/integration/app-otel-tracing.test.ts` 退出碼 0，所有 tests PASS
    - `bun test` 退出碼 0（全套測試通過，包含既有測試）
  </acceptance_criteria>
  <done>app-otel-tracing.test.ts 存在，至少 3 個整合測試案例（成功路徑 span attributes、error span、span name 格式），全套 `bun test` 通過</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → InMemorySpanExporter | 測試環境使用 in-memory exporter，無網路連線需求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | test span data | accept | InMemorySpanExporter 只存在於測試 process 記憶體，不傳輸至外部；測試完成後 GC |
| T-13-04-02 | Tampering | exporter 跨 test 污染 | mitigate | `afterEach(() => exporter.reset())` 確保每個 test 從空白 exporter 開始；每個 test 獨立斷言 |
| T-13-04-03 | Denial of Service | global TracerProvider 被 test 覆蓋 | accept | `provider.register()` 在 beforeAll 執行一次；多個 test suite 可能衝突，但 bun test 並行隔離模式可接受 |
</threat_model>

<verification>
```bash
# 單元測試
bun test tests/unit/shared/plugins/tracing.plugin.test.ts

# 整合測試
bun test tests/integration/app-otel-tracing.test.ts

# 全套測試（確保不破壞既有）
bun test

# 確認 ATTR_* 常數使用（不用 deprecated 字串）
grep "SEMATTRS_\|'http\.status_code'\|'http\.method'" tests/unit/shared/plugins/tracing.plugin.test.ts tests/integration/app-otel-tracing.test.ts
# 應無輸出
```
</verification>

<success_criteria>
- `tests/unit/shared/plugins/tracing.plugin.test.ts` 存在，所有測試通過
- `tests/integration/app-otel-tracing.test.ts` 存在，驗證 PROD-03 成功標準 #1、#2、#4
- 整合測試使用 InMemorySpanExporter（不需真實 OTLP endpoint）
- `bun test` 全套（含新舊測試）退出碼 0
- PROD-03 成功標準 #5 得到機器驗證（現有測試不受影響）
</success_criteria>

<output>
完成後建立 `.planning/phases/13-opentelemetry-tracing/13-04-SUMMARY.md`
</output>
